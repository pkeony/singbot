// ===== Polyfills (ES5 환경 호환) =====
(function () {
  if (!Array.prototype.find) {
    Object.defineProperty(Array.prototype, 'find', {
      value: function (predicate) {
        if (this == null) throw new TypeError('"this" is null or not defined');
        var o = Object(this);
        var len = o.length >>> 0;
        if (typeof predicate !== 'function') throw new TypeError('predicate must be a function');
        var thisArg = arguments[1];
        var k = 0;
        while (k < len) {
          var kValue = o[k];
          if (predicate.call(thisArg, kValue, k, o)) return kValue;
          k++;
        }
        return undefined;
      },
    });
  }
  if (!Array.prototype.findIndex) {
    Object.defineProperty(Array.prototype, 'findIndex', {
      value: function (predicate) {
        if (this == null) throw new TypeError('"this" is null or not defined');
        var o = Object(this);
        var len = o.length >>> 0;
        if (typeof predicate !== 'function') throw new TypeError('predicate must be a function');
        var thisArg = arguments[1];
        var k = 0;
        while (k < len) {
          var kValue = o[k];
          if (predicate.call(thisArg, kValue, k, o)) return k;
          k++;
        }
        return -1;
      },
    });
  }
  if (!Array.prototype.every) {
    Object.defineProperty(Array.prototype, 'every', {
      value: function (callbackfn, thisArg) {
        'use strict';
        var T, k;
        if (this == null) throw new TypeError('this is null or not defined');
        var O = Object(this);
        var len = O.length >>> 0;
        if (typeof callbackfn !== 'function') throw new TypeError();
        if (arguments.length > 1) T = thisArg;
        k = 0;
        while (k < len) {
          var kValue;
          if (k in O) {
            kValue = O[k];
            var testResult = callbackfn.call(T, kValue, k, O);
            if (!testResult) return false;
          }
          k++;
        }
        return true;
      },
    });
  }
  if (!Array.prototype.reduce) {
    Object.defineProperty(Array.prototype, 'reduce', {
      value: function (callback) {
        if (this === null) throw new TypeError('Array.prototype.reduce called on null or undefined');
        if (typeof callback !== 'function') throw new TypeError(callback + ' is not a function');
        var o = Object(this);
        var len = o.length >>> 0;
        var k = 0;
        var value;
        if (arguments.length >= 2) {
          value = arguments[1];
        } else {
          while (k < len && !(k in o)) k++;
          if (k >= len) throw new TypeError('Reduce of empty array with no initial value');
          value = o[k++];
        }
        while (k < len) {
          if (k in o) value = callback(value, o[k], k, o);
          k++;
        }
        return value;
      },
    });
  }
  if (!Array.prototype.filter) {
    Object.defineProperty(Array.prototype, 'filter', {
      value: function (func, thisArg) {
        'use strict';
        if (!((typeof func === 'Function' || typeof func === 'function') && this)) throw new TypeError();
        var len = this.length >>> 0, res = new Array(len), t = this, c = 0, i = -1;
        var thisContext = thisArg === undefined ? this : thisArg;
        while (++i !== len) {
          if (i in this) {
            if (func.call(thisContext, t[i], i, t)) res[c++] = t[i];
          }
        }
        res.length = c;
        return res;
      },
    });
  }
  if (typeof String.prototype.startsWith !== 'function') {
    String.prototype.startsWith = function (str) {
      return this.slice(0, str.length) === str;
    };
  }
  if (!Array.prototype.map) {
    Object.defineProperty(Array.prototype, 'map', {
      value: function (callback, thisArg) {
        var T, A, k;
        if (this == null) throw new TypeError('this is null or not defined');
        var O = Object(this);
        var len = O.length >>> 0;
        if (typeof callback !== 'function') throw new TypeError(callback + ' is not a function');
        if (arguments.length > 1) T = thisArg;
        A = new Array(len);
        k = 0;
        while (k < len) {
          var kValue, mappedValue;
          if (k in O) {
            kValue = O[k];
            mappedValue = callback.call(T, kValue, k, O);
            A[k] = mappedValue;
          }
          k++;
        }
        return A;
      },
    });
  }
  if (!Object.values) {
    Object.values = function (obj) {
      var values = [];
      for (var key in obj) {
        if (obj.hasOwnProperty(key)) values.push(obj[key]);
      }
      return values;
    };
  }
  if (!Array.prototype.some) {
    Object.defineProperty(Array.prototype, 'some', {
      value: function (fun) {
        'use strict';
        if (this == null) throw new TypeError('Array.prototype.some called on null or undefined');
        if (typeof fun !== 'function') throw new TypeError();
        var t = Object(this);
        var len = t.length >>> 0;
        var thisArg = arguments.length >= 2 ? arguments[1] : void 0;
        for (var i = 0; i < len; i++) {
          if (i in t && fun.call(thisArg, t[i], i, t)) return true;
        }
        return false;
      },
    });
  }
})();
// ===== 명언 DB =====

var QUOTES_DB = [
  // ===== 동기부여 =====
  { text: "오늘 할 수 있는 일을 내일로 미루지 마라.", author: "벤자민 프랭클린" },
  { text: "실패는 성공의 어머니다.", author: "토마스 에디슨" },
  { text: "천 리 길도 한 걸음부터.", author: "노자" },
  { text: "시작이 반이다.", author: "아리스토텔레스" },
  { text: "할 수 있다고 믿으면 이미 반은 이룬 것이다.", author: "시어도어 루스벨트" },
  { text: "넘어지는 것은 실패가 아니다. 넘어진 채로 있는 것이 실패다.", author: "메리 픽포드" },
  { text: "포기하지 않으면 실패도 없다.", author: "앤드류 카네기" },
  { text: "나는 불가능이라는 단어를 모른다.", author: "나폴레옹" },
  { text: "매일 조금씩 나아지면, 결국 큰 일이 이루어진다.", author: "존 우든" },
  { text: "가만히 있으면 가만히 있는 만큼의 결과를 얻는다.", author: "이소룡" },
  { text: "행동은 모든 성공의 기본 열쇠이다.", author: "파블로 피카소" },
  { text: "위대한 일을 하려면 그 일을 사랑해야 한다.", author: "스티브 잡스" },
  { text: "꿈을 꾸는 것에는 돈이 들지 않는다.", author: "월트 디즈니" },
  { text: "지금 하지 않으면 영원히 하지 않게 된다.", author: "괴테" },
  { text: "열정 없이 이루어진 위대한 일은 없다.", author: "헤겔" },
  { text: "완벽을 기다리다 보면 아무것도 완성되지 않는다.", author: "볼테르" },
  { text: "노력 없이 얻은 것은 없다.", author: "호라티우스" },
  { text: "큰 목표를 이루려면 허락을 구하지 마라.", author: "미상" },
  { text: "해보지 않으면 할 수 있는지 없는지도 모른다.", author: "미상" },
  { text: "모든 성취는 해보겠다는 결심에서 시작된다.", author: "미상" },
  { text: "오늘의 나는 어제의 나보다 나은 사람이면 된다.", author: "오프라 윈프리" },
  { text: "남과 비교하지 말고, 어제의 나와 비교하라.", author: "조던 피터슨" },
  { text: "사람은 자기가 생각하는 대로 된다.", author: "간디" },
  { text: "성공은 최종적인 것이 아니며, 실패는 치명적인 것이 아니다. 중요한 것은 계속하는 용기이다.", author: "처칠" },
  { text: "두려움을 안고 가라. 용기란 두려움이 없는 것이 아니라 두려움을 극복하는 것이다.", author: "넬슨 만델라" },

  // ===== 인생/삶 =====
  { text: "인생은 짧고 예술은 길다.", author: "히포크라테스" },
  { text: "나는 생각한다 고로 존재한다.", author: "데카르트" },
  { text: "삶이 있는 한 희망은 있다.", author: "키케로" },
  { text: "오늘이라는 날은 두 번 다시 오지 않는다.", author: "단테" },
  { text: "어제는 이미 지나갔고, 내일은 아직 오지 않았다. 우리에게 있는 건 오늘뿐이다.", author: "마더 테레사" },
  { text: "변화하지 않으면 성장도 없고, 성장하지 않으면 진정한 삶이 아니다.", author: "게일 쉬히" },
  { text: "세상은 고통으로 가득하지만, 그것을 이겨내는 사람들로도 가득하다.", author: "헬렌 켈러" },
  { text: "진정한 발견의 여행은 새로운 풍경을 찾는 것이 아니라 새로운 눈을 갖는 것이다.", author: "마르셀 프루스트" },
  { text: "지금 이 순간에 집중하라. 그것만이 당신이 확실히 가진 것이다.", author: "부처" },
  { text: "과거에서 배우고, 현재를 살고, 미래를 희망하라.", author: "아인슈타인" },
  { text: "단순함이야말로 궁극의 정교함이다.", author: "레오나르도 다 빈치" },
  { text: "지식에 투자하는 것이 가장 좋은 이자를 낸다.", author: "벤자민 프랭클린" },
  { text: "배움에는 왕도가 없다.", author: "유클리드" },
  { text: "아는 것이 힘이다.", author: "프랜시스 베이컨" },
  { text: "뜻이 있는 곳에 길이 있다.", author: "링컨" },
  { text: "행복은 습관이다. 그것을 몸에 지니라.", author: "허버드" },
  { text: "웃으면 온 세상이 따라 웃고, 울면 혼자 운다.", author: "엘라 휠러 윌콕스" },
  { text: "좋은 친구를 얻는 유일한 방법은 먼저 좋은 친구가 되는 것이다.", author: "에머슨" },
  { text: "현재에 충실하면 미래는 저절로 밝아진다.", author: "맹자" },
  { text: "세상에서 가장 용감한 행동은 스스로 생각하는 것이다.", author: "코코 샤넬" },

  // ===== 성공/도전 =====
  { text: "성공의 비결은 단 한 가지, 잘할 수 있는 일에 광적으로 집중하는 것이다.", author: "톰 모나건" },
  { text: "작은 기회로부터 종종 위대한 업적이 시작된다.", author: "데모스테네스" },
  { text: "나 자신에 대한 자신감을 잃으면 온 세상이 나의 적이 된다.", author: "에머슨" },
  { text: "가장 큰 영광은 한 번도 실패하지 않음이 아니라 실패할 때마다 다시 일어서는 데에 있다.", author: "공자" },
  { text: "쉬지 않고 달리는 사람보다 제 방향을 아는 사람이 더 빨리 도착한다.", author: "프랜시스 베이컨" },
  { text: "기회는 준비된 자에게 온다.", author: "파스퇴르" },
  { text: "실패한 곳에서 다시 시작하라. 그것이 성공의 지름길이다.", author: "헨리 포드" },
  { text: "도전하지 않는 것이 가장 큰 실패다.", author: "마크 저커버그" },
  { text: "불가능이란 노력하지 않는 자의 변명이다.", author: "나폴레옹" },
  { text: "승리는 가장 끈기 있는 자에게 돌아간다.", author: "나폴레옹" },
  { text: "세상에 불가능이란 없다. 다만 아직 방법을 찾지 못한 것뿐이다.", author: "토마스 에디슨" },
  { text: "먼저 자신을 비웃어라. 다른 사람이 비웃기 전에.", author: "엘사 맥스웰" },
  { text: "최고가 되려면, 최고에게 배워라.", author: "미상" },
  { text: "한 번의 실패와 영원한 실패를 혼동하지 마라.", author: "스콧 피츠제럴드" },
  { text: "미래를 예측하는 최선의 방법은 미래를 만들어내는 것이다.", author: "앨런 케이" },

  // ===== 지혜/철학 =====
  { text: "소크라테스의 유일한 지혜는 자신이 아무것도 모른다는 것을 아는 것이었다.", author: "플라톤" },
  { text: "지혜로운 자는 물처럼 다투지 않는다.", author: "노자" },
  { text: "세 사람이 걸으면 반드시 나의 스승이 있다.", author: "공자" },
  { text: "가장 어두운 밤도 끝이 있고, 태양은 반드시 떠오른다.", author: "빅토르 위고" },
  { text: "참을 수 없는 고통은 없고, 영원한 행복도 없다.", author: "세르반테스" },
  { text: "깊이 뿌리박은 나무는 바람에 흔들리지 않는다.", author: "용비어천가" },
  { text: "꽃이 피는 것은 꽃의 노력이 아니라 뿌리의 노력이다.", author: "미상" },
  { text: "비 온 뒤에 땅이 굳어진다.", author: "속담" },
  { text: "고생 끝에 낙이 온다.", author: "속담" },
  { text: "티끌 모아 태산.", author: "속담" },
  { text: "호랑이 굴에 들어가야 호랑이를 잡는다.", author: "속담" },
  { text: "바늘 도둑이 소 도둑 된다.", author: "속담" },
  { text: "급하면 돌아가라.", author: "속담" },
  { text: "우물을 파도 한 우물을 파라.", author: "속담" },
  { text: "세 살 버릇 여든까지 간다.", author: "속담" },
  { text: "원숭이도 나무에서 떨어진다.", author: "속담" },
  { text: "낮말은 새가 듣고 밤말은 쥐가 듣는다.", author: "속담" },
  { text: "공든 탑이 무너지랴.", author: "속담" },
  { text: "가는 말이 고와야 오는 말이 곱다.", author: "속담" },
  { text: "백지장도 맞들면 낫다.", author: "속담" },
  { text: "돌다리도 두들겨 보고 건너라.", author: "속담" },
  { text: "소 잃고 외양간 고친다.", author: "속담" },

  // ===== 사랑/관계 =====
  { text: "사랑하는 것은 천국을 살짝 엿보는 것이다.", author: "카렌 선드" },
  { text: "사랑은 소유가 아니라 감사하는 것이다.", author: "미상" },
  { text: "세상에서 가장 좋은 것은 서로 사랑하는 것이다.", author: "빅토르 위고" },
  { text: "진정한 사랑은 끝이 없다. 끝이 있다면 그것은 사랑이 아니다.", author: "미상" },
  { text: "혼자 꾸는 꿈은 꿈에 불과하지만, 함께 꾸는 꿈은 현실이 된다.", author: "오노 요코" },
  { text: "가족은 선택할 수 없지만, 사랑할 수는 있다.", author: "미상" },
  { text: "친구란 네가 실수해도 절대 변하지 않는 사람이다.", author: "미상" },
  { text: "말 한마디에 천 냥 빚을 갚는다.", author: "속담" },
  { text: "웃는 얼굴에 침 뱉으랴.", author: "속담" },

  // ===== 유머/위트 =====
  { text: "인생은 자전거를 타는 것과 같다. 균형을 잡으려면 움직여야 한다.", author: "아인슈타인" },
  { text: "나는 실패한 것이 아니다. 작동하지 않는 10,000가지 방법을 발견한 것이다.", author: "토마스 에디슨" },
  { text: "미쳐야 미친다.", author: "정약용" },
  { text: "계란으로 바위치기라도 해봐야 안다.", author: "미상" },
  { text: "돈이 전부는 아니지만, 전부가 아닌 것 중에서는 최고다.", author: "미상" },
  { text: "일찍 일어나는 새가 벌레를 잡는다. 하지만 일찍 일어나는 벌레는 잡힌다.", author: "미상" },
  { text: "인생에 리모컨은 없다. 직접 일어나서 채널을 바꿔라.", author: "미상" },
  { text: "오늘 하루 안 웃으면 손해 본 거다.", author: "미상" },
  { text: "걱정의 90%는 일어나지 않는다.", author: "마크 트웨인" },
  { text: "내일의 나에게 미루지 마라. 내일의 나도 하기 싫다.", author: "미상" },
  { text: "월요일을 좋아하는 사람은 없지만, 금요일을 싫어하는 사람도 없다.", author: "미상" },
  { text: "다이어트는 내일부터.", author: "전 세계 공통" },
  { text: "잠이 보약이다.", author: "속담" },

  // ===== 자기계발 =====
  { text: "독서는 마음의 양식이다.", author: "키케로" },
  { text: "배우고 때때로 익히면 또한 기쁘지 아니한가.", author: "공자" },
  { text: "아침에 도를 들으면 저녁에 죽어도 좋다.", author: "공자" },
  { text: "스스로에게 정직한 것이 성공의 첫 걸음이다.", author: "미상" },
  { text: "작은 성공이 모여 큰 성공이 된다.", author: "미상" },
  { text: "습관이 운명을 만든다.", author: "미상" },
  { text: "오늘 읽는 책이 내일의 나를 만든다.", author: "미상" },
  { text: "변명하는 시간에 한 걸음이라도 더 나아가라.", author: "미상" },
  { text: "비교는 기쁨을 훔치는 도둑이다.", author: "시어도어 루스벨트" },
  { text: "좋은 습관은 형성하기 어렵지만, 함께 살기 쉽다.", author: "미상" },
  { text: "당신의 시간은 한정되어 있다. 남의 인생을 사느라 낭비하지 마라.", author: "스티브 잡스" },
  { text: "모든 전문가도 한때는 초보자였다.", author: "미상" },
  { text: "실력은 배신하지 않는다.", author: "미상" },
  { text: "오늘의 땀이 내일의 보석이 된다.", author: "미상" },

  // ===== 긍정/행복 =====
  { text: "행복은 목적지가 아니라 여행이다.", author: "미상" },
  { text: "감사할 줄 아는 사람이 진정으로 부유한 사람이다.", author: "미상" },
  { text: "작은 것에 감사하면 큰 것이 찾아온다.", author: "미상" },
  { text: "오늘 하루, 충분히 잘했다.", author: "미상" },
  { text: "당신은 생각보다 강하다.", author: "미상" },
  { text: "힘들 때 웃을 수 있다면, 당신은 이미 강한 사람이다.", author: "미상" },
  { text: "좋은 날이 올 거라 믿으면, 정말로 좋은 날이 온다.", author: "미상" },
  { text: "햇살이 비치지 않는 곳에서도 꽃은 핀다.", author: "미상" },
  { text: "비가 그치면 무지개가 뜬다.", author: "미상" },
  { text: "매일이 새로운 시작이다.", author: "미상" },
  { text: "당신의 미소가 세상을 바꿀 수 있다.", author: "미상" },
  { text: "지금 이 순간이 가장 젊은 순간이다.", author: "미상" },
  { text: "후회 없는 하루를 보내자.", author: "미상" },
  { text: "나를 사랑하는 것이 모든 것의 시작이다.", author: "미상" },
  { text: "오늘도 수고한 나에게 박수를.", author: "미상" },

  // ===== 리더십/일 =====
  { text: "리더는 길을 알고, 길을 가고, 길을 보여주는 사람이다.", author: "존 맥스웰" },
  { text: "혼자 가면 빨리 가고, 함께 가면 멀리 간다.", author: "아프리카 속담" },
  { text: "문제를 만든 것과 같은 수준의 사고로는 문제를 해결할 수 없다.", author: "아인슈타인" },
  { text: "기회는 일하는 모습으로 나타난다.", author: "토마스 에디슨" },
  { text: "위대한 일은 충동이 아닌 작은 일들의 결합으로 이루어진다.", author: "빈센트 반 고흐" },
  { text: "일을 시작하는 방법은 말을 멈추고 행동하는 것이다.", author: "월트 디즈니" },
  { text: "계획 없는 목표는 단지 소원일 뿐이다.", author: "생텍쥐페리" },
  { text: "오늘의 할 일을 내일로 미루면, 내일은 오늘보다 더 바빠진다.", author: "미상" },
  { text: "프로와 아마추어의 차이는 꾸준함이다.", author: "미상" },
  { text: "결과보다 과정이 중요하다. 과정이 좋으면 결과는 따라온다.", author: "미상" },

  // ===== 한국 명언 =====
  { text: "하늘은 스스로 돕는 자를 돕는다.", author: "격언" },
  { text: "천재는 1%의 영감과 99%의 노력으로 만들어진다.", author: "토마스 에디슨" },
  { text: "될 때까지 하면 된다.", author: "정주영" },
  { text: "시련은 있어도 실패는 없다.", author: "정주영" },
  { text: "이봐, 해봤어?", author: "정주영" },
  { text: "꿈은 이루어진다.", author: "2002 월드컵" },
  { text: "나는 나비의 꿈을 꾸는 장자인가, 장자의 꿈을 꾸는 나비인가.", author: "장자" },
  { text: "물은 높은 곳에서 낮은 곳으로 흐르고, 사람은 낮은 곳에서 높은 곳을 향한다.", author: "미상" },
  { text: "뿌린 대로 거둔다.", author: "성경" },
  { text: "많이 가진 자가 부자가 아니라, 적게 필요한 자가 부자다.", author: "미상" },

  // ===== 자연/계절 =====
  { text: "봄이 오지 않는 겨울은 없다.", author: "미상" },
  { text: "모든 날이 좋은 날이다.", author: "운문선사" },
  { text: "구름 위에는 항상 태양이 빛나고 있다.", author: "미상" },
  { text: "작은 풀잎에도 우주가 깃들어 있다.", author: "월트 휘트먼" },
  { text: "자연은 서두르지 않지만, 모든 것을 이루어낸다.", author: "노자" },
  { text: "나무는 바람이 불어도 뿌리가 깊으면 쓰러지지 않는다.", author: "미상" },
  { text: "별을 보려면 어둠을 견뎌야 한다.", author: "미상" },

  // ===== 현대 명언 =====
  { text: "Stay hungry, stay foolish. 갈망하라, 우직하라.", author: "스티브 잡스" },
  { text: "Done is better than perfect. 완벽보다 완성이 낫다.", author: "마크 저커버그" },
  { text: "Move fast and break things. 빠르게 움직이고 부숴라.", author: "마크 저커버그" },
  { text: "Talk is cheap. Show me the code. 말은 쉽다. 코드를 보여줘.", author: "리누스 토르발스" },
  { text: "어떤 일이든 처음엔 불가능해 보이고, 그 다음엔 어려워 보이고, 마지막엔 쉬워 보인다.", author: "넬슨 만델라" },
  { text: "성공한 사람이 되기보다 가치 있는 사람이 되라.", author: "아인슈타인" },
  { text: "삶은 가까이서 보면 비극이고, 멀리서 보면 희극이다.", author: "찰리 채플린" },
  { text: "상상력은 지식보다 중요하다.", author: "아인슈타인" },
  { text: "간결함은 지혜의 정수이다.", author: "셰익스피어" },
  { text: "최고의 복수는 성공이다.", author: "프랭크 시나트라" },
  { text: "배고픈 소크라테스가 배부른 돼지보다 낫다.", author: "존 스튜어트 밀" },
  { text: "역경은 재능을 드러내고, 순경은 재능을 숨긴다.", author: "호라티우스" },
  { text: "생각을 바꾸면 세상이 바뀐다.", author: "노먼 빈센트 필" },
  { text: "오직 자기 자신만이 자기 인생의 주인공이다.", author: "미상" },

  // ===== 아침/하루 =====
  { text: "좋은 아침은 좋은 하루의 시작이다.", author: "미상" },
  { text: "오늘 하루도 감사하며 시작하자.", author: "미상" },
  { text: "어제보다 나은 오늘을 만들자.", author: "미상" },
  { text: "하루를 시작하기에 가장 좋은 시간은 바로 지금이다.", author: "미상" },
  { text: "새벽 공기처럼 상쾌한 하루를 보내세요.", author: "미상" },
  { text: "커피 한 잔의 여유가 하루를 바꾼다.", author: "미상" },
  { text: "오늘의 나에게 응원의 박수를 보내자.", author: "미상" },
  { text: "힘든 하루를 보내더라도, 내일은 새 날이다.", author: "미상" },
  { text: "오늘 하루, 나답게 살자.", author: "미상" },
  { text: "작은 행복을 찾는 것이 큰 행복의 비결이다.", author: "미상" },
  { text: "하루 한 번은 거울을 보며 웃어보자.", author: "미상" },
  { text: "산책 한 번이 인생을 바꿀 수도 있다.", author: "미상" },
  { text: "좋은 음악, 좋은 사람, 좋은 음식. 이 세 가지면 충분하다.", author: "미상" },
  { text: "오늘 본 하늘은 어제와 같은 하늘이 아니다.", author: "미상" },
  { text: "지금 이 순간을 즐기세요. 이 순간이 바로 당신의 인생입니다.", author: "미상" },

  // ===== 도전/극복 =====
  { text: "폭풍이 지나간 뒤에야 무지개를 볼 수 있다.", author: "미상" },
  { text: "넘어져도 일어서는 사람이 진짜 강한 사람이다.", author: "미상" },
  { text: "오르막길이 힘든 이유는 정상이 가까워지고 있기 때문이다.", author: "미상" },
  { text: "실패해도 괜찮다. 다시 도전하면 된다.", author: "미상" },
  { text: "지금 겪는 고통이 미래의 나를 만든다.", author: "미상" },
  { text: "포기는 배추 셀 때만 하는 거다.", author: "미상" },
  { text: "지금의 고생이 나중의 추억이 된다.", author: "미상" },
  { text: "못 해도 된다. 안 하면 안 된다.", author: "미상" },
  { text: "실패를 두려워하지 말고, 도전하지 않는 것을 두려워하라.", author: "미상" },
  { text: "눈물 없이는 빵을 먹지 못한 자, 인생의 참맛을 알지 못한다.", author: "괴테" },

  // ===== 건강/마음 =====
  { text: "건강은 제일의 재산이다.", author: "에머슨" },
  { text: "몸이 건강해야 마음도 건강하다.", author: "미상" },
  { text: "웃음은 최고의 약이다.", author: "미상" },
  { text: "하루 30분 걸으면 인생이 바뀐다.", author: "미상" },
  { text: "잘 먹고 잘 자는 것이 최고의 자기관리다.", author: "미상" },
  { text: "마음이 편해야 일도 잘 된다.", author: "미상" },
  { text: "오늘 하루, 자신에게 조금 더 너그러워져 보세요.", author: "미상" },
  { text: "스트레스는 해결하는 것이 아니라 관리하는 것이다.", author: "미상" },
  { text: "깊은 호흡 한 번이 마음을 바꿀 수 있다.", author: "미상" },

  // ===== 돈/부 =====
  { text: "부자는 돈을 관리하고, 가난한 사람은 돈에 관리당한다.", author: "미상" },
  { text: "돈은 좋은 하인이지만, 나쁜 주인이다.", author: "프랜시스 베이컨" },
  { text: "절약은 큰 수입이다.", author: "키케로" },
  { text: "투자의 제1원칙은 절대 돈을 잃지 않는 것이다.", author: "워렌 버핏" },
  { text: "남에게 쓰는 돈이 가장 값진 투자다.", author: "미상" },

  // ===== 시간 =====
  { text: "시간은 금이다.", author: "벤자민 프랭클린" },
  { text: "잃어버린 시간은 다시 돌아오지 않는다.", author: "미상" },
  { text: "시간이 약이다.", author: "속담" },
  { text: "하루하루가 선물이다. 그래서 오늘을 present라고 부른다.", author: "쿵푸팬더" },
  { text: "지금 이 순간이 인생에서 가장 젊은 순간이다. 지금 시작하라.", author: "미상" },
  { text: "바쁜 것과 생산적인 것은 다르다.", author: "팀 페리스" },

  // ===== 꿈 =====
  { text: "꿈을 크게 가져라. 어차피 꾸는 거.", author: "도널드 트럼프" },
  { text: "꿈이 없으면 죽은 것과 같다.", author: "마크 트웨인" },
  { text: "꿈을 향해 가는 길에 실패란 없다. 배움만 있을 뿐.", author: "미상" },
  { text: "당신의 꿈이 당신을 이끌게 하라.", author: "미상" },
  { text: "세상의 모든 위대한 것들은 꿈에서 시작되었다.", author: "미상" },
  { text: "꿈꾸는 자만이 별에 닿을 수 있다.", author: "미상" },

  // ===== 감성 =====
  { text: "가끔은 아무 생각 없이 하늘을 바라보는 것도 좋다.", author: "미상" },
  { text: "비 오는 날에는 비를 즐기면 된다.", author: "미상" },
  { text: "한 잔의 따뜻한 차가 마음을 녹인다.", author: "미상" },
  { text: "음악은 영혼의 언어다.", author: "베토벤" },
  { text: "가을엔 책을, 겨울엔 사랑을.", author: "미상" },
  { text: "별이 빛나는 밤, 당신은 혼자가 아닙니다.", author: "미상" },
  { text: "꽃은 자기가 예쁜 줄 모른다. 당신도 그렇다.", author: "미상" },
  { text: "세상에서 가장 아름다운 말은 '괜찮아'다.", author: "미상" },
  { text: "지금 걷고 있는 길이 맞다. 계속 가면 된다.", author: "미상" },
  { text: "오늘의 석양은 내일의 일출을 약속한다.", author: "미상" }
];
// ===== 넌센스 퀴즈 DB (네이버 937문제) =====

var NONSENSE_DB = [
  { q: "백가지 과일이 죽기 직전을 다른 말로?", a: "백과사전", h: "백과XX", r: "백가지 과일 =백과, 죽기전 =사전" },
  { q: "지금 몇 시 몇 분?", a: "짜장면시키신분", h: "XXX시키신분", r: "짜장면시 키신분" },
  { q: "A젖소와 B젖소가 싸움을 했는데 B젖소가 이겼다 이유는?", a: "에이졌소", h: "XX졌소", r: "에이 졌소~ 삐졌소?" },
  { q: "펭귄이 다니는 중학교는?", a: "냉방중", h: "XX중", r: "냉방중(中)학교" },
  { q: "깨뜨리고 칭찬 받는 것은?", a: "신기록", h: "X기록", r: "신기록을 깨면 모두들 칭찬합니다!" },
  { q: "거꾸로 매달린 집에 수십개의 문이 있는 것은?", a: "벌집", h: "X집", r: "거꾸로 매달려 수많은 입구가 있는 벌집" },
  { q: "청바지를 돋보이게 하는 걸음걸이는?", a: "진주목걸이", h: "XX목걸이", r: "진(jean) 주목 걸이" },
  { q: "[창고에 양초가 꽉 차있는 것]을 세 글자로?", a: "초만원", h: "X만원", r: "(양)초가 꽉 차있으니 만원" },
  { q: "못 사온다고 해놓고 사온 것은?", a: "못", h: "X (문제에 답이 있어요!)", r: "못 사온다고 했으니, 못을 가지고왔죠" },
  { q: "무가 자기소개를 할 때 하는 말은?", a: "나무", h: "X무", r: "나는 무야~ 나~무!" },
  { q: "운전하는 사람들이 가장 싫어하는 춤은?", a: "우선멈춤", h: "XXX춤", r: "달려야 하는데 우선 멈춤이라니!!" },
  { q: "싸움을 잘하는 오리는?", a: "을지문덕", h: "XX문덕", r: "을지문덕(DUCK) 장군" },
  { q: "늘 청바지를 원하는 꽃은?", a: "진달래", h: "X달래", r: "매일 매일 진(jean) 달라고 해서 - 진달래" },
  { q: "비 친구가 비한테 사우디아냐고 물어보는 말은?", a: "사우디알아비야", h: "사우디XXXX", r: "사우디 알아? 비야?" },
  { q: "양 중에서 가장 뜨거운 양은?", a: "태양", h: "X양", r: "하늘위에 뜨거운 태양!" },
  { q: "고기 먹을 때 따라오는 개는?", a: "이쑤시개", h: "XXX개", r: "고기 먹고 다들 이쑤시개 사용하시죠?" },
  { q: "좀비는 왜 멍청할까?", a: "머리가좀비어서", h: "머리가XXXX", r: "머리가 좀 비어서" },
  { q: "이 세상에서 가장 더운 바다는?", a: "열바다", h: "X바다", r: "뜨거운 열 바다" },
  { q: "[힘센 말과 고양이]를 네 글자로?", a: "슈퍼마켓", h: "슈퍼XX", r: "슈퍼~ 마(馬) 켓(캣,cat)" },
  { q: "가수 샤이니가 다니는 고등학교는?", a: "아미고", h: "XX고", r: "아미고(高)등학교" },
  { q: "간짜장이 그냥 짜장보다 비싼 이유는?", a: "간 때문이야", h: "X 때문이야", r: "짜장 앞에 간이 붙어서? - 간 때문이야~!" },
  { q: "돈인데, 결혼을 해야 생기는 돈은?", a: "사돈", h: "X돈", r: "결혼을 하면 생기는 사돈" },
  { q: "사람들이 가장 좋아하는 영화는?", a: "부귀영화", h: "XX영화", r: "부귀영화 싫어하시는 분 있으신가요?" },
  { q: "아기돼지 삼 형제에서 한 마리가 더 늘어나면?", a: "모두 죽음", h: "모두XX", r: "아기돼지 사형제" },
  { q: "슬픈 사람들끼리 모여서 노는 것?", a: "ㅠㅠ상종", h: "ㅠㅠXX", r: "슬퍼서 눈물 흘리는 ㅠㅠ" },
  { q: "아마존엔 누가 살까?", a: "존", h: "X", r: "아마…존" },
  { q: "실내화가 자기소개할 때 하는 말은?", a: "익스큐즈미", h: "익스XXX", r: "익스큐즈미~ 실례합니다~" },
  { q: "한국은 원, 일본은 엔, 호주는?", a: "호주머니", h: "호주XX", r: "호주+돈(머니)" },
  { q: "사람 세 명이 탄 차를 세 글자로?", a: "인삼차", h: "X삼차", r: "인(人) 삼(3) 차(車)" },
  { q: "비가 얼어죽으면?", a: "비동사", h: "비XX", r: "비가 동사(얼어죽다)했으니 - 비동사!" },
  { q: "닭이 스키니 바지를 입고 하는 말?", a: "꼬끼오", h: "XX오", r: "꽉끼는 스키니를 입으니~ 꼬끼오(꼭끼어~!)" },
  { q: "모자 매장 진열대 사이에 신발을 두면?", a: "캡사이신", h: "XX이신", r: "모자(cap) 사이에 신" },
  { q: "사과가 웃으면?", a: "풋사과", h: "X사과", r: "웃을 때~ 풋! 하고 웃는 경우가 있죠?" },
  { q: "다정함의 반대는?", a: "선택장애", h: "XX장애", r: "다정한 것에 반대는 하나도 못 정한 선택장애" },
  { q: "북 중에서 가장 큰 북은?", a: "동서남북", h: "XXX북", r: "넓고 넓은 동서남북" },
  { q: "깨인데, 먹지 못하는 깨는?", a: "주근깨", h: "XX깨", r: "주근깨는 먹을 수 없습니다." },
  { q: "독수리가 불에 타면 어떤 소리가 날까?", a: "이글이글", h: "X글X글", r: "이글(eagle) ~ 이글(eagle) 타올라라!" },
  { q: "영국 축구 대표 팀에서 뛰는 일본 선수는?", a: "왜인루니", h: "XX루니", r: "왜인(일본사람) 루니" },
  { q: "공기 1kg와 금 1kg 중, 어느 것이 더 무거울까?", a: "똑같다", h: "X같다", r: "둘 다 1kg라서 무게는 동일합니다." },
  { q: "평생 부동산 투기만 한 사람이 남긴 유언은?", a: "저승사자", h: "XX사자", r: "저승도 사버리자" },
  { q: "망칠 수록 돈을 버는 사람은?", a: "어부", h: "X부", r: "어부는 망(그물)을 쳐서 고기잡고 돈을 벌죠" },
  { q: "군인이 돈을 다 쓰면?", a: "무전병", h: "XX병", r: "무전(돈이 없는) 병사" },
  { q: "오리인데, 물속에서만 사는 오리는?", a: "가오리", h: "X오리", r: "가오리는 물에서만 살죠!" },
  { q: "오백에서 백을 빼면?", a: "오", h: "X (글자를 지워보세요)", r: "오백에서 글자 백을 지우면, 오!" },
  { q: "대학생들의 전투력이 높을 때는?", a: "개강할때", h: "XX할때", r: "개 강할때" },
  { q: "일원동보다 싼 동네 이름은?", a: "삼전동", h: "XX동", r: "삼전하는 동네" },
  { q: "공기만 먹어도 살이 찌는 것은?", a: "풍선", h: "X선", r: "풍선은 공기를 넣으면 빵빵해지죠~" },
  { q: "[병에 걸린 가수 호란]을 네글자로?", a: "병자호란", h: "XX호란", r: "병자가 된 호란 - 병자호란" },
  { q: "팔이 네개인 사람들이 사는 나라는?", a: "네팔", h: "X팔", r: "팔이 네개라서 - 네팔" },
  { q: "꽃가게 주인장이 제일 싫어하는 나라는?", a: "시드니", h: "XX니", r: "꽃이 왜이리 시드니~" },
  { q: "천하장사가 타고 다니는 차는?", a: "으랏차차", h: "으랏XX", r: "힘센 천하장사는 으랏차차(車)" },
  { q: "교회에 가면 주는 돈은?", a: "구원", h: "X원", r: "구원을 해주니 구원" },
  { q: "가슴에 흑심을 품고 있는 것은?", a: "연필", h: "X필", r: "연필은 속에 검은 연필심(흑심)을 가지고 있죠" },
  { q: "이상한 사람들이 모이는 곳은?", a: "치과", h: "X과", r: "이(tooth)가 상하면 치과가서 치료 받아야죠~" },
  { q: "왕과 처음 만날 때 하는 인사?", a: "하이킹", h: "XX킹", r: "만나서 반가우니 하이~킹!" },
  { q: "직접 만든 총은?", a: "손수건", h: "XX건", r: "손수 만든 건" },
  { q: "돌하르방이 서커스단을 보고하는 말은?", a: "제주도좋다", h: "XXX좋다", r: "재주도, 제주도 좋다" },
  { q: "얼음이 죽으면?", a: "다이빙", h: "XX빙", r: "다이(die) 빙(氷)" },
  { q: "[제발 울지마~]를 한 글자로?", a: "뚝", h: "X (X 그쳐!)", r: "뚝! 울지마!" },
  { q: "무엇이든 팔 수 있는 나라는?", a: "팔라우", h: "XX우", r: "이것 저것 다 팔라우~" },
  { q: "결정 장애가 많은 대학은?", a: "고려대학교", h: "XX대학교", r: "고려할게 많아서 결정을 못함" },
  { q: "곰돌이 푸가 차에 치이면?", a: "카푸치노", h: "카푸XX", r: "카(car)가 푸를 쳤네?(쳤노?)" },
  { q: "사자를 넣고 끓인 국은?", a: "동물의 왕국", h: "XXX 왕국", r: "동물의 왕(사자)를 넣고 끓인 국" },
  { q: "도둑이 훔친 돈을 네 글자로?", a: "슬그머니", h: "XX머니", r: "도둑은 항상 슬그머니(money) 돈을 훔치죠" },
  { q: "떡 중에 가장 빨리 먹는 떡은?", a: "헐레벌떡", h: "XX벌떡", r: "급하게 헐레벌떡 먹으니 헐레벌'떡'" },
  { q: "고추장 보다 높은 것은?", a: "초고추장", h: "X고추장", r: "초(강력)을 붙여 초고추장!" },
  { q: "[소 네 마리]를 두 글자로?", a: "소포", h: "X포", r: "소가 네 마리라서 소포?" },
  { q: "우리나라 사람들이 다 같이 쓰는 가위는?", a: "한가위", h: "X가위", r: "대한민국 사람은 모두 한가위를 보냅니다" },
  { q: "딩동댕의 반대말은?", a: "땡", h: "X (틀리면 X!)", r: "딩동댕(성공) - 땡(실패)" },
  { q: "만 마리의 소들이 절을하는 것을 세글자로?", a: "만우절", h: "XX절", r: "만마리 우(牛)가 절을 하다" },
  { q: "세상에서 가장 잘 깨지는 창문은?", a: "와장창", h: "XX창", r: "유리가 와장창~ 잘깨져서 와장'창'" },
  { q: "아재가 좋아하는 악기는?", a: "아쟁", h: "아X", r: "아재는 아쟁을 좋아해" },
  { q: "[골뱅이가 무를 때렸다]를 다섯글자로?", a: "골뱅이무침", h: "골뱅이XX", r: "골뱅이가 무를 침 - 골뱅이무침" },
  { q: "길면 길 수록 좋은 강은?", a: "만수무강", h: "XXX강", r: "오래 오래~ 만수무강하는게 최고입니다." },
  { q: "굴인데 먹을 수 없는 굴은?", a: "동굴", h: "X굴", r: "동굴은 먹을 수 없죠!" },
  { q: "세상에서 가장 더러운 강은?", a: "요강", h: "X강", r: "요강은 깨끗하지 않죠" },
  { q: "3월에 대학생이 강한 이유는?", a: "개강해서", h: "XX해서", r: "개 강해서" },
  { q: "엄마가 길을 잃으면?", a: "맘마미아", h: "맘마XX", r: "맘마(엄마)가 미아(길을 잃다)" },
  { q: "구리는 구리인데 못 쓰는 구리는?", a: "멍텅구리", h: "XX구리", r: "멍텅 구리는 구리가 아니지요" },
  { q: "가수 설운도가 옷 벗는 순서는?", a: "상하의", h: "XX의", r: "상하이~ 상하이~ 트위스트" },
  { q: "모든 사람들이 거짓말만 하는 절은?", a: "만우절", h: "XX절", r: "모두 거짓말만 하는 절 - 만우절?" },
  { q: "사람이 가장 많이 말하는 소리는?", a: "숨소리", h: "X소리", r: "사람은 평생 숨을 쉬면서 삽니다" },
  { q: "남들과 반대로 생활하는 쥐는?", a: "박쥐", h: "X쥐", r: "다른 쥐와 다르게 거꾸로 매달려 생활하는 박쥐" },
  { q: "곰은 사과를 어떻게 먹을까?", a: "베어먹음", h: "XX먹음", r: "곰은 베어(bear)이니, 베어먹음?" },
  { q: "손을 올리면 멈추는 것은?", a: "택시", h: "X시", r: "택시~ 하면서 손을 들면 택시가 멈추죠~" },
  { q: "우리나라에서 김이 가장 많이나는 곳은?", a: "목욕탕", h: "XX탕", r: "뜨거운 수증기로 김이 나는 목욕탕" },
  { q: "오래 살 것 같은 연예인은?", a: "이승깁니다", h: "XX깁니다", r: "이승이 길어서 이승에서 오래삽니다." },
  { q: "[돼지가 방귀를 뀌면]을 세글자로?", a: "돈가스", h: "X가스", r: "돈(돼지 돈, 豚)이 가스를 뀌면 - 돈가스!" },
  { q: "술을 좋아하는 사람들이 모인 나라는?", a: "호주", h: "X주", r: "호(好, 좋아하다) 주(酒, 술)라서 호주?" },
  { q: "어부가 가장 싫어하는 노래는?", a: "바다가 육지라면", h: "바다가 XX라면", r: "바다가 육지로 변하면 어부는 일이 없죠" },
  { q: "[가수 비가 LA에 갈 것이다]를 네 글자로?", a: "LA갈비", h: "LAXX", r: "LA로 갈 비, 그래서 LA갈비" },
  { q: "송해가 샤워를 하면?", a: "뽀송뽀송해", h: "XXXX해", r: "샤워를 하면 피부가 뽀송뽀송(해)" },
  { q: "자동차가 푸를 치면?", a: "카푸치노", h: "XX치노", r: "자동차(car)가 푸를 치노" },
  { q: "눈으로 못 보고, 입으로만 보는 것은?", a: "맛", h: "X (X을 보다!)", r: "음식을 맛보다~ 맛은 입으로 보는거죠" },
  { q: "몽골 사람이 땀을 흘리면?", a: "몽골몽골", h: "몽X몽X", r: "몽골 몽골 땀을 흘림" },
  { q: "들어가는 입구는 하나지만, 나오는 입구는 2개인 것은?", a: "바지", h: "바X", r: "허리통 하나에 다리 통 2개 - 바지!" },
  { q: "사람이 먹을 수 있는 제비는?", a: "수제비", h: "X제비", r: "많은 사람들이 좋아하는 수제비" },
  { q: "[자식이 아홉명이다]를 세 글자로?", a: "아이구", h: "아이X", r: "아이가 구(9)명이라 '아이구'" },
  { q: "우리나라까지 석유가 도착하는데 소요되는 시간은?", a: "오일", h: "X일", r: "OIL 오일" },
  { q: "사람의 몸무게가 가장 많이 나갈 때는?", a: "철들 때", h: "XX 때", r: "철(iron)을 들면 정말 무거워지겠죠?" },
  { q: "곤충의 몸을 삼등분 하면?", a: "죽는다", h: "X는다", r: "몸을 삼등분 하면 죽겠죠?" },
  { q: "과자가 자기소개를 하면?", a: "전과자", h: "전XX", r: "전 과자예요" },
  { q: "톱 중에서 가장 유명한 톱은?", a: "톱스타", h: "X스타", r: "톱 중에 스타(star) 톱스타!" },
  { q: "허수아비의 아들 이름은?", a: "허수", h: "X수", r: "허수의 아버지(아비)가 허수아비니까" },
  { q: "겉과 속이 다른 사람이 좋아하는 음식은?", a: "따로국밥", h: "XX국밥", r: "겉과 속이 따로 나누어진 사람이 좋아해서 따로국밥?" },
  { q: "세상에서 가장 예쁜 식물은?", a: "뷰티풀", h: "XX풀", r: "BEAUTY풀" },
  { q: "발은 발은데, 머리 위에 달린 발은?", a: "가발", h: "X발", r: "가발은 머리위에 쓰는거 아시죠?" },
  { q: "쓰 가운데가 분홍색이면?", a: "스핑크스", h: "XX크스", r: "스 핑크 스" },
  { q: "스님들만 이용하는 병실은?", a: "중환자실", h: "X환자실", r: "중(스님)이 환자일 때 있는 곳 - 중환자실" },
  { q: "바람이 귀엽게 부는 지역은?", a: "분당", h: "X당", r: "바람이 분당~" },
  { q: "로버트 할리가 심은 나무 이름은?", a: "할리우드", h: "할리XX", r: "할리가 심은 나무(wood)" },
  { q: "다리는 있는데 발이 없는 것은?", a: "바지", h: "바X", r: "바지에 다리는 있지만 발은 없습니다" },
  { q: "세상에서 가장 빠른 새는?", a: "눈깜짝할새", h: "눈XXX새", r: "정말 빠른~ 눈깜짝할새!!" },
  { q: "간을 깔고 자면?", a: "간이침대", h: "XX침대", r: "간이 곧 침대" },
  { q: "회의하러 가는 걸 세 글자로 줄이면?", a: "회의감", h: "회의X", r: "회의하러감-회의감" },
  { q: "감기에 다시 걸리면?", a: "되감기", h: "X감기", r: "다시 되 감기" },
  { q: "세상에서 글을 가장 사랑하는 물건은?", a: "글러브", h: "글XX", r: "글을 사랑해서 글러브(love)" },
  { q: "다 자랐는데 계속 자라라고 하는 것은?", a: "자라", h: "X라", r: "자라(snapping turtle)는 다 커도 자라라고 부르죠" },
  { q: "소금을 가장 비싸게 파는 법?", a: "소와 금을 분리해서 판다", h: "X와 X을 분리해서 판다", r: "소금보다는 소와 금이 더 비싸게 팔수 있습니다." },
  { q: "세상에서 가장 비싼 붕어는?", a: "금붕어", h: "X붕어", r: "가장 비싼 금(金) 붕어~" },
  { q: "[눈떠라]라는 말의 세계 공통어는?", a: "아멘", h: "X멘", r: "눈감고 기도 후, 아멘~ 하면 눈을 뜨죠" },
  { q: "항상 미안해하는 연예인?", a: "지성", h: "X성", r: "죄송, 지송, 지성해요" },
  { q: "북유럽 사람들이 책을 많이 읽는 이유는?", a: "bookyoulove", h: "BookyouXXXX", r: "북 유 러브~" },
  { q: "초등학생이 가장 좋아하는 동네는?", a: "방학동", h: "XX동", r: "학생들은 방학을 제일 좋아하죠" },
  { q: "자가용의 반대말은?", a: "커용", h: "X용", r: "[작다]의 반대는 [크다]" },
  { q: "형제 중에 곱슬머리가 있다를 다른 말로하면?", a: "브로컬리", h: "브로XX", r: "브로의 머리는 컬이 살아 있어" },
  { q: "서울이 추운 걸 다섯 글자로 하면?", a: "서울시립대", h: "서울XXX", r: "서울이 시립대" },
  { q: "결혼한 복숭아를 네글자로?", a: "웨딩피치", h: "XX피치", r: "결혼한(웨딩) 복숭아(피치)" },
  { q: "남자는 힘, 여자는?", a: "헐", h: "X", r: "남자(him), 여자(her)" },
  { q: "수프가 환하게 빛나면?", a: "스프라이트", h: "XX라이트", r: "수프 라이트?" },
  { q: "세상에서 가장 빠른 차는?", a: "첫차", h: "X차", r: "가장 빠르게 오는 차는 첫차~!" },
  { q: "깨가 죽으면?", a: "주근깨", h: "XX깨", r: "주근(죽은) 깨 - 주근깨" },
  { q: "신사들이 많이 사는 동네는?", a: "신사동", h: "XX동", r: "신사들이 모여사는 신사동?" },
  { q: "눈물 없이 우는 것은?", a: "새", h: "X (사람은 아니에요~)", r: "새는 항상 울지만 눈물을 흘리지는 않아요~" },
  { q: "식인종이 밥투정 할 때 하는 말은?", a: "살맛안나", h: "살X안나", r: "사람 살을 먹는 식인종이니, 밥투정은 살~맛안나!" },
  { q: "송해가 믿는 종교는?", a: "송혜교", h: "XX교", r: "송해가 믿는 교" },
  { q: "택시기사가 가장 좋아하는 동물은?", a: "타이거", h: "타XX", r: "이 택시 타!, 타~ 이거" },
  { q: "기어다니는 팽이는?", a: "달팽이", h: "X팽이", r: "느리게~ 느리게~ 기어가는 달팽이" },
  { q: "서서 쉬고 누워서 일하는 것은?", a: "가야금", h: "XX금", r: "가야금은 눕혀 연주하고 서서 보관하죠~" },
  { q: "창피한 것도 모르고 체면도 없는 사람의 나이는?", a: "넉살", h: "X살", r: "넉살이 좋아지면 부끄러운게 별로 없죠" },
  { q: "계속 거짓말을 하는 사람은?", a: "또라이", h: "또XX", r: "또 거짓말(LIE)" },
  { q: "[나는 F학점이다]를 세 글자로?", a: "IMF", h: "XXF", r: "I'm(나는) F" },
  { q: "참기름을 먹을 수 없는 이유는?", a: "고소공포증이있어서", h: "XXXXX있어서", r: "고소한 참기름에 고소 공포증이 있어서" },
  { q: "아마추어 반대말은?", a: "확실히추워", h: "XXX추워", r: "불확실한 아마?의 반대는 확실히죠" },
  { q: "북인데 살아있는 북은?", a: "거북", h: "X북", r: "거북이는 살아있는 파충류 입니다." },
  { q: "소고기가 없는 나라는?", a: "소고기무국", h: "XXX무국", r: "소고기가 없는(없을 무,無) 나라(나라 국,國)" },
  { q: "길에서 총을 쏘면?", a: "탕웨이", h: "X웨이", r: "총을 탕! 길에서(way)" },
  { q: "검도를 하다가 죽었다. 왜일까?", a: "죽도록맞아서", h: "XXX맞아서", r: "죽도로 맞아서" },
  { q: "쇼핑중독에 걸린 동물은?", a: "사자", h: "사X", r: "이거 사자~ 저거 사자~!" },
  { q: "잘생긴 부처님을 다른 말로?", a: "부처핸섬", h: "부처XX", r: "부처핸섬(HANDSOME)" },
  { q: "가장 기분좋은 바람은?", a: "신바람", h: "X바람", r: "신바람 나면 기분좋죠!" },
  { q: "곰들이 목욕하는 곳은?", a: "곰탕", h: "X탕", r: "곰들의 목욕탕~ 곰탕" },
  { q: "마시지 않고 씹어먹는 물은?", a: "나물", h: "X물", r: "나물은 꼭꼭 씹어서 먹어야 합니다." },
  { q: "소녀시대가 타는 차는?", a: "제시카", h: "XX카", r: "소녀시대만 탈 수 있는 차 - 제시카!" },
  { q: "눈사람의 반대말은?", a: "일어난 사람", h: "XXX 사람", r: "누운(눈~)사람의 반대는 일어난 사람!" },
  { q: "자인데, 가장 외로운 자는?", a: "혼자", h: "X자", r: "혼자 있으면 가장 외롭죠" },
  { q: "세상에서 가장 작은 섬은?", a: "건포도", h: "XX도", r: "독도보다 훨씬 작은 건포도(島)" },
  { q: "사람이 죽지 않는 산맥은?", a: "안데스산맥", h: "XXX산맥", r: "안 죽는(DEATH) 산맥" },
  { q: "차도가 없는 나라는?", a: "인도", h: "X도", r: "인도(人道)에는 차가 다닐 수 없죠" },
  { q: "파리인데, 물속에서 사는 파리는?", a: "해파리", h: "X파리", r: "물속에서 살아가는 해파리" },
  { q: "늘 술이 넘치는 지하철 역은?", a: "양주역", h: "XX역", r: "늘 양주가 있어서 양주역?" },
  { q: "비가 빅뱅에 안 들어간 이유는?", a: "태양을 피하고 싶어서", h: "XXX피하고 싶어서", r: "비가 부르는 노래, 태양을 피하고 싶어서~" },
  { q: "지구 곳곳에 불교 파 퍼진 이유는?", a: "중력때문", h: "중X때문", r: "불교에 중의 힘(力력) 때문" },
  { q: "창문을 깼는데 피가 안나면?", a: "윈도우xp", h: "윈도우XX", r: "창문(WINDOW) X 피" },
  { q: "[병든자여, 내게로 오라]라고 말한 사람은?", a: "고물장수", h: "XX장수", r: "빈 병은 고물장수에게로~" },
  { q: "음식점 주인이 가장 싫어하는 도시는?", a: "상해", h: "X해", r: "음식이 상하는걸 좋아하는 주인은 없습니다." },
  { q: "꿩먹고 알도 먹는 사람은?", a: "꿩 주인", h: "X 주인", r: "꿩 주인은 꿩도 먹고 알도 먹겠죠?" },
  { q: "예전 사람들이 많이 쓴 종이는?", a: "B4용지", h: "XX용지", r: "before(B4) 용지" },
  { q: "공이 웃으면?", a: "풋볼", h: "X볼", r: "풋! 하고 웃는 볼" },
  { q: "칠판이 웃으면?", a: "킥보드", h: "X보드", r: "킥킥킥 웃는 칠판" },
  { q: "푸우가 넘어지면?", a: "쿵푸", h: "X푸", r: "쿵하고 넘어진 푸" },
  { q: "[감이 싸우다 죽었다]를 세 글자로?", a: "감전사", h: "X전사", r: "감이 싸우다 죽었으니(전사), 감전사" },
  { q: "텅 비어야 배부르고, 꽉 차면 배고픈 것은?", a: "도시락", h: "XX락", r: "도시락은 다 먹고 텅 비어야 배가 부르죠~" },
  { q: "집에서 매일 먹는 약은?", a: "치약", h: "X약", r: "하루 세 번 양치할 때는 치약을 쓰죠!" },
  { q: "매일 시험을 봐도 계속 빵점만 맞는 연예인은?", a: "백지영", h: "XX영", r: "시험지를 백지로 내서 빵점맞는 연예인 - 백지영" },
  { q: "삼전동보다 싼 동네 이름은?", a: "노원동", h: "X원동", r: "NO원동" },
  { q: "가는 말이 고우면?", a: "오는말은 컴", h: "오는말은 X", r: "GO~ COME!" },
  { q: "지방 흡입의 반대말은?", a: "수도권배출", h: "XXX배출", r: "수도권에서 지방으로 배출" },
  { q: "탈모인 사람들이 여는 행사는?", a: "공모전", h: "XX전", r: "0 모전" },
  { q: "세상에서 가장 폭력적인 나라는?", a: "칠레", h: "X레", r: "너도~ 나도~ 칠레? 하면서 싸우는 나라" },
  { q: "슈렉 어머니의 직업은?", a: "녹색어머니", h: "XX어머니", r: "슈렉처럼 녹색인 어머니" },
  { q: "군인들이 가장 싫어하는 창문은?", a: "영창", h: "X창", r: "군인들은 영창에 가는 걸 가장 싫어합니다." },
  { q: "겨울에만 피는 꽃은?", a: "눈꽃", h: "X꽃", r: "눈은 겨울에만 내립니다" },
  { q: "소녀시대 팬들이 가장 좋아하는 지하철 역은?", a: "서현역", h: "XX역", r: "소녀시대 서현을 생각하며 좋아하겠죠?" },
  { q: "잠들기 전에 하는 일은?", a: "눈 감는 일", h: "X 감는 일", r: "눈 뜨고 자는 사람은 거의 없겠죠?" },
  { q: "[한 입 배어먹은 사과]를 네글자로?", a: "파인애플", h: "XX애플", r: "파인자국이 있는 애플(사과)" },
  { q: "항상 청바지를 가지고 있는 연예인은?", a: "소유진", h: "XX진", r: "항상 청바지(jean)를 소유해서 소유진!" },
  { q: "늘 허리를 굽히고 살아가는 것은?", a: "새우", h: "X우", r: "새우의 허리는 늘 굽어 있죠" },
  { q: "귀인데 소리를 듣지 못하는 귀는?", a: "뼈다귀", h: "XX귀", r: "뼈다귀는 소리를 들을 수 없습니다." },
  { q: "세상에서 가장 쉬운 숫자는?", a: "19만", h: "XX만", r: "쉽구만~(십구만)" },
  { q: "세상에서 가장 웃긴 글은?", a: "싱글벙글", h: "X글X글", r: "즐거우면 싱글벙글 웃죠" },
  { q: "돌잔치를 영어로 하면?", a: "락페스티발", h: "X페스티발", r: "락(ROCK) 페스티발" },
  { q: "있을 수도 있고 없을 수도 있는 섬은?", a: "아마도", h: "XX도", r: "있을지 없을지 모르니 아마도(섬 도,島)?" },
  { q: "[창이 날라오니까 피해]를 세글자로?", a: "창피해", h: "XX해", r: "창(을) 피해!" },
  { q: "전화기로 세운 건물은?", a: "콜로세움", h: "콜로XX", r: "전화를 콜(call)해서 세운(세움) - 콜로세움" },
  { q: "누를수록 더 또렷한 것은?", a: "도장", h: "X장", r: "도장은 꾹~ 누를수록 더 또렷하게 찍힙니다." },
  { q: "개근하는 학생이 제일 많은 대학은?", a: "가야대", h: "XX대학교", r: "가기 싫어도 가야대" },
  { q: "장독이 가장 많은 섬은?", a: "독도", h: "X도", r: "장독이 많은 독도" },
  { q: "세상에서 가장 돈이 많은 나무는?", a: "은행나무", h: "XX나무", r: "돈을 많이 보관해서 은행나무 일까요?^^" },
  { q: "자인데, 가장 무서운 자는?", a: "사자", h: "X자", r: "사자와 만나면 정말 무섭겠죠?" },
  { q: "[나는 자전거를 타지 못한다]를 영어로?", a: "모터사이클", h: "XX사이클", r: "나는 자전거 못타(모터~) 그래서 모터사이클" },
  { q: "책 중에서 가장 읽기 힘든 책?", a: "지침서", h: "XX서", r: "지치게 만드는 지침서" },
  { q: "[발이 두 개 달린 소]를 세 글자로?", a: "이발소", h: "XX소", r: "이(2) 발(다리) 소" },
  { q: "[머리 아홉개]를 두 글자로?", a: "구두", h: "구X", r: "머리가 아홉 개 - 구(9)두(頭)" },
  { q: "63빌딩에서 떨어져도 살 수 있는 방법은?", a: "1층에서 떨어진다", h: "X층에서 떨어진다", r: "아무리 높은 빌딩이라도 1층에서 떨어지면 괜찮을꺼에요~" },
  { q: "보내기 싫으면?", a: "가위나바위를낸다", h: "XXXXXX낸다", r: "가위나 바위를 내면 되죠" },
  { q: "차 사고를 다를 말로 하면?", a: "붕어빵", h: "XX빵", r: "붕~~~ 어~~ 빵!" },
  { q: "용 중에서 소리를 낼 수 없는 용은?", a: "조용", h: "X용", r: "모두 조용(龍)!" },
  { q: "시간이 갈 수록 환해지는 것은?", a: "흰머리", h: "X머리", r: "나이가 들 수록 흰머리가 늘어나죠" },
  { q: "세상에서 가장 큰 콩은?", a: "홍콩", h: "X콩", r: "우리가 먹는 콩보다 훨씬 크겠죠?" },
  { q: "새가 불에 타면?", a: "타조", h: "X조", r: "불에 탄 새(새 조,鳥) - 타조" },
  { q: "귀인데, 소리가 나는 귀는?", a: "까마귀", h: "XX귀", r: "까마귀는 까악~ 까악~하며 소리를 냅니다." },
  { q: "세상에서 제일 얇은 바위는?", a: "머리카락", h: "머리XX", r: "머리카락(ROCK)" },
  { q: "[술과 차를 팔지 않는다]를 네 글자로?", a: "주차금지", h: "XX금지", r: "주(술,酒), 차(茶)는 금지!!" },
  { q: "고양이가 지옥에 가면?", a: "헬로키티", h: "헬로XX", r: "hell(지옥)로 키티(고양이)" },
  { q: "온 나라에 공이 넘치는 나라는?", a: "남아공", h: "XX공", r: "공이 남아 돌아서 남아공?" },
  { q: "63빌딩에서 돈을 떨어뜨리면?", a: "아깝다", h: "아X다", r: "아까운 내돈이 떨어지다니~" },
  { q: "차도녀의 반대말은?", a: "인도녀", h: "X도녀", r: "차도의 반대말은 인도" },
  { q: "푸가 염소를 무서워하는 이유는?", a: "아기염소여럿이푸를뜯고놀아서", h: "아기염소 여럿이 XXXXXXX", r: "아기 염소 여럿이 풀을 뜯고 놀아요~" },
  { q: "세상에서 가장 예쁜 돌은?", a: "샤론스톤", h: "XX스톤", r: "아름다움의 아이콘 샤론스톤(stone)" },
  { q: "야구 선수가 왕한테 공을 던지면서 하는 말은?", a: "송구하옵니다", h: "XX하옵니다", r: "송구~ 하옵니다." },
  { q: "콩 한 알을 영어로 하면?", a: "원빈", h: "X빈", r: "원(ONE)빈(BEAN)" },
  { q: "산불을 보고 손자가 할아버지에게 하는 말?", a: "산타할아버지", h: "XX할아버지", r: "산이 타~ 할아버지, 산타할아버지" },
  { q: "검게 태어나서 빨갛게 살다가 하얗게 죽어가는 것은?", a: "연탄", h: "X탄", r: "검은 연탄은 빨갛게 타서 하얀 재가 됩니다." },
  { q: "땅에 심어도 새싹이 나지 않는 씨는?", a: "비타민C", h: "비타민X", r: "비타민C를 땅에 심어도 새싹은 나지 않습니다." },
  { q: "테이가 죽는 이유는?", a: "가슴아파서목이메여서", h: "가슴XXX목이XXX", r: "가슴 아파서~ 목이 메여서~" },
  { q: "짱구와 오징어의 차이는?", a: "짱구는못말려", h: "짱구는XXX", r: "오징어는 말려도 짱구는 못말려~" },
  { q: "인도는 지금 몇 시일까?", a: "네시", h: "X시", r: "인도 네시아" },
  { q: "차가 울면?", a: "잉카", h: "X카", r: "잉잉잉 우는 카(car)" },
  { q: "할머니가 좋아하는 과일은?", a: "할배", h: "할X", r: "할머니가 좋아하는 배, 할배" },
  { q: "손재주가 뛰어난 왕은?", a: "메이킹", h: "XX킹", r: "메이킹(MAKING)" },
  { q: "개그맨들이 잘 걸리는 병은?", a: "요절복통", h: "XXX통", r: "너무 웃어서 생기는 요절복통" },
  { q: "알은 알인데, 날아가는 알은?", a: "총알", h: "X알", r: "빵~ 하고 총을 쏘면 총알이 날아갑니다!" },
  { q: "세탁소 주인이 가장 좋아하는 차는?", a: "구기자차", h: "XXX차", r: "옷이 구겨지면 세탁소에 맡기겠죠" },
  { q: "새우가 등장하는 드라마는?", a: "대하드라마", h: "XX드라마", r: "대하(새우의 한 종류)가 나와서 대하드라마?" },
  { q: "별 중에서 가장 슬픈 별은?", a: "이별", h: "X별", r: "이별만큼 슬픈 것이 또 있을까요?" },
  { q: "해변에서 사람들이 하는 욕은?", a: "해수욕", h: "XX욕", r: "해변에서는 해수욕을 할 수 있습니다." },
  { q: "가면 갈수록 멀어지는 곳은?", a: "출발지", h: "XX지", r: "가면 갈수록 출발지와는 멀어지네요~" },
  { q: "대학생 커플만 찍는 카메라는?", a: "cctv", h: "CCXX", r: "캠퍼스커플(CC) TV" },
  { q: "송해가 상을 받으면?", a: "황송해", h: "X송해", r: "상을 받으면 황송하지요" },
  { q: "구명보트에는 몇 명이 탈 수 있을까?", a: "9명", h: "X명", r: "구(9)명 보트니 9명이겠죠?" },
  { q: "남 등쳐먹고 사는 사람은?", a: "안마사", h: "안XX", r: "안마사는 사람들 등을 맛사지하고 돈을 받죠" },
  { q: "머리를 감을 때, 먼저 감아야 하는 것은?", a: "눈", h: "X (따가우니까?)", r: "먼저 눈을 감아야, 머리를 감을 수 있어요~" },
  { q: "논리적인 사람이 총을 쏘면 나는 소리는?", a: "타당타당", h: "XX타당", r: "논리가 타당힌 사람이 쏘는 총은 타당~" },
  { q: "도둑이 싫어하는 과자는?", a: "누네띠네", h: "누네XX", r: "도둑이 눈에 띄면" },
  { q: "길 가다가 나무를 주우면?", a: "우드득", h: "우드X", r: "나무(WOOD)득, 우드득" },
  { q: "모두가 자기 산이라고 주장하는 산은?", a: "마이산", h: "XX산", r: "모두가 내 것이라고 주장하는 산 - 마이산" },
  { q: "탤런트 최지우가 키우는 개는?", a: "지우개", h: "지우X", r: "최지우가 키우니 지우~개!" },
  { q: "마른 옷은 벗고 젖은 옷은 입는 것은?", a: "빨랫줄", h: "XX줄", r: "빨랫줄에 걸린 옷은 젖은 옷, 마르면 벗겨가죠" },
  { q: "목수도 고칠 수 없는 집은?", a: "고집", h: "X집", r: "최고의 목수도 고집을 꺾을 순 없죠" },
  { q: "모자가 놀라면?", a: "모자이크", h: "모자XX", r: "모자가 이크~ 하고 놀람" },
  { q: "소금, 간장, 참기름이 김밥을 죽였는데 참기름만 경찰서에 간 이유는?", a: "소금과간장이짜서", h: "XXXXX이짜서", r: "소금과 간장이 짜서 참기름에게 뒤집어 씌움" },
  { q: "아기공룡 둘리가 좋아하는 전은?", a: "일억년전", h: "XXX전", r: "김치전 보다 좋아하는 일억년전" },
  { q: "개미의 목구멍보다 작은 것은?", a: "개미의 먹이", h: "개미의 XX", r: "먹이는 목구멍을 통해 들어가죠~" },
  { q: "물고기들이 사는 방은?", a: "PC방", h: "XX방", r: "피쉬(fish) 들이 모여 있어 피쉬방 - PC방" },
  { q: "[개가 사람을 가르친다]를 네 글자로?", a: "개인지도", h: "XX지도", r: "개가 인(人)을 지도하다" },
  { q: "세균 중 가장 센 세균은?", a: "대장균", h: "XX균", r: "균들의 대장! 대장균" },
  { q: "영화감독들이 초초하게 모여있는 지하철 역은?", a: "개봉역", h: "XX역", r: "영화감독들은 초초하게 개봉을 기다립니다." },
  { q: "몸에 좋지 않은 청바지는?", a: "유해진", h: "XX진", r: "몸에 유해한 진(JEAN)" },
  { q: "전주비빔밥 보다 늦은 비빔밥은?", a: "이번주 비빔밥", h: "XX주 비빔밥", r: "전주보다 이번주가 시간이 늦죠" },
  { q: "[움직이는 총]을 세 글자로?", a: "이동건", h: "이동X", r: "이동하는 건(gun) - 이동건" },
  { q: "멈출 때 떨어지는 것은?", a: "속도", h: "X도", r: "멈추면 속도가 떨어집니다" },
  { q: "먹기 전에는 1개인데, 먹을 때는 2개가 되는 것은?", a: "나무젓가락", h: "나무XXX", r: "밥먹을 때, 나무 젓가락을 두 개로 갈라서 먹죠!" },
  { q: "아무리 마셔도 배부르지 않은 것은?", a: "공기", h: "X기", r: "공기는 아무리 마셔도 배부르지 않습니다." },
  { q: "오락실을 지키는 두마리 용은?", a: "일인용과 이인용", h: "XX용과 XX용", r: "오락실 기기는 일인용(龍)과 이인용(龍)이 있죠~" },
  { q: "물건을 사고도 받는 돈은?", a: "거스름돈", h: "XXX돈", r: "물건 살 때, 꼭 거스름돈은 챙겨야죠~" },
  { q: "우리가 손에 가지고 있는 금은?", a: "손금", h: "X금", r: "우린 모두 손바닥에 손금이 있습니다~!" },
  { q: "사람들이 즐겨 먹는 피는?", a: "커피", h: "X피", r: "매일 한잔 씩 마시는 커피~" },
  { q: "집에 갔는데 엄마가 없었다를 세 글자로 하면?", a: "마마무", h: "마XX", r: "마마무(無)" },
  { q: "살아있는 사람이 돈을 내고 들어가는 관은?", a: "여관", h: "X관", r: "잠을 자러 돈을 내고 여관으로 가죠!" },
  { q: "누워도 보이지 않는 천장은?", a: "입천장", h: "X천장", r: "입천장은 누워서도 볼 수 없죠?" },
  { q: "해와 달 중, 해만 취재하는 사람은?", a: "해리포터", h: "X리포터", r: "해만 취재하는 리포터 - 해리포터" },
  { q: "세상에서 가장 큰 컵은?", a: "월드컵", h: "XX컵", r: "가장 큰 축제 중 하나~ 월드컵" },
  { q: "스마트폰 사용자가 좋아하는 파이는?", a: "와이파이", h: "XX파이", r: "스마트폰 사용자는 빠른 와이파이를 좋아해~" },
  { q: "[매우 바쁜 콩]을 세글자로?", a: "콩비지", h: "X비지", r: "아주 바쁜(busy)한 콩~ 콩비지" },
  { q: "생각을 많이 하는 왕은?", a: "띵킹", h: "X킹", r: "THINKING" },
  { q: "누룽지를 영어로 하면?", a: "바비브라운", h: "XX브라운", r: "밥이 브라운색, 바비브라운" },
  { q: "세상 모든 사람이 가기 싫어하는 길은?", a: "저승길", h: "XX길", r: "죽음의 저승길을 걷고 싶은 사람 있을까요?" },
  { q: "집합을 영어로 하면?", a: "홈플러스", h: "X플러스", r: "집(홈)+합(플러스)" },
  { q: "비가 자기소개를 하면?", a: "나비야", h: "나XX", r: "나 비야" },
  { q: "세상에서 가장 외로운 비는?", a: "홀아비", h: "XX비", r: "홀아비는 혼자라서 너무 외롭겠죠?" },
  { q: "연예인 중 치아가 가장 잘보이는 사람은?", a: "이보영", h: "XX영", r: "이가 잘보여~ 이보영" },
  { q: "[화장실에 다녀온 원숭이]를 다섯글자로?", a: "일본원숭이", h: "XX원숭이", r: "화장실에서 일보고 온 - 일본원숭이" },
  { q: "어린 물고기를 두 글자로?", a: "영어", h: "X어", r: "영(young) 어(魚)" },
  { q: "종교를 가진 사람들이 가장 싫어하는 비는?", a: "사이비", h: "XX비", r: "종교인들은 사이비를 가장 싫어하죠!" },
  { q: "귀가 불에 타면?", a: "타이어", h: "X이어", r: "불에 탄 귀(ear) - 타이어" },
  { q: "왕이 직접 주차를 하면?", a: "파킹", h: "파X", r: "주차(parking)? 파킹(king)!" },
  { q: "촌 사람들이 사는 나라는?", a: "양상국", h: "XX국", r: "촌사람이라서 양상국(國)" },
  { q: "[나는 5살이 아니야]를 다섯 글자로?", a: "오세아니아", h: "XX아니아", r: "난 오(5)세 아니야(아!)" },
  { q: "설날에 용돈 하나도 못 받으면?", a: "설거지", h: "X거지", r: "설날에 거지" },
  { q: "세종대왕님이 다녔던 고등학교는?", a: "가갸거겨고교", h: "가갸XXX교", r: "가갸거겨고교(고등학교)" },
  { q: "언제나 말 다툼이 있는 곳은?", a: "경마장", h: "XX장", r: "경마장에서는 늘 말들의 경주(다툼)가 있죠!" },
  { q: "불이 2개 있으면?", a: "이불", h: "X불", r: "2불" },
  { q: "잠잘 때, 늘 옆에 있는 개의 이름은?", a: "베개", h: "X개", r: "잘 때 꼭 필요한 베개" },
  { q: "있어도 쓰지 못하는 돈은?", a: "사돈", h: "X돈", r: "쓸 수 없는 돈이 바로 사돈입니다." },
  { q: "누구나 노력하면 얻을 수 있는 금은?", a: "저금", h: "X금", r: "돈을 모으는 저금은 누구나 노력하면 할 수 있어요!" },
  { q: "우리가 매일 먹는 금은?", a: "소금", h: "X금", r: "모든 음식에 들어가 있는 소금" },
  { q: "[열 받은 강아지]를 세글자로?", a: "핫도그", h: "X도그", r: "핫(hot) 도그(dog)" },
  { q: "배우 김태희가 차를 타고다가 동생 이완을 만나서 하는 말?", a: "타이완", h: "타XX", r: "이 차, 타~ 이완아 - 타이완" },
  { q: "가수 미쓰에이가 먹는 쌀은?", a: "유돈노미", h: "XXX미", r: "유돈노미(米)~ 유돈노미(米)" },
  { q: "사람 몸에 있는 꼬리는?", a: "눈꼬리", h: "X꼬리", r: "우리는 눈꼬리를 가지고 있습니다." },
  { q: "법을 어긴 사람이 받는 금은?", a: "벌금", h: "X금", r: "위법 행위는 벌금을 받을 수 있습니다!" },
  { q: "세상에서 가장 싸움을 잘하는 축구선수는?", a: "펠레", h: "X레", r: "누구든지 때릴 것 같은 이름 펠레!" },
  { q: "게임 뮤직을 다른 말로?", a: "오락가락", h: "오락XX", r: "오락 가락" },
  { q: "걸어다니는 귀는?", a: "당나귀", h: "XX귀", r: "당나귀는 걸어다닐 수 있습니다." },
  { q: "용이 상천하는 걸 4자로 줄이면?", a: "올라가용", h: "XXX용", r: "하늘로 올라가용~" },
  { q: "세상에서 가장 정신없는 대학교는?", a: "부산대", h: "XX대", r: "여기는 왜 이렇게 부산하니?" },
  { q: "[털이 길게 자란 남자]를 네 글자로?", a: "모자란놈", h: "모XX놈", r: "모(毛)가 자란 사람 - 모자란놈" },
  { q: "옛날 왕 중 산수를 가장 잘했던 왕은?", a: "연산군", h: "XX군", r: "산수는 수를 연산하는 것, 연산군이 가장 잘하겠네요" },
  { q: "한국에서 땅 값이 가장 싼 동네는?", a: "일원동", h: "XX동", r: "집값이 일원이라 일원동?" },
  { q: "밥인데 먹을 수 없는 밥은?", a: "톱밥", h: "X밥", r: "나무에서 나오는 톱밥은 먹을 수 없습니다." },
  { q: "숫자 5가 가장 싫어하는 집은?", a: "오페라 하우스", h: "오페라 XXX", r: "5(오)를 패는 집이라 오페라(패라~) 하우스?" },
  { q: "겨울에만 살아나는 사람은?", a: "눈사람", h: "X사람", r: "여름은 눈이 없어 눈사람을 만들 수 없습니다." },
  { q: "식인종이 우사인 볼트를 보면?", a: "패스트푸드", h: "XXX푸드", r: "식인종에게 육상선수 우사인볼트는 빠른(fast) 음식(food)" },
  { q: "숫자 8을 가로로 나누면?", a: "숫자 0", h: "숫자 X", r: "8을 가로로 나누면 0이 2개 나오죠" },
  { q: "고양이가 먹을 수 없는 쥐는?", a: "콩쥐와 팥쥐", h: "X쥐와 X쥐", r: "생쥐는 먹어도 콩쥐/팥쥐는 못먹죠" },
  { q: "왼쪽으로 절하면?", a: "좌절", h: "X절", r: "왼쪽(왼 좌,左)으로 절을 하다 - 좌절" },
  { q: "바나나가 웃으면?", a: "바나나킥", h: "바나나X", r: "바나나가 킥킥 웃음" },
  { q: "[뼈로 만든 방]을 두 글자로?", a: "골룸", h: "X룸", r: "골(骨) 룸(room)" },
  { q: "화가 날 때 쓰는 알파벳은?", a: "A", h: "X (에이~)", r: "화날 때 하는 말 '에이(A)~!!'" },
  { q: "아이스크림이 죽은 이유는?", a: "차가와서", h: "XX와서", r: "자동차가 와서, 아이스크림이 차가와서" },
  { q: "정전을 세 글자로 늘이면?", a: "무전기", h: "XX기", r: "무(無)전기" },
  { q: "고릴라의 콧구멍이 큰 이유는?", a: "손가락이 굵어서", h: "XXX이 굵어서", r: "굵은 손으로 코를 파려면 콧구멍이 커야죠" },
  { q: "개미네 집 주소는?", a: "허리도 가늘군 만지면 부러지리", h: "XX도 가늘군 만지면 XX지리", r: "주소의 도/군/면/리를 이용했네요" },
  { q: "[왕이 넘어졌다]를 두 글자로?", a: "킹콩", h: "킹X", r: "킹(king)이 콩~ 하고 넘어져서 킹콩" },
  { q: "아빠는 차가 5대, 아들은 차가 2대인 것을 네 글자로?", a: "세대차이", h: "XX차이", r: "다섯(5) 대 - 두(2) 대 = 세대차이나네요~" },
  { q: "스님이 숫자를 6까지 세었다를 4자로 하면?", a: "식스센스", h: "식스XX", r: "식스 센 스~님" },
  { q: "펭귄들이 타고 다니는 차는?", a: "알래스카", h: "XXX카", r: "알래스카(car)~!!" },
  { q: "길가다 음식을 주우면?", a: "푸드득", h: "XX득", r: "길가다 음식(=food)를 주운(=얻을득得) 것이니까요~" },
  { q: "사람들이 가장 먹기 싫어하는 탕은?", a: "골탕", h: "X탕", r: "골탕 먹어서 좋은 사람 있을까요?" },
  { q: "열 받을수록 키가 작아지는 것은?", a: "양초", h: "X초", r: "양초는 열을 받으면 녹아내리죠!" },
  { q: "차를 발로 차면?", a: "카놀라유", h: "XX라유", r: "자동차(car) 놀라유~" },
  { q: "아이스크림이 죽으면?", a: "다이하드", h: "XX하드", r: "다이(die) 하드(아이스크림)" },
  { q: "떼돈을 버는 곳은?", a: "목욕탕", h: "XX탕", r: "목욕탕은 때를 밀게해서 돈을 벌죠" },
  { q: "이병헌이 나온 대학은?", a: "단언컨대", h: "XXX대", r: "이병헌이 자주 얘기하죠. 단언컨대" },
  { q: "공원이 늙으면?", a: "팍삭", h: "X삭", r: "공원(park)이 팍삭 늙어요" },
  { q: "이 수가 키우는 개는?", a: "이수씨개", h: "XX씨개", r: "이수씨가 키우는 이수씨개" },
  { q: "빵이 시골로 간 이유는?", a: "소보로", h: "X보로", r: "소보러(소보로) 시골로 간 빵..그래서 소보로 빵?" },
  { q: "놀부가 가장 좋아하는 술은?", a: "심술", h: "X술", r: "놀부의 주특기는 심술부리기~" },
  { q: "돌이 죽으면?", a: "고인돌", h: "XX돌", r: "고인이 되어버린 돌 - 고인돌" },
  { q: "자동차를 깜짝 놀라게 하면?", a: "카놀라유", h: "XX라유", r: "자동차(car) 놀라유~" },
  { q: "친구따라 가는 역은?", a: "강남역", h: "XX역", r: "친구따라 강남간다~ 강남역!" },
  { q: "로또 복권의 당첨 확률을 두 배로 높이는 법은?", a: "복권을 두장산다", h: "복권을 XX산다", r: "두 장 사면 확률이 두 배가 되겠죠?" },
  { q: "고추가 웃으면?", a: "풋고추", h: "X고추", r: "웃을 때~ 풋! 하고 웃는 경우가 있죠?" },
  { q: "모든 일을 대충하는 사람들이 모여있는 땅은?", a: "얼렁뚱땅", h: "X렁X땅", r: "뭐든지 대충~ 얼렁뚱'땅'" },
  { q: "세상에서 가장 센 오리는?", a: "회오리", h: "X오리", r: "강력한 회오리 보다 센 오리가 있을까요?" },
  { q: "병아리가 먹는 약은?", a: "삐약", h: "X약", r: "병아리는 삐약~삐약(藥)" },
  { q: "항상 잘못된 생각을 하는 물건은?", a: "카메라", h: "XX라", r: "카메라가 찰칵찰칵~ 착각하니까" },
  { q: "세계에서 몸집이 가장 큰 여자는?", a: "태평양", h: "XX양", r: "김양~ 이양~ 보다 태평양이 훨씬 크죠" },
  { q: "진정한 프로의 자세는?", a: "프로포즈", h: "XX포즈", r: "프로의 자세(pose)" },
  { q: "갓은 갓인데 못쓰는 갓은?", a: "쑥갓", h: "X갓", r: "쑥갓은 채소라 머리에 쓸 수 없어요~" },
  { q: "아이폰을 킬 수 있는 동물은?", a: "드래곤", h: "드XX", r: "드래곤(drag-on) 밀어서 잠금해제" },
  { q: "겉으로는 눈물 흘리며 속 타는 것은?", a: "촛불", h: "X불", r: "안에서는 불이타고 겉에서는 촛농이 떨어지는 촛불" },
  { q: "[아이 추워]의 반대 말은?", a: "어른 더워", h: "XX 더워", r: "아이-> 어른, 추워-> 더워" },
  { q: "옷에 걸고 다니는 빵은 ?", a: "멜빵", h: "X빵", r: "멜빵은 옷에 걸어 착용합니다" },
  { q: "하늘에 별이 없으면 어떻게 될까?", a: "별볼일 없다", h: "X볼일 없다", r: "별이 없다면 별 볼일이 없죠" },
  { q: "거북이가 길을 가다가 구토를 했어 왜 했을까?", a: "속이거북해서", h: "속이XXXX", r: "거북이 속이 거북해서" },
  { q: "곤충이 부딪히면?", a: "충돌", h: "X돌", r: "곤충끼리 충돌" },
  { q: "정삼각형의 동생 이름은?", a: "정삼각", h: "XX각", r: "저사람이 정삼각의 형이래, 정삼각(의)형" },
  { q: "사람들이 가장 싫어하는 금은?", a: "세금", h: "X금", r: "세금을 더 내고 싶은 사람 있을까요?" },
  { q: "눈은 눈인데 보지 못하는 눈은?", a: "티눈", h: "X눈", r: "티눈은 눈이지만 보지 못하지요" },
  { q: "자동차가 3대 있으면?", a: "쓰리랑카", h: "XX랑카", r: "쓰리(3)랑카" },
  { q: "아이비가 타고 다니는 차는?", a: "유혹의소나타", h: "XXX소나타", r: "아이비가 부른 유혹의 소나타" },
  { q: "배우 원빈이 타고 다니는 말은?", a: "웃기지마", h: "XXX마", r: "웃기지마(馬)~! 얼마면 돼! - 드라마 가을동화" },
  { q: "[죽이다]의 반대말은?", a: "밥이다", h: "X이다", r: "죽의 반대는 밥! 그래서 밥이다!" },
  { q: "머리가 아프면 약을 얼마나 먹어야 할까?", a: "두통", h: "X통", r: "머리가 아플땐 두통" },
  { q: "중학생과 고등학생이 타고 다니는 차는?", a: "중고차", h: "XX차", r: "중/고등 학생이 타는 차, 중고차" },
  { q: "못에서 피가 나는 이유는?", a: "모세혈관이있어서", h: "XXXX이있어서", r: "못에 혈관이 있어서" },
  { q: "자기만 옳다고 하는 사람이 사는 집?", a: "고집", h: "X집", r: "자기만 옳다고 '고집'해서" },
  { q: "[당신은 시골에 삽니다]를 세 글자로 줄이면?", a: "유인촌", h: "XX촌", r: "유(you) 인(in) 촌(村)" },
  { q: "화상 통화를 안 하는 이유는?", a: "뜨거워서", h: "XX워서", r: "화상 입을까 뜨거워서" },
  { q: "광선검은 무슨 색깔일까?", a: "주황색", h: "XX색", r: "광선검을 뽑을 때 나는 소리, 주~황~!!" },
  { q: "친구를 앞으로 만나기 싫으면?", a: "뒤로 만난다", h: "XX 만난다", r: "앞으로 만나기 싫으면 뒤로~" },
  { q: "일년 내내 피는 꽃은?", a: "웃음꽃", h: "XX꽃", r: "웃음은 일년 내내 계속되죠" },
  { q: "아우디보다 먼저 나온 차?", a: "형디", h: "X디", r: "아우(동생)보다 먼저 세상에 나온 사람은 형" },
  { q: "신라면 다음에 나올 제품은?", a: "통일신라면", h: "XX신라면", r: "신라 다음은 통일신라" },
  { q: "공부해서 남주는 사람은?", a: "선생님", h: "XX님", r: "선생님 지식은 학생들에게 나눠주죠" },
  { q: "소년들만 마시는 차는?", a: "보이차", h: "XX차", r: "소년(보이)들이 먹는 차~" },
  { q: "사슴이 눈이 좋으면?", a: "굿아이디어", h: "굿XXXX", r: "GOOD EYE DEAR" },
  { q: "호랑이가 차를 타고 가던 중, 친구를 만나서 하는 말?", a: "타이거", h: "XX거", r: "어서 타~! 이거!!" },
  { q: "못 팔아도 돈 버는 사람은?", a: "철물점 주인", h: "XX점 주인", r: "철물점은 못을 팔아서 돈을 벌죠" },
  { q: "노인이 버스에 타도 절대 자리양보를 안하는 사람은?", a: "운전기사", h: "XX기사", r: "운전을 위해 자리양보는 할 수 없습니다." },
  { q: "큰 입으로 먹고, 작은 입으로 내보내는 것은?", a: "주전자", h: "XX자", r: "주전자는 위에 큰 구멍으로 넣고, 앞에 작은 구멍으로 내보냅니다." },
  { q: "선박을 평가하는 사람은?", a: "베타테스터", h: "베타XXX", r: "배타는 것을 테스트 하는 사람" },
  { q: "세상에서 가장 무서운 비는?", a: "낭비", h: "X비", r: "낭비가 심하면 생활이 힘들어요" },
  { q: "쉽게 속는 사람들이 많이 모이는 서버는?", a: "속눈썹", h: "XX썹", r: "잘 속는 속눈 썹" },
  { q: "도둑이 도둑질 하러 가는 걸음걸이는?", a: "털레털레", h: "X레X레", r: "물건을 털러가는 도둑의 발걸음은 털레털레?" },
  { q: "[방금까지 울다가 울음을 그친 사람]을 다섯 글자로?", a: "아까운 사람", h: "XXX 사람", r: "아까 운(눈물 흘린) 사람" },
  { q: "가다의 반대말은?", a: "노가다", h: "X가다", r: "NO가다" },
  { q: "피카츄가 담배를 주우면서 하는 말?", a: "피까", h: "피X", r: "필까? 피까?" },
  { q: "[아빠 두 분에 엄마 한분]을 네글자로?", a: "두부한모", h: "X부X모", r: "두분의 부(父)와 한분의 모(母) - 두부한모" },
  { q: "학교에서 숙제를 안해도 혼나지 않는 사람은?", a: "선생님", h: "XX님", r: "선생님은 숙제를 할 필요 없죠~" },
  { q: "엄마들이 아침마다 말하는 나라는?", a: "일어나라", h: "XX나라", r: "매일 아침 어머니들이 하는 말 - 일어나라!!" },
  { q: "불이 났는데 빨리 뛰지 않는 이유는?", a: "화재경보", h: "화재XX", r: "경보로 걸으면 뛸 수가 없죠" },
  { q: "신하가 왕과 헤어질 때 하는 말은?", a: "바이킹", h: "바이X", r: "바이(bye)~ 킹(king)" },
  { q: "앉아서 절하면?", a: "좌절", h: "X절", r: "앉아서(坐 좌) 절" },
  { q: "소금의 유통기간은 언제까지?", a: "천일염", h: "X일염", r: "천일이염" },
  { q: "색색 옷을 입어도 늘 검게 보이는 것은?", a: "그림자", h: "XX자", r: "튀는 색의 옷을 입어도 그림자는 늘 검은색" },
  { q: "추운 날에는 보이지만 더운 날에는 보이지 않는 것은?", a: "입김", h: "X김", r: "추우면 생겨나는 입김~ 더우면 볼 수 없어요!" },
  { q: "북한 사람이 친구들과 축구를 하다 공을 잃어버렸을 때 한 말은?", a: "공산당", h: "공XX", r: "공 새로 산당, 공산당" },
  { q: "가슴의 무게는?", a: "네근", h: "X근", r: "가슴은 두근~ 두근~ 그래서 네근" },
  { q: "대학생이 토르와 같은 이유는?", a: "벼락치고망치기떄문", h: "벼락치고XXXXX", r: "시험때 벼락치기 공부하고 망치기 때문" },
  { q: "화장실에 사는 두 마리 용은?", a: "신사용과 숙녀용", h: "XX용과 XX용", r: "신사용(龍)과 숙녀용(龍)~ 두 마리의 용이 사는 화장실?" },
  { q: "더하든 곱하든 답이 6이 나오는 숫자 3개는?", a: "1과 2와 3", h: "X과 X와 X", r: "1+2+3=6, 1*2*3=6" },
  { q: "남들이 깨끗해질수록 본인은 더러워 지는 것은?", a: "걸레", h: "X레", r: "걸레로 닦으면 깨끗하지만, 걸레는 더러워집니다~" },
  { q: "남자들이 좋아하는 우유는?", a: "아이유", h: "XX유", r: "국민 여동생 '아이유!!'" },
  { q: "개 중에서 가장 빠른 개는?", a: "번개", h: "X개", r: "순식간에 나타나~ 사라지는 번개" },
  { q: "이란 사람이 자기소개할 때 부르는 노래는?", a: "나이런사람이야", h: "나XXXX이야", r: "나 이란사람이야~" },
  { q: "이 세상 모든 사람들의 머리카락 수를 곱하면?", a: "숫자 0", h: "숫자 X", r: "대머리 등 머리카락이 0개인 사람도 있으니, 곱하면 0~" },
  { q: "설득을 가장 잘하는 사람은?", a: "사냥꾼", h: "사XX", r: "타당타당~ 총을 쏘는 타당한 사람이니까" },
  { q: "대학생들이 가장 싫어하는 강은?", a: "재수강", h: "XX강", r: "재수강은 대학생을 힘들게 하죠~" },
  { q: "차가운 물이 나오지 않는 지하철 역은?", a: "온수역", h: "XX역", r: "온수(뜨거운 물)만 나오니 온수역?" },
  { q: "눈코 뜰 새 없이 바쁜 사람은?", a: "머리감는사람", h: "XXXX사람", r: "머리 감는 동안 눈코 뜰 수가 없죠" },
  { q: "서울로 가는 소는?", a: "소설가", h: "XX가", r: "소 서울로 가" },
  { q: "사람들이 가장 찾아뵙기 싫은 선생님은?", a: "의사선생님", h: "XX선생님", r: "몸이 아플 때 찾아가니, 다들 싫어하겠죠?" },
  { q: "돌잔치를 영어로?", a: "RockFestival", h: "RockXXXXXXXX", r: "돌은 영어로? rock, 잔치를 영어로? festival!" },
  { q: "검은색으로 칠해야 더 깨끗해지는 것은?", a: "검은 구두", h: "XX 구두", r: "검은 구두는 검은색으로 칠해야 깨끗합니다" },
  { q: "춤은 춤인데, 움직일 수 없는 춤은?", a: "멈춤", h: "X춤", r: "움직이지마! 멈춤!" },
  { q: "알파벳은 총 몇글자 일까?", a: "세글자", h: "X글자", r: "알.파.벳 - 세글자 맞죠?" },
  { q: "산타 할아버지가 제일 증오하는 자동차 이름은?", a: "산타페", h: "산XX", r: "산타를 때려? 산타페?" },
  { q: "모든 사람이 어쩔 수 없이 먹는 것은?", a: "나이", h: "나X", r: "우리 모두 한살 한살 나이를 먹습니다" },
  { q: "굶는 사람이 많은 나라는?", a: "헝가리", h: "X가리", r: "헝그리? 헝가리!" },
  { q: "가장 아름다운 접속사는?", a: "BUT", h: "BXX", r: "비유티, 뷰티~" },
  { q: "발인데 가장 향기로운 발은?", a: "꽃다발", h: "XX발", r: "다양한 꽃향기가 가득한 꽃다발" },
  { q: "밤에는 도저히 할 수 없는 것은?", a: "낮잠", h: "X잠", r: "밤에 낮잠을 잘 수 있을까요?" },
  { q: "세상에서 가장 추운 바다는?", a: "썰렁해", h: "XX해", r: "썰렁해(海)~ 썰렁하면 추워집니다" },
  { q: "통닭 10마리 더 주세요를 네 글자로 하면?", a: "치킨텐더", h: "XX텐더", r: "치킨 10(ten) 더" },
  { q: "세상에서 가장 가난한 왕은?", a: "최저임금", h: "XX임금", r: "최저임금을 받는 임금" },
  { q: "매일 잠만 자는 소는?", a: "주무소", h: "XX소", r: "빨리 주무소(소 우,牛)" },
  { q: "드라큘라가 가장 싫어하는 사람?", a: "찔러도 피 안나는 사람", h: "찔러도 X 안나는 사람", r: "피가 안나오니 드라큘라가 싫어하겠죠" },
  { q: "아이유가 자라면?", a: "어른유", h: "XX유", r: "아이가 자라나면 어른이 됩니다." },
  { q: "감자가 의문을 품으면?", a: "웨지감자", h: "XX감자", r: "왜지? 의문을 품은 감자" },
  { q: "세상에서 가장 큰 차는?", a: "아프리카", h: "XXX카", r: "광활한 대륙 아프리카" },
  { q: "멋대로 운전하는 사람들이 다니는 길은?", a: "마음대로", h: "XX대로", r: "강남대로가 아닌 자기 마음대로~" },
  { q: "탄산이 요리를 하면?", a: "베이킹소다", h: "XXX소다", r: "BAKING소다" },
  { q: "[치질에 걸린 양]을 세글자로?", a: "양치질", h: "양XX", r: "치질에 걸린 양이라 양치질?" },
  { q: "던지고 때려도 칭찬받는 사람은?", a: "야구선수", h: "XX선수", r: "공을 잘 던치고 잘 때리면 칭찬 받는 야구 선수" },
  { q: "경찰서의 반댓말은?", a: "경찰앉아", h: "경찰XX", r: "일어서다의 반대는 앉다" },
  { q: "땅이 우는 소리는?", a: "흙흙", h: "흙X", r: "슬플 때 우는 소리 흑흑(흙흙~)" },
  { q: "고리 중, 입을 수 있는 고리는?", a: "저고리", h: "X고리", r: "우리 전통의상 중 하나~ 저고리" },
  { q: "노래할 때 먼저 찾아가는 도시는?", a: "전주", h: "X주", r: "노래시작 전 전주가 나오죠~" },
  { q: "형을 너무 좋아하는 동생을 세글자로?", a: "형광펜", h: "XX펜", r: "형(을) 광(적으로 좋아하는) 팬(펜)" },
  { q: "아버지가 생선 드실 때 하는 말은?", a: "어두컴컴", h: "XX컴컴", r: "고기는 머리가 맛있죠, 어두 컴컴!" },
  { q: "동물원에서 살 수 있는 동물은?", a: "판다", h: "X다", r: "판다~ 판다!" },
  { q: "곰이 물구나무를 서면?", a: "문", h: "X (곰을 꺼꾸로 뒤집으면?)", r: "곰을 거꾸로 뒤집으면 문" },
  { q: "아몬드가 죽으면?", a: "다이아몬드", h: "XX아몬드", r: "다이(die) 아몬드" },
  { q: "[너는 정말 잘생겼구나]를 네글자로 표현하면?", a: "그걸 믿니", h: "그걸 XX", r: "여러분은 스스로 잘생겼다고 생각하시나요?" },
  { q: "공부를 열심히 해도 늘 배우라는 말을 듣는 사람은?", a: "배우", h: "배X", r: "사람들은 배우에게 늘 배우라고 부르죠" },
  { q: "비행기 안에 화장실을 다섯 글자로?", a: "공중화장실", h: "XX화장실", r: "공중에 있는 비행기 속 화장실이니, 공중화장실" },
  { q: "세상에서 가장 빠른 닭은?", a: "후다닥", h: "후X닥", r: "후다닥(닭) 빠르게 달리는 닭!" },
  { q: "손님이 뜸하면 돈버는 사람은?", a: "한의사", h: "X의사", r: "손님이 뜸하러 오면 한의사는 돈을 벌죠" },
  { q: "소녀시대가 옷 가게에 가서 하는 말은?", a: "티파니", h: "X파니", r: "티(티셔츠) 파니?" },
  { q: "세상에서 가장 억울한 도형은?", a: "원통", h: "X통", r: "억울하고 원통합니다." },
  { q: "울면 안되는 날은?", a: "울면집이 쉬는 날", h: "XX집이 쉬는 날", r: "울면집이 쉬면 울면을 시켜 먹을 수 없죠" },
  { q: "[불에 타고 있는 뱀]을 네 글자로?", a: "뱀파이어", h: "X파이어", r: "뱀이 파이어(fire) 안에 있으니 뱀파이어" },
  { q: "세상에서 가장 장사를 잘하는 동물은?", a: "판다", h: "X다", r: "뭐든지 다 팔아버린다! 다 판다!" },
  { q: "[달에 사는 사람]을 두 글자로?", a: "달인", h: "달X", r: "달에 간 사람(사람 인,人)" },
  { q: "가장자리에 있는 돌은?", a: "구석", h: "X석", r: "구석(石)" },
  { q: "산불이 났을 때 발생하는 기체는?", a: "탄산가스", h: "XX가스", r: "불에 탄 산에서 나오는 가스" },
  { q: "세상에서 가장 아름다운 개는?", a: "무지개", h: "XX개", r: "빨주노초파남보~ 아름다운 무지개" },
  { q: "사람들이 가장 좋아하는 방석은?", a: "돈방석", h: "X방석", r: "돈방석을 싫어하는 사람이 있을까요?" },
  { q: "몸이 아파도 반드시 가야하는 대학교는?", a: "가야대", h: "XX대", r: "무슨일이 있어도 가야하는 가야대!" },
  { q: "세상에서 가장 이쁜 소는?", a: "미소", h: "X소", r: "미소(소 우,牛)가 가장 아름답죠~" },
  { q: "남자들만 모여있는 지하철 역은?", a: "남성역", h: "XX역", r: "남성들은 남성역에서 모여라!" },
  { q: "만두가게 주인이 가장 듣기 싫어하는 말?", a: "속터져", h: "속XX", r: "속터진 만두는 팔 수 없죠" },
  { q: "소는 소인데 도저히 알 수 없는 소는?", a: "모르겠소", h: "XXX소", r: "정말 모르겠소(소 우,牛)" },
  { q: "직장인들이 가장 좋아하는 SNS는?", a: "페이수북", h: "XX수북", r: "월급(pay) 수북하게" },
  { q: "[옷장안에 불이 났다]를 다섯글자로 하면?", a: "장안의 화재", h: "XX의 화재", r: "(옷)장안에 화재가~" },
  { q: "이자 없이 꾸는 것은?", a: "꿈", h: "X (자면서 꾸는 것)", r: "우리 모두 이자 없이 꿈 꿀수 있습니다!" },
  { q: "인천 앞바다의 반대말?", a: "인천엄마다", h: "인천XXX", r: "앞바다, 아빠다? 엄마다!" },
  { q: "국사책을 태우면?", a: "불국사", h: "X국사", r: "불(탄)국사(책) - 불국사" },
  { q: "헤어진 사람들이 만나는 지하철 역은?", a: "상봉역", h: "XX역", r: "서로 만나는 상봉~ 상봉하는 역 - 상봉역" },
  { q: "[매우 바쁜 떡]을 세 글자로?", a: "비지떡", h: "비지X", r: "매우 바쁜(busy) 떡이라 비지떡" },
  { q: "가수 샤이니가 사는 동네 이름은?", a: "링딩동", h: "XX동", r: "링딩동~ 링딩동~ 샤이니의 노래죠!" },
  { q: "서울시민 모두가 동시에 외치면?", a: "천만의말씀", h: "XX의 말씀", r: "서울시민 천만의 말씀" },
  { q: "네 마리의 고양이가 괴물이 되면?", a: "포켓몬스터", h: "XX몬스터", r: "포(four) 켓(cat) 이 몬스터(monster)로!" },
  { q: "조금 뚱뚱한 사람들이 사는 동네는?", a: "반포동", h: "XX동", r: "반(만) 포동~포동한 사람들이 사는 동네~" },
  { q: "1,2,3위보다 4위를 좋아하는 사람은?", a: "장모님", h: "장XX", r: "장모님은 사위를 좋아하죠" },
  { q: "문인데 닫지 못하는 문은?", a: "소문", h: "X문", r: "소문은 닫을 수 없죠" },
  { q: "아무리 예뻐도 미녀라고 못하는 사람은?", a: "미남", h: "미X", r: "남자가 아무리 예뻐도 여자라고 할 순 없죠" },
  { q: "가장 안전한 감옥은?", a: "안전제일", h: "안전XX", r: "안전한 JAIL" },
  { q: "세상에서 가장 보기 싫은 개는?", a: "꼴불견", h: "XX견", r: "꼴불견(개 견,犬)" },
  { q: "세상에서 가장 더러운 얼음은?", a: "오무라이스", h: "XX라이스", r: "오물아이스? 오무라이스!" },
  { q: "신경통 환자들이 가장 싫어하는 악기는?", a: "비올라", h: "XX라", r: "비오면 허리아픈 신경통 환자들, 내일도 비올라?" },
  { q: "청소년이 옥상에서 떨어지면?", a: "비행청소년", h: "XX청소년", r: "비행하며 떨어진 비행 청소년" },
  { q: "다섯 그루의 나무를 두 글자로 하면?", a: "오목", h: "X목", r: "오(5) 목(木) - 다섯 그루의 나무" },
  { q: "밥먹고 나면 찾아오는 거지는?", a: "설거지", h: "X거지", r: "밥먹고 꼭 하는 설거지" },
  { q: "요리를 잘하는 왕은?", a: "쿠킹", h: "X킹", r: "요리를(cooking)을 잘하는 왕(king)" },
  { q: "세상에서 가장 뜨거운 전화는?", a: "화상전화", h: "XX전화", r: "너무 뜨거워 화상에 걸린 걸까요? 그래서 화상전화?" },
  { q: "오리가 얼면 어떻게 될까?", a: "언덕", h: "X덕", r: "오리가 얼면 언(frozen) 덕(duck)" },
  { q: "할 말 있는 사람들이 모이는 지하철역은?", a: "대화역", h: "XX역", r: "대화가 필요한 사람들은 대화역으로~" },
  { q: "달이 방구를 끼면?", a: "문방구", h: "XX구", r: "문(MOON) 방구" },
  { q: "불을 끄면 보이고, 불을 켜면 보이지 않는 것은?", a: "야광", h: "X광", r: "야광은 불을 꺼야 볼 수 있습니다" },
  { q: "김치의 신은?", a: "갓김치", h: "X김치", r: "갓(GOD) 김치" },
  { q: "아무리 뜨겁게 해도 '이것은 차다'라고 말하는 것은?", a: "차", h: "X (문제에 답이 있어요!)", r: "차(茶)는 뜨거워도 차가워도 차라고 합니다." },
  { q: "군인들이 좋아하는 시간은?", a: "저녁", h: "저X", r: "전역할떄" },
  { q: "임산부에게 치명적인 균은?", a: "유산균", h: "XX균", r: "임산부에게 유산은 위험하죠" },
  { q: "닭은 닭인데 먹지 못하는 닭은?", a: "까닭", h: "X닭", r: "닭은 먹어도 까닭은 먹을 수 없죠~" },
  { q: "[신발이 화가 났다]를 세글자로?", a: "신발끈", h: "XX끈", r: "신(신발)이 화나서 발끈했다" },
  { q: "눈이 오면 강아지가 폴짝 폴짝 뛰는 이유는?", a: "발이 시려서", h: "X이 시려서", r: "좋은 것 보다는 발이 시려서 뛰는 걸까요?" },
  { q: "부산 앞바다 반대말은?", a: "부산엄마다", h: "부산XXX", r: "앞바다, 아빠다? 엄마다!" },
  { q: "어떤 약속도 다 들어주는 산은?", a: "수락산", h: "XX산", r: "모든 약속을 수락하겠다! 수락산!" },
  { q: "참새의 영어이름은?", a: "Jack", h: "JXXK", r: "참새는 짹짹 우니까 ~ 잭(jack)?" },
  { q: "원더걸스가 즐겨먹는 쌀은?", a: "텔미", h: "X미", r: "원더걸스의 텔미(米)" },
  { q: "세상에서 가장 큰 다이아몬드가 있는 곳?", a: "야구장", h: "XX장", r: "야구장의 모양 때문에 다들 '녹색 다이아몬드'라고 부릅니다." },
  { q: "신데렐라가 잠을 못 자면?", a: "모짜렐라", h: "XX렐라", r: "잠을 못자, 모짜렐라" },
  { q: "포도가 자기소개할 때 하는 말은?", a: "포도당", h: "XX당", r: "나는 '포도다(포도당~!)'" },
  { q: "딸기가 직장을 잃으면?", a: "딸기시럽", h: "딸기XX", r: "딸기야 너무 슬퍼하지마, 시럽은 맛있어~" },
  { q: "가까이 있지만 서로 못 보는 것은?", a: "눈", h: "X (얼굴에 있는 것)", r: "왼쪽/오른쪽 눈은 가깝지만 서로 볼 수 없죠" },
  { q: "맞으면 맞을 수록 좋은 것은?", a: "시험문제", h: "XX문제", r: "시험문제는 많이 맞으면 좋죠" },
  { q: "새로운 욕이 만들어 지는 곳은?", a: "뉴욕", h: "X욕", r: "새로운(new) 욕이 만들어져서, 뉴욕" },
  { q: "사람들이 가장 무서워 하는 사자는?", a: "저승사자", h: "XX사자", r: "죽음을 몰고오는 공포의 대상! 저승사자" },
  { q: "슈퍼주니어 멤버 신동 옆에 있는 사람은?", a: "신동엽", h: "신XX", r: "신동 옆에 있는 사람" },
  { q: "나무들이 나오는 드라마는?", a: "수목드라마", h: "XX드라마", r: "수목(樹木, 나무)들이 나오니 수목드라마!" },
  { q: "[영웅은 여자를 좋아한다]를 네 글자로?", a: "영웅호걸", h: "영웅XX", r: "영웅은 호(좋을 호,好) 걸(girl)" },
  { q: "안녕을 5번 말하면?", a: "하이파이브", h: "하이XXX", r: "하이X5 = 하이파이브!" },
  { q: "소고기가 불에 타면?", a: "탄소", h: "X소", r: "불에 탄 소" },
  { q: "야채 장수들이 가장 싫어하는 도시는?", a: "시드니", h: "XX니", r: "모든 야채가 시들 시들~ 이렇게 시드니?" },
  { q: "물로 만든 고양이를 세글자로?", a: "물로켓", h: "XX켓", r: "물로 만든 고양이(cat), 물로캣(켓)" },
  { q: "결정 장애자가 많은 나라는?", a: "고려", h: "고X", r: "고려할게 많아서 결정을 못함" },
  { q: "소가 이민을 가면?", a: "이민우", h: "XX우", r: "이민을 간 소(소 우,牛) - 이민우" },
  { q: "올라 갈 수는 있지만, 내려갈 수 없는 것은?", a: "나이", h: "X이", r: "나이는 한살씩 올라가지만 내려가지는 않습니다." },
  { q: "차두리가 불에 타면?", a: "두리번", h: "두리X", r: "두리번(BURN)" },
  { q: "식인종이 밥투정할 때 하는 말?", a: "살맛안나", h: "XX안나", r: "사람 살맛이 안나" },
  { q: "세상에서 가장 착한 사자는?", a: "자원봉사자", h: "XXX사자", r: "자원봉사하는 자원봉사자" },
  { q: "늘 '지금', '당장'을 외치는 소는?", a: "롸잇 나우", h: "롸잇 X우", r: "롸잇(right) 나우(now,牛)" },
  { q: "책인데 읽을 수 없는 책은?", a: "주책", h: "X책", r: "주책은 읽을 수 없습니다." },
  { q: "걸그룹 여자친구가 좋아하는 나물은?", a: "두릅", h: "두X", r: "오늘부터 우리는 su tu tu ru 좋아해요" },
  { q: "비행소녀를 영어로?", a: "스튜어디스", h: "XXX디스", r: "비행기 안에서 일하는 여성(소녀)는 스튜어디스입니다." },
  { q: "사람들이 가장 무서워하는 물은?", a: "괴물", h: "X물", r: "괴물만큼 무서운게 없죠" },
  { q: "아이가 장난감을 사 달라고 하는 것을 다른 말로?", a: "고르곤졸라", h: "XXX졸라", r: "고르고서 사달라 졸라" },
  { q: "스포츠 경기 때마다 바빠지는 역은?", a: "중계역", h: "XX역", r: "스포츠 경기 중계로 바쁜 중계역!" },
  { q: "바닷가에서 김을 지키는 사람은", a: "김경호", h: "김XX", r: "김을 경호하는" },
  { q: "김밥이 경찰서에 간 이유는?", a: "참깨가고소해서", h: "참깨가XXXX", r: "김밥에 올리는 참깨가 고소해서" },
  { q: "나무를 주면 살고, 물을 주면 죽는 것은?", a: "불", h: "X (활활 타오르는 그것!)", r: "불은 나무를 주면 더 타오르고, 물을 주면 꺼질 수 있습니다" },
  { q: "[형이 박을 줬다]를 세 글자로?", a: "박준형", h: "XX형", r: "박을 준 형 - 박준형" },
  { q: "잘생긴 가위는?", a: "핸섬가이", h: "XX가이", r: "handsome (잘생긴) 가위" },
  { q: "병든 자여 내게로 오라고 한 사람은?", a: "엿장수", h: "X장수", r: "엿장수 아저씨는 병을 주면 엿으로 바꿔주죠" },
  { q: "누우면 멀어지고 일어나면 가까워지는 것은?", a: "천장", h: "X장", r: "높아질 수록 가까워 지는 곳~ 천장입니다." },
  { q: "가수 빅뱅이 잘 내는 동물 소리는?", a: "암소소리", h: "XX소리", r: "I'm so sorry(암쏘소리~) but I love - 빅뱅 거짓말" },
  { q: "날마다 이상한 것을 보는 사람은?", a: "치과의사", h: "XX의사", r: "이가 상하면 치과로 가서 치료를 받죠" },
  { q: "[남자]는 있고 [여자]는 없는 것?", a: "글자의 받침", h: "글자의 XX", r: "남자는 ㅁ 받침이, 여자는 받침이 없죠" },
  { q: "미꾸라지 보다 한 단계 더 큰 미꾸라지는?", a: "미꾸엑스라지", h: "미꾸XX라지", r: "라지(large)보다 큰 사이즈는 엑스라지(xlarge)" },
  { q: "천마리의 호랑이가 사는 동네는?", a: "천호동", h: "XX동", r: "천마리의 호랑이 = 천호" },
  { q: "진짜 새를 두 글자로 줄이면?", a: "참새", h: "X새", r: "참(truth) 새(bird)라서 진짜 새" },
  { q: "펭귄이 다니는 고등학교는?", a: "냉장고", h: "XX고", r: "냉장고(高)등학교" },
  { q: "유럽인이 먹는 음식은?", a: "이유식", h: "XX식", r: "유럽 국가는 EU에 가입된 국가가 많죠?" },
  { q: "세상에서 가장 예의없는 머리는?", a: "버르장머리", h: "XXX머리", r: "어른들이 자주 말씀하시죠 - 버르장머리 없는 놈!" },
  { q: "아재가 침을 흘리면?", a: "아밀라아제", h: "아밀XXX", r: "침에는 아말라아제가 있죠" },
  { q: "학생들이 학교에 가는 이유는?", a: "학교가 올 수 없으니까", h: "XX가 올 수 없으니까", r: "학교가 온다면 등교할 필요 없겠네요?" },
  { q: "공포영화를 봐도 놀라지 않는 연예인은?", a: "태연", h: "X연", r: "언제나 태연한 태연?" },
  { q: "사공이 많으면 배는 어떻게 될까?", a: "가라 앉는다", h: "XX 앉는다", r: "너무 무거워서" },
  { q: "열두 달 중 뱀과 별이 없는 달은?", a: "11월", h: "XX월", r: "NO뱀버" },
  { q: "닭이 열 받으면 어떻게 될까?", a: "통닭이 된다", h: "XX이 된다", r: "닭을 불에 구우면 통닭~!" },
  { q: "땅콩의 반댓말은?", a: "하늘콩", h: "XX콩", r: "땅의 반대 말은 하늘" },
  { q: "똥의 성씨는?", a: "응가", h: "X가", r: "똥은 가문이 응가(家)일까요?" },
  { q: "[산에서 떨어지다 나무에 걸린 사람]을 여섯 글자로?", a: "덜떨어진 사람", h: "XXXX 사람", r: "떨어지다 중간에 걸렸으니, 덜 떨어진 사람" },
  { q: "가수 씨엔블루가 신는 신발은?", a: "정용화", h: "XX화", r: "정용화(靴)를 신는 씨엔블루?" },
  { q: "물고기 중, 가장 공부를 열심히 한 물고기는?", a: "닥터피쉬", h: "XX피쉬", r: "공부를 열심히 해서 의사가 된 물고기 - 닥터피쉬" },
  { q: "먹고 살기 위해 하는 내기는?", a: "모내기", h: "X내기", r: "쌀을 얻기 위해서는 꼭 해야하는 모내기" },
  { q: "자동차를 톡하고 치면?", a: "카톡", h: "X톡", r: "자동차(car)를 톡!" },
  { q: "매일 달리는 사람들이 모인 도시는?", a: "경주", h: "X주", r: "오늘도 달리기 경주다!!" },
  { q: "양이 맨 꼭대기에 있는 모습은?", a: "고양이", h: "X양이", r: "높은 곳(고)에 있는 양이" },
  { q: "가수 비 매니저가 하는 일을 네 글자로?", a: "비만관리", h: "비만XX", r: "매니저가 비만 관리해서~ 비만관리" },
  { q: "모든 사람이 가장 좋아하는 일은?", a: "휴일", h: "X일", r: "휴일만큼 행복한게 또 있을까요?" },
  { q: "헌병이 가장 무서워하는 사람은?", a: "고물상주인", h: "XXX주인", r: "고물상 주인이 헌병을 전부 가져가요" },
  { q: "보라돌이 나나 뽀가 자기들끼리 식사할 때 두비가 하는 말은?", a: "뚜비두밥", h: "XX두밥", r: "뚜비두 밥줘" },
  { q: "길에서 인기 많은 성은?", a: "택씨", h: "X씨", r: "여기저기서 불러요~ 택시!" },
  { q: "미국에서 비가 내리면?", a: "USB", h: "XXB", r: "US에 비" },
  { q: "이장님과 이장님이 힘을 합치면 어떻게 될까?", a: "사장님", h: "XX님", r: "이(2)장 + 이(2)장 = 사(4)장" },
  { q: "장 그래 가 밥을 먹는다를 영어로 하면?", a: "great", h: "GRXXX", r: "그래 잇(eat)" },
  { q: "세종대왕님이 만든 우유는?", a: "아야어여오요우유", h: "아야어여XXXX", r: "한글에서 아야어여오요(우유!)" },
  { q: "사람이 안에 있지만, 문을 두드려도 열어주지 않는 곳은?", a: "화장실", h: "XX실", r: "화장실은 밖에서 문을 두드려도 열어주지 않죠" },
  { q: "[살아있는 쥐]를 두 글자로?", a: "생쥐", h: "X쥐", r: "살아있는 쥐 - 생쥐!" },
  { q: "오리를 생으로 먹으면?", a: "회오리", h: "X오리", r: "날로 먹는 회 + 오리" },
  { q: "세상에서 가장 더운 여자는?", a: "더운걸", h: "XX걸", r: "더운 여자(girl) 그래서 더운걸~" },
  { q: "하루만 지나도 헌 것이 되는 것은?", a: "신문", h: "X문", r: "매일 매일 나오는 신문은 어제 것도 헌 것이 됩니다." },
  { q: "목적지가 얼마나 먼지 길 가는 여성분께 묻는 말은?", a: "아주머니", h: "XX머니", r: "아주~ 머니?" },
  { q: "[수학]을 한 글자로 줄이면?", a: "솩", h: "X (빨리 발음해 보세요)", r: "수학을 빠르게 발음해 보세요~ 솩" },
  { q: "쓸수록 자꾸 늘어나는 것은?", a: "빚", h: "X (이자가 붙어서?)", r: "빚은 쓰면 쓸수록 늘어납니다~" },
  { q: "누구나 가지고 있는 풀은?", a: "눈꺼풀", h: "XX풀", r: "우리 몸에 한 부분 - 눈꺼풀!" },
  { q: "발인데, 허공에서 춤추는 발은?", a: "깃발", h: "X발", r: "허공에 매달려 바람에 춤추는 깃발" },
  { q: "춤 중에서 가장 황홀한 춤은?", a: "입맞춤", h: "XX춤", r: "황홀한 입맞춤" },
  { q: "해가 울면 사람들이 뭐라고 할까?", a: "해운대", h: "XX대", r: "해가 운대, 그래서 해운대" },
  { q: "소금이 죽으면?", a: "죽염", h: "X염", r: "죽은 염(소금, 鹽)이라서 죽염" },
  { q: "사람들이 가장 좋아하는 국은?", a: "천국", h: "X국", r: "누구나 천국에 가고 싶어 합니다!" },
  { q: "차 문을 세게 닫으면 안 되는 이유?", a: "차문은4개니까", h: "차문은XXXX", r: "차문은 4개, 3개만 닫으면 안되죠" },
  { q: "신사가 자기를 소개할 때, 말하는 네 글자?", a: "신사임당", h: "신사XX", r: "[신사 입니다]를 빠르게 발음하면 [신사임당]" },
  { q: "뭐든 부수는 왕은?", a: "브레이킹", h: "XXX킹", r: "BREAK! 브레이KING!" },
  { q: "오리는 있지만 거위는 없는 곳은?", a: "지하철역", h: "XXX역", r: "지하철역 중 오리역이 있는거 알고 있으신가요?" },
  { q: "타이타닉의 구명보트에는 몇 명이 탈 수 있을까?", a: "구명", h: "X명", r: "구명 보트이므로 9명" },
  { q: "호주에서 생산하는 쌀은?", a: "호미", h: "X미", r: "쌀을 한자로 쓰면 미(米-쌀미)" },
  { q: "운전자들이 가장 싫어하는 턱은?", a: "과속방지턱", h: "XXXX턱", r: "운전할 때 과속방지턱이 많으면 불편합니다~" },
  { q: "낮에만 가는 시계는?", a: "해시계", h: "X시계", r: "해시계는 태양의 움직임을 측정합니다." },
  { q: "산타가 중국집에 가면?", a: "울면안돼", h: "XX안돼", r: "울면 안돼" },
  { q: "코가 긴 사람은?", a: "장비", h: "장X", r: "긴(長 장) 코(鼻 비)" },
  { q: "산토끼의 반대말은?", a: "판토끼", h: "X토끼", r: "[사다]의 반대말은 [팔다]" },
  { q: "자기 전에 먹어야 되는 과자는?", a: "눈을감자", h: "XX감자", r: "자기전에 눈을 감아야죠" },
  { q: "인터넷에서 가장 많이 볼 수 있는 색은?", a: "검색", h: "X색", r: "우리가 인터넷에서 매일 하는 검색" },
  { q: "정보통신이 가장 발달한 나라는?", a: "아이티", h: "XX티", r: "IT가 발달한 나라라서 아이티!!" },
  { q: "방울인데, 소리가 나지 않는 방울은?", a: "솔방울", h: "X방울", r: "솔방울은 흔들어도 소리가 나지 않습니다" },
  { q: "때리면 살고 안 때리면 죽는 것은?", a: "팽이", h: "팽X", r: "팽이는 때리지 않고 있으면 쓰러집니다" },
  { q: "[스님이 길을 가다 9를 보다]를 세 글자로?", a: "구본승", h: "구본X", r: "숫자 9(구)를 본 승려 - 구본승" },
  { q: "거만한 사람들이 모여 사는 나라는?", a: "오만", h: "X만", r: "오만한 사람들이 모여있는 걸까요?" },
  { q: "공부 안하고 매일 노는 고1 학생은 1년 후에 뭐가 될까?", a: "고2", h: "고X", r: "고1학생은 1년 후에 고2가 됩니다." },
  { q: "수학자들이 가장 좋아하는 보석은?", a: "사파이어", h: "사파XX", r: "4πr" },
  { q: "일이 산더미처럼 쌓인 역은?", a: "일산역", h: "XX역", r: "일이 산처럼 쌓인 역, 일산역!" },
  { q: "등산을 좋아하는 왕은?", a: "하이킹", h: "XX킹", r: "하이킹을 좋아하는 킹(King)" },
  { q: "그랜저가 죽으면?", a: "그랜다이져", h: "그랜XXX", r: "그랜 다이(DIE)져" },
  { q: "안 씻는 사람들이 모여사는 성은?", a: "안시성", h: "XX성", r: "너희 오늘도 안 씻어?(안시성~)" },
  { q: "파가 가지고 있는 공짜 차는?", a: "파프리카", h: "파XX카", r: "파 free(공짜) car(차)" },
  { q: "엄마가 상자다를 영어로 하면?", a: "megabox", h: "XXXXbox", r: "앰이가박스" },
  { q: "개가 화를 쉽게 내는 이유는?", a: "분노개이지폭발", h: "분노XXX폭발", r: "분노 개 이지(EAZY) 폭발" },
  { q: "쥐 네마리가 모인 것을 두 글자로?", a: "쥐포", h: "X포", r: "쥐가 네마리(4,four)라 쥐포!" },
  { q: "가장 길고, 가장 짧은 단어는?", a: "생략", h: "X략", r: "아무리 길고/짧아도 생략할 수 있죠" },
  { q: "[소가 번개에 맞아 죽다]를 다섯 글자로?", a: "우사인볼트", h: "우사인XX", r: "우(牛)의 사인(死因)은 볼트(bolt, 번개)" },
  { q: "전화기가 다니는 중학교는?", a: "부재중", h: "XX중", r: "부재중(中)학교" },
  { q: "예수님과 부처님의 가장 큰 차이점은?", a: "종교차이", h: "XX차이", r: "두 분의 차이는 기독교, 불교 종교차이~" },
  { q: "와이프가 언니를 소개해주겠다고 집에 데려왔다를 다른말로?", a: "공개처형", h: "공개XX", r: "처형을 공개" },
  { q: "토마토는 거꾸로 하면 '토마토', 그럼 쓰레기통을 거꾸로 하면?", a: "쓰레기가 쏟아진다", h: "쓰레기가 XXXX", r: "통을 뒤집으니 쓰레기가 나오곘죠?" },
  { q: "맥주가 죽기 전에 남긴 말은?", a: "유언비어", h: "유언XX", r: "유언을 남긴 맥주(BEER)" },
  { q: "[12345678]을 네글자로 줄이면?", a: "영구없다", h: "XX없다", r: "0과 9가 없네요? 영구없다~" },
  { q: "알파벳이 눈물을 흘리면?", a: "운영자", h: "X영자", r: "운 영자(영문자)" },
  { q: "이효리가 타고 다니는 차는?", a: "겟차", h: "X차", r: "이효리 겟차" },
  { q: "우유가 넘어지면?", a: "아야", h: "아X", r: "우유 글씨가 옆으로 넘어져수 누우면 아야" },
  { q: "갓 쓰고 한 평생 부엌에서만 사는 것은?", a: "솥", h: "X (X뚜껑)", r: "솥은 갓처럼 생긴 솥뚜껑을 쓰고 부엌에 있습니다" },
  { q: "[호랑이 아홉마리]를 두 글자로?", a: "호구", h: "X구", r: "호랑이(범 호,虎) 아홉 마리 - 호구" },
  { q: "아무리 빨리 달려도 앞서가지 못하는 것은?", a: "자동차 뒷바퀴", h: "자동차 X바퀴", r: "뒷바퀴가 앞바퀴를 앞지를 수는 없죠" },
  { q: "둘리가 다니는 고등학교는?", a: "요리보고", h: "XXX고", r: "요리보고~ 저리봐도~ 알수 없는 둘리 둘리!" },
  { q: "아기들이 배고플 때 가는 지하철 역은?", a: "수유역", h: "XX역", r: "아이가 배고프면 수유, 수유는 수유역에서~" },
  { q: "[딸기가 직장을 잃었다]를 네 글자로?", a: "딸기시럽", h: "XX시럽", r: "딸기가 실업자가 되니, 딸기시럽(실업)" },
  { q: "손가락은 영어로 '핑거', 그럼 주먹을 영어로?", a: "오므린거", h: "XXX거", r: "손가락은 핑거~ 손가락을 오므린 주먹은 오므린거~" },
  { q: "음력 설날에 사용하는 물은?", a: "구정물", h: "XX물", r: "구정에 사용하는 물이라 구정물" },
  { q: "사자마자 물에 적셔 버리는 옷은?", a: "수영복", h: "XX복", r: "수영복은 물속에서만 사용하죠" },
  { q: "가수 비를 아는지 물어볼 때, 말하는 네 글자?", a: "너비아니", h: "너비XX", r: "너 비 아니(알고있니)?" },
  { q: "다이어트하는 사람이 여름을 싫어하는 이유는?", a: "비만이와서", h: "비XXXX", r: "비가 많이 와서, 비만이 와서" },
  { q: "눈인데 앞을 볼 수 없는 눈은?", a: "함박눈", h: "XX눈", r: "하늘에서 내리는 함박눈은 앞을 볼 수 없습니다." },
  { q: "나나가 지구에 오면?", a: "지구온나나", h: "지구XXX", r: "지구에 온 나나" },
  { q: "비랑 내가 같이 있으면?", a: "비엔나", h: "비XX", r: "비 AND 나" },
  { q: "음식에 다시다를 넣으면?", a: "음식이다시다", h: "음식이XXX", r: "음식에 다시다를 넣으면 음식이 다 시죠" },
  { q: "모기가 피를 빠는 이유는?", a: "모기말라서", h: "모기XXX", r: "모기가 목이 말라서" },
  { q: "방인데 들어갈 수 없는 방은?", a: "나방", h: "X방", r: "나방에 들어갈 수는 없습니다." },
  { q: "제빵왕 김탁구가 가장 싫어하는 개그는?", a: "빵터지는 개그", h: "XX지는 개그", r: "제빵왕은 빵이 터지는 걸 가장 싫어합니다" },
  { q: "세상에서 가장 비가 많이 오는 곳은?", a: "비무장지대", h: "XXX지대", r: "비가 무진장 많이 오는 곳, 비~무장~지대" },
  { q: "옛날 왕실의 돈 관리는 누가 했을까?", a: "세자", h: "X자", r: "돈을 잘 세니, 세자가 돈을 관리해라~!" },
  { q: "방바닥 보다 높은 바닥은?", a: "발바닥", h: "X바닥", r: "우리 발바닥은 항상 방바닥 위에 있죠" },
  { q: "[가수 비와 내가 소의 마리 수를 센다]를 여섯 글자로?", a: "비엔나 소세지", h: "비엔나 XXX", r: "비&(엔)나, 소(소 우,牛) 세지(수를 세다)" },
  { q: "[불이 4개 있다]를 네글자로?", a: "사파이어", h: "X파이어", r: "사(4) 파이어(fire)" },
  { q: "목욕탕에 가면 두고 나오는 것은?", a: "때", h: "X (밀고 나오는 것)", r: "목욕탕에서는 때를 밀고 나오죠" },
  { q: "바다에 사는 오리는?", a: "미더덕", h: "XX덕", r: "미더덕(DUCK)" },
  { q: "말과 행동을 같이하는 사람은?", a: "기수", h: "X수", r: "기수는 늘 말(말 마,馬)과 함께 행동하죠" },
  { q: "매일 미안한 동물은?", a: "오소리", h: "X소리", r: "오~ SORRY" },
  { q: "한번 웃으면 영원히 웃는 것은?", a: "사진", h: "X진", r: "한번 찍은 사진은 변하지 않습니다." },
  { q: "미술학과 학생들이 타고 다니는 버스는?", a: "캔버스", h: "XX스", r: "미술 그림 그리는 캔버스" },
  { q: "세상에서 가장 행복한 바다는?", a: "사랑해", h: "XX해", r: "사랑해(海), 사랑만큼 행복한 건 없죠" },
  { q: "이 빠진 호랑이는?", a: "호랑", h: "X랑", r: "글자 '호랑이'에서 이를 빼면 - 호랑" },
  { q: "시속 300km의 기차와 부딪혀도 살 수 있는 방법은?", a: "뒤에서 부딪힌다", h: "X에서 부딪힌다", r: "뒤에서 부딪히면 가속도를 받지 않겠죠?" },
  { q: "불인데, 연기가 없는 불은?", a: "반딧불", h: "XX불", r: "반짝 반짝 반딧불은 연기가 나지 않습니다~" },
  { q: "우리가 매일 받는 상은?", a: "밥상", h: "X상", r: "매일 매일 밥상에서 밥을 먹죠~" },
  { q: "[문을 두드리는 여자]를 다섯 글자로?", a: "똑똑한 여자", h: "XX한 여자", r: "문을 똑똑~ 하고 두드리는 여자" },
  { q: "모기가 좋아하는 은행은?", a: "혈액은행", h: "XX은행", r: "혈액이 모여있는 곳 - 혈액은행" },
  { q: "서울에서 가장 긴 전철 역은?", a: "길음역", h: "XX역", r: "가장 길어서 길음?" },
  { q: "바다에서 태어났지만, 바다로 가면 사라지는 것은?", a: "소금", h: "X금", r: "소금은 바다에서 나오지만, 바다에 담그면 녹아 사라집니다." },
  { q: "두 발로 걸어다니는 쥐는?", a: "미키 마우스", h: "XX 마우스", r: "미키마우스는 두 발로 걸어 다닙니다." },
  { q: "뽀가 집에 가면?", a: "뽀빠이", h: "뽀XX", r: "뽀야 안녕~ 뽀 빠이~" },
  { q: "세상에서 가장 못생긴 새는?", a: "니생김새", h: "X생김새", r: "너의 생김새" },
  { q: "우리나라에서 가장 화가 많이 나있는 도시는?", a: "성남", h: "X남", r: "성남시는 성남" },
  { q: "일본에서 가장 낚시를 잘하는 사람은?", a: "다나까", h: "다XX", r: "무엇이든 다 낚아! 다나까!" },
  { q: "웃긴 이빨은?", a: "펀치", h: "X치", r: "웃긴(Fun) 치아" },
  { q: "구리 중에 가장 강한 구리는?", a: "강호동", h: "XX동", r: "강호 동(구리)" },
  { q: "길을 가다 넘어진 오리를 세글자로?", a: "철퍼덕", h: "XX덕", r: "철퍼덕(duck) 넘어지는 경우 많죠?" },
  { q: "약인데, 아껴 먹는 약은?", a: "절약", h: "X약", r: "아껴쓰는 습관~ 절약" },
  { q: "파리 중에 가장 무거운 파리는?", a: "돌팔이", h: "X팔이", r: "돌(stone) 팔이(파리), 돌이라 무겁겠죠?" },
  { q: "새로 나온 욕은?", a: "뉴욕", h: "X욕", r: "NEW욕" },
  { q: "텔레토비가 만든 안경점 이름은?", a: "아이조아", h: "XX조아", r: "아이(eye)가 좋아~아이조아" },
  { q: "서울에서 가장 뚱뚱한 사람들이 사는 동네는?", a: "개포동", h: "XX동", r: "포동포동" },
  { q: "똑똑한 사람이 들어가는 방은?", a: "화장실", h: "XX실", r: "똑똑~ 밖에서 노크하고 들어가는 방 - 화장실" },
  { q: "나무가 네 개 있으면?", a: "포트리스", h: "XX리스", r: "Four Trees" },
  { q: "손님에게 깎아주려고 애쓰는 가게는?", a: "미용실", h: "XX실", r: "미용실은 손님의 머리를 깎아주는 곳 입니다." },
  { q: "청바지에 술을 부으면?", a: "진주", h: "X주", r: "진(jean) 주(酒)" },
  { q: "먹고 살기 위해 필요한 술은?", a: "기술", h: "X술", r: "기술이 있으면 돈을 벌어 먹고 살 수 있겠죠" },
  { q: "이 세상에서 가장 끝에 있는 것은?", a: "어금니", h: "XX니", r: "이(이 치,齒)의 가장 끝에 있는 건, 어금니" },
  { q: "김밥이 죽으면 어디로 가나요?", a: "김밥천국", h: "XX천국", r: "김밥은 김밥 천국으로" },
  { q: "하나에 하나를 더해도 다시 하나가 되는 것은?", a: "물방울", h: "X방울", r: "물방울 위에 물방울을 더해도 다시 하나의 큰 물방울이 됩니다." },
  { q: "처음 만난 소가 하는 말은?", a: "반갑소", h: "XX소", r: "만나서 반갑소(소 우,牛)~" },
  { q: "일본 프랑스 러시아를 합치면?", a: "왜블러", h: "X블러", r: "왜, 불, 러" },
  { q: "스캔들 없이 깨끗한 연예인은?", a: "노사연", h: "X사연", r: "스캔들 사연이 없는 연예인 노(NO) 사연" },
  { q: "서비스가 나쁜 식당이 노벨상을 받았다. 왜일까?", a: "테이블에 벨이 없어서", h: "테이블에XXXXX", r: "벨이 없어 No벨상" },
  { q: "곰돌이 푸한테 막 대하면?", a: "푸대접", h: "X대접", r: "푸대접 받으면 기분 나쁘죠!!" },
  { q: "우주인들이 술 마시는 곳은?", a: "스페이스바", h: "XX이스바", r: "스페이스 바(BAR)" },
  { q: "총을 대충 쏘면?", a: "설렁탕", h: "XX탕", r: "설렁 설렁 탕탕~ 총을 쏨" },
  { q: "감기에 빨리 걸리면?", a: "빨리감기", h: "XX감기", r: "빨리 걸린 빨리감기" },
  { q: "곰돌이 푸가 여러명이 모인 것을 두 글자로?", a: "푸들", h: "푸X", r: "푸 들이 모여있는 것이라 푸들!" },
  { q: "파 중에서 가장 유명한 파는?", a: "파스타", h: "파XX", r: "파 중에 스타(star) 파스타!" },
  { q: "회의하다가 웃으면?", a: "회의크", h: "회의X", r: "회의+크(크크크크 웃음)" },
  { q: "[물고기가 방귀를 뀌면]을 네 글자로?", a: "생선가스", h: "XX가스", r: "생선(물고기)이 가스를 뀌면 - 생선가스!" },
  { q: "대머리가 총을 한 발만 쏘는 이유는?", a: "두발이없어서", h: "XXX없어서", r: "머리카락(두발)이 없어서" },
  { q: "사람들이 가장 좋아하는 물은?", a: "선물", h: "X물", r: "선물 싫어하는 분 있으신가요?" },
  { q: "산에 불이 났다를 두글자로?", a: "산타", h: "산X", r: "산이 불에 타~ 산타~" },
  { q: "먹으면 먹을수록 힘이 빠지는 것은?", a: "더위", h: "X위", r: "먹으면 먹을 수록 힘이 빠지는 더위!" },
  { q: "자동차 키 색은?", a: "카키색", h: "XX색", r: "자동차(car) 키 색" },
  { q: "들 수 있는 있지만 오를 수 없는 산은?", a: "우산", h: "X산", r: "우산은 들지만, 오를 수 없죠?" },
  { q: "떡인데, 먹을 수 없는 떡은?", a: "그림의 떡", h: "XXX 떡", r: "그림은 먹을 수 없으니, 그림의 떡도 못먹겠네요" },
  { q: "화장실에서 볼 일을 보고 나온 사람은 어느나라 사람일까?", a: "일본사람", h: "X본사람", r: "화장실에서 일보고 나온사람 ~ 일본사람" },
  { q: "곰돌이 푸가 가장 싫어하는 동물은?", a: "아기염소", h: "XX염소", r: "동요 아기염소 - 아기염소 여럿이 풀을(푸를) 뜯고 놀아요~" },
  { q: "역 3개가 함께 있는 지하철 역은?", a: "역삼역", h: "XX역", r: "역이 3개라 - 역삼역" },
  { q: "[양치기 소년]이 사는 지하철 역은?", a: "목동역", h: "XX역", r: "양을 치는 사람은 목동, 목동이 사는 역은 목동역!" },
  { q: "엘리베이터에서 스타킹 조심해야 하는 이유?", a: "올라가니까", h: "X라가니까", r: "올라가니까? 올나가니까!" },
  { q: "땅이 급상승하면?", a: "지붕", h: "X붕", r: "땅(地 땅 지) 붕~" },
  { q: "매일 혼자 다니는 곤충은?", a: "아싸호랑나비", h: "아싸XXXX", r: "아싸 호랑나비~ 한마리가~" },
  { q: "겨울에 많이 쓰는 끈은?", a: "따끈따끈", h: "X끈X끈", r: "겨울에는 따끈따끈한게 최고죠" },
  { q: "우주에 사는 벌레는?", a: "스타벅스", h: "XX벅스", r: "스타~벅스" },
  { q: "슈렉 어머니가 가입한 단체 이름은?", a: "녹색어머니회", h: "XX어머니회", r: "슈렉은 녹색, 슈랙어머니는 녹색어미니회로~" },
  { q: "[이것이 코다]를 세글자로?", a: "디스코", h: "XX코", r: "디스(this) 코 - 디스코" },
  { q: "코미디언들이 모여있는 거리는?", a: "웃음거리", h: "XX거리", r: "코미디언들만 있다면 웃음 넘치는 거리가 되겠네요!" },
  { q: "수컷 소가 암컷 소의 발을 밟았을 때?", a: "암소쏘리", h: "XX쏘리", r: "암소야 미안해 SORRY" },
  { q: "늑대가 동생을 부를 때 하는 말은?", a: "아우", h: "X우", r: "동생을 부르는 울음 - 아~ 우!!!" },
  { q: "세상에서 가장 야한 가수는?", a: "다비치", h: "다XX", r: "다비쳐서 다비치" },
  { q: "호주에서 파는 술은?", a: "호주", h: "X주", r: "술을 한자로 쓰면 주(酒-술주)" },
  { q: "사자가 숙제를 못하는 이유는?", a: "밀림의제왕", h: "XXX제왕", r: "숙제 미루기, 밀림의 제왕" },
  { q: "산타할아버지가 싫어하는 면은?", a: "울면", h: "X면", r: "울면안돼~ 울면안돼~" },
  { q: "사람들이 가장 싫어하는 거리는?", a: "걱정거리", h: "XX거리", r: "걱정거리가 생기면 누구나 싫어하죠" },
  { q: "모기가 먹는 알파벳은?", a: "P", h: "X (모기가 좋아하는?)", r: "모기는 우리 피(P)를 빨아먹죠" },
  { q: "세상에서 가장 맛있는 집은?", a: "닭똥집", h: "닭X집", r: "닭똥집 요리 좋아하시나요?" },
  { q: "법적으로 바가지 요금을 받아도 되는 사람을?", a: "바가지장수", h: "XXX장수", r: "바자기를 파니, 바가지 요금을 받아도 되겠죠?" },
  { q: "숫자 8을 세로로 나누면?", a: "숫자 3", h: "숫자 X", r: "8을 세로로 나누면 3이 나옵니다." },
  { q: "바바리맨이 축구를 못하는 이유는?", a: "노골적이라서", h: "XXX이라서", r: "NO골적이어서" },
  { q: "더울 때는 짧고 추울 때는 긴 것은?", a: "저녁", h: "X녁", r: "여름은 밤이 짧고 겨울은 밤이 길죠" },
  { q: "식인종이 공무원을 보고 뭐라고 할까?", a: "정부미", h: "XX미", r: "공직에 있는 밥이라 정부미" },
  { q: "[초인종이 없다]를 두 글자로?", a: "노벨", h: "X벨", r: "벨이 없다 - 노벨!" },
  { q: "가수 슈퍼주니어가 가지고 있는 바다는?", a: "동해", h: "X해", r: "슈퍼주니어의 바다 - 동해" },
  { q: "동물원에서 가장 비싼 동물은?", a: "백조", h: "X조", r: "이거 얼마에요? 백만,천만,억~ 백조!!" },
  { q: "[가장 강력한 즐거움]을 세글자로?", a: "최강희", h: "XX희", r: "최강의 즐거움(희, 憙) - 최강희!" },
  { q: "모자가 뭉쳐있으면?", a: "밀짚모자", h: "XX모자", r: "밀집해 있는 밀짚모자" },
  { q: "눈을 감아야 볼 수 있는 것은?", a: "꿈", h: "X (자면서 꾸는 것)", r: "꿈은 눈을 감고 잠이 들어야 볼 수 있습니다!" },
  { q: "[남자와 여자가 서있다]를 네글자로?", a: "선남선녀", h: "X남X녀", r: "서있는 남자/여자 - 선남선녀" },
  { q: "비가 1시간 동안 내리면?", a: "추적60분", h: "추적XXX", r: "추적추적 비가 60분동안" },
  { q: "가짜로 내리는 비는?", a: "사이비", h: "XX비", r: "가짜로 내리는 비는 사이비(비 우,雨)" },
  { q: "텔레토비 뽀와 헤어질 때 하는 말은?", a: "뽀빠이", h: "뽀XX", r: "뽀~ 빠이(bye)!" },
  { q: "모든 사람을 일으키는 숫자는?", a: "다섯", h: "다X", r: "다(모두) 섯(일어나라)!" },
  { q: "[아주 오래전에 건설된 다리]를 네 글자로?", a: "구닥다리", h: "XX다리", r: "구닥 다리(bridge)" },
  { q: "사람의 욕심을 한 글자로 표현하면?", a: "더", h: "X (X 주세요!)", r: "더~ 더~ 원하는게 사람의 욕심" },
  { q: "대변과 소변 중 먼저 나오는 것은?", a: "급한 것", h: "XX 것", r: "급한게 먼저 나오겠죠" },
  { q: "신문이 일하러 가면?", a: "일간신문", h: "XX신문", r: "일하러 간 신문" },
  { q: "아리 아리랑 쓰리쓰리의 엄마는?", a: "아라리", h: "아XX", r: "아라리가 났네~" },
  { q: "이미자 + 이미자는?", a: "사미자", h: "XX자", r: "이(2)미자 + 이(2)미자 = 사(4)미자" },
  { q: "매일 밤 우리가 덮는 불은?", a: "이불", h: "X불", r: "잠잘 때 꼭 덮는 것 - 이불!" },
  { q: "[양위에 탄 사람]을 세 글자로?", a: "양탄자", h: "XX자", r: "양(을) 탄 자(탄 사람) - 양탄자" },
  { q: "어부들이 가장 싫어하는 가수는?", a: "배철수", h: "X철수", r: "배를 철수 시키면 돈을 못 벌겠죠?" },
  { q: "앞으로 가나 뒤로 가나 똑같은 나라는?", a: "스위스", h: "스X스", r: "스위스는 거꾸로 해도 스위스" },
  { q: "달걀장수가 달걀을 팔고 받은 돈은?", a: "에그머니", h: "XX머니", r: "에그(egg)을 팔아서 번 머니(money) 에그머니(에구머니~)" },
  { q: "머리에 보약을 달고 다니는 것은?", a: "사슴", h: "사X", r: "머리에 녹용은 보약으로 쓰이죠~" },
  { q: "펭귄이 다니는 대학교는?", a: "빙하시대", h: "XXX대", r: "빙하시대(大)학교" },
  { q: "진짜 문제가 많은 곳은?", a: "시험지", h: "XX지", r: "시험지에는 문제가 가득~합니다" },
  { q: "청바지를 땅에 심으면?", a: "심은진", h: "심은X", r: "땅에 심은 진" },
  { q: "소 중에서 가장 깨끗한 소는?", a: "청소", h: "X소", r: "깨끗하고 말끔하게~ 청소!" },
  { q: "반성문이 두 개 있으면?", a: "성문", h: "X문", r: "반(0.5)성문 + 반(0.5)성문 = 성문" },
  { q: "책을 찢을 때나는 소리는?", a: "북북", h: "북X", r: "북북(BOOK)" },
  { q: "신부가 성당에서 하는 일은?", a: "미사일", h: "XX일", r: "미사 보는 일" },
  { q: "위로 올리면 하나가 되고, 내리면 둘이 되는 것은?", a: "지퍼", h: "X퍼", r: "지퍼는 위로 올리면 하나의 줄로, 내리면 두 개의 줄로 나누어 집니다." },
  { q: "오키나와 가면 살찌는 이유는?", a: "5끼나와서", h: "XX나와서", r: "5끼 먹으면 살찌죠" },
  { q: "바다와 육지 사이엔 뭐가 있을까?", a: "와", h: "X", r: "바다 와 육지" },
  { q: "바다에 살고 있는 사자는?", a: "바다사자", h: "XX사자", r: "바다에 살고 있는 사자라 바다사자?" },
  { q: "사람들이 가장 좋아하는 공은?", a: "성공", h: "X공", r: "성공만큼 좋은 것도 없죠" },
  { q: "눈이 매력적인 사람이 갖고 있는것은?", a: "눈부심", h: "눈XX", r: "눈에 대한 자부심, 눈부심" },
  { q: "금은 금인데 도둑고양이에게 잘 어울리는 금은?", a: "야금야금", h: "X금X금", r: "도둑 고양이가 야금야금 훔쳐가죠" },
  { q: "길 가다가 부딪히면?", a: "친자확인", h: "XX확인", r: "어깨를 친자를 확인해야죠" },
  { q: "가지 말라고 해도 계속 흘러가는 것은?", a: "시간", h: "X간", r: "아무리 소리쳐도 시간은 계속 흘러갑니다." },
  { q: "말인데 타지 못하는 말은?", a: "거짓말", h: "XX말", r: "조랑말은 탈 수 있어도 거짓말은 탈 수 없습니다." },
  { q: "할머니가 할아버지를 그리워하면?", a: "옹앓이", h: "X앓이", r: "옹 앓이" },
  { q: "이가 빠져도 계속 새로 나오는 연예인?", a: "이나영", h: "XX영", r: "이가 계속 나영" },
  { q: "반성문을 영어로 하면?", a: "글로벌", h: "XX벌", r: "글로 벌을 받는 반성문" },
  { q: "도둑에게 가장 어울리는 금은?", a: "슬금슬금", h: "X금X금", r: "도둑은 항상 슬금슬금 이동하죠" },
  { q: "시간은 마치 소변과도 같다를 다른 말로?", a: "쉬는시간", h: "XX시간", r: "쉬 = 시간" },
  { q: "물 없이도 할 수 있는 물놀이는?", a: "사물놀이", h: "XX놀이", r: "악기만 있다면 물 없이도 할 수 있는 사물놀이" },
  { q: "침대를 밀고 돌리면?", a: "배드민턴", h: "배드XX", r: "배드(bed)를 민(밀고) 턴(돌리다)" },
  { q: "[미소]의 반대말은?", a: "당기소", h: "XX소", r: "[밀다]의 반대말은 [당기다]" },
  { q: "가수 중 가장 잠을 많이 자는 사람은?", a: "이미자", h: "XX자", r: "이미~ 자고 있는 사람, 이미자!" },
  { q: "감전사한 사람의 유언은?", a: "먼저감전", h: "XX감전", r: "먼저감,전" },
  { q: "입으로 먹지 않고 귀로 먹는 것은?", a: "욕", h: "X (듣기 싫은것)", r: "너 오늘도 욕먹었니? 욕은 귀로 먹죠" },
  { q: "세상에서 가장 야한 음식재료는?", a: "버섯", h: "버X", r: "버서, 버섯" },
  { q: "먹을수록 추워지는 음식은?", a: "추어탕", h: "XX탕", r: "먹다보면 추어~ 추어(추워~)" },
  { q: "세상에서 가장 뜨거운 과일은?", a: "천도복숭아", h: "XX복숭아", r: "천도(1000℃)나 되는 복숭아!" },
  { q: "위에서 아래로 자라나는 것은?", a: "고드름", h: "XX름", r: "고드름은 하늘에서 땅 방향으로 자라납니다" },
  { q: "이 세상에서 가장 큰 것은?", a: "어금니", h: "XX니", r: "이 중에서 가장 큰 이는 어금니" },
  { q: "고기만두가 고기를 보고 하는 말은?", a: "내안에 너 있다", h: "내안에 XXX", r: "고기만두 안에는 고기가 들어 있습니다" },
  { q: "담배를 주운 피카츄에게 통키가 한 말은?", a: "피구왕", h: "피XX", r: "피구와~~" },
  { q: "할아버지가 가장 좋아 하는 돈은?", a: "할머니", h: "X머니", r: "할아버지는 할머니(money)를 좋아하죠!" },
  { q: "공인데 찰 수 없는 공은?", a: "허공", h: "X공", r: "허공에 발길질을 해도 찰 수 없습니다." },
  { q: "돌고래는 영어로 돌핀, 그럼 고래는 영어로?", a: "핀", h: "X (돌을 빼면?)", r: "돌고래 = 돌핀, 고래는 돌을 빼서 '핀'" },
  { q: "학생들이 가장 싫어하는 열매는?", a: "야자", h: "X자", r: "야자(야간자율학습)을 좋아하는 학생이 있을까요?" },
  { q: "높은 곳에서 아기를 낳으면?", a: "하이애나", h: "하이XX", r: "높은곳(high)에서 애나" },
  { q: "가수 f(x)의 반대 말은?", a: "역함수", h: "X함수", r: "f(x), 함수의 반대는 역함수" },
  { q: "하늘에서 콩이 2개 떨어지면?", a: "스카이콩콩", h: "스카이XX", r: "하늘에서(SKY) 콩이 콩~ 콩~" },
  { q: "사람이 항상 가지고 다니는 흉기는?", a: "머리칼", h: "XX칼", r: "머리카락, 머리칼" },
  { q: "딸기가 도망가면?", a: "딸기쩀", h: "딸기X", r: "딸기가 째고 도망감" },
  { q: "우리가 몸에 지니고 있는 톱은?", a: "손톱", h: "X톱", r: "우리 몸에 지니고 있는 톱 - 손톱" },
  { q: "담배가 목장에 간 이유는?", a: "말보로", h: "XX로", r: "말을 보기 위해서" },
  { q: "고양이를 싫어하는 동물은?", a: "미어캣", h: "XX캣", r: "미워 캣!" },
  { q: "동생과 형이 싸우는데, 동생 편만 드는 것은?", a: "형편 없는 세상", h: "XX 없는 세상", r: "형의 편이 없는 세상" },
  { q: "오뎅을 다섯 글자로 하면?", a: "뎅뎅뎅뎅뎅", h: "XXX뎅뎅", r: "[뎅]이 5개 - 오뎅" },
  { q: "고등학생이 싫어하는 나무는?", a: "야자나무", h: "XX나무", r: "고등학생은 야간자율학습을 싫어해요" },
  { q: "아침식사로 먹을 수 없는 것은?", a: "점심과 저녁식사", h: "XX과 저녁XX", r: "아침에 점심/저녁 식사 하는 분 있나요" },
  { q: "안에서 천국을 제공해주는 항은?", a: "인천국제공항", h: "XXX제공항", r: "인(in) 천국 제공항" },
  { q: "[붉은 길에 떨어진 동전]을 네글자로", a: "홍길동전", h: "XX동전", r: "홍(紅) 길에 떨어진 동전이라 홍길동전" },
  { q: "정사각형 동생의 이름은?", a: "정사각", h: "정XX", r: "정사각이의 형" },
  { q: "수도를 틀어도 석유가 나오는 역은?", a: "중동역", h: "XX역", r: "석유가 넘치는 중동~ 그래서 중동역?" },
  { q: "육지에 사는 고래는?", a: "술고래", h: "X고래", r: "우리 아빠도~ 삼촌도 술고래!" },
  { q: "보드가 웃으면 뭐라고 할까?", a: "킥보드", h: "X보드", r: "킥~킥 하고 웃어서 킥보드" },
  { q: "간장인데, 사람이 먹을 수 없는 간장은?", a: "애간장", h: "X간장", r: "애간장은 먹을 수 없죠" },
  { q: "소나무가 삐지면?", a: "칫솔", h: "X솔", r: "흥! 칫! 솔(소나무)" },
  { q: "들어갈 때는 잔뜩 짊어지고, 나올 때는 아무것도 없는 것은?", a: "숟가락", h: "XX락", r: "입에 들어갈 때는 밥을 짊어지지만, 나올 때는 아무것도 없네요~" },
  { q: "[돌이 모여사는 마을]을 두 글자로?", a: "석촌", h: "X촌", r: "돌이 모여사는 마을이라 석촌!" },
  { q: "감인데 먹을 수 없는 감은?", a: "영감", h: "X감", r: "곶감은 먹어도 영감은 먹을 수 없습니다." },
  { q: "사형 집행하는 게는?", a: "목베게", h: "XX게", r: "목을 베게" },
  { q: "세상에사 가장 더운 남자는?", a: "덥군", h: "X군", r: "김군~많이 덥나? 더운 남자 - 덥군" },
  { q: "세상에서 가장 잔인한 비빔밥은?", a: "산채비빔밥", h: "XX비빔밥", r: "산채로 비벼먹어 잔인할 것 같은 산채비빔밥" },
  { q: "사오정이 다니는 고등학교는?", a: "뭐라고", h: "XX고", r: "귀가 안들리는 사오정 - 뭐라고(高)등학교" },
  { q: "장사하는 사람들이 가장 싫어하는 경기는?", a: "불경기", h: "X경기", r: "불경기에는 장사가 잘 되지 않죠" },
  { q: "저팔계가 정육점에서 하는 말?", a: "저팔게요", h: "저팔XX", r: "저 팔게요?" },
  { q: "지진 났을 때 절대 부르면 안 되는 노래는?", a: "동요", h: "X요", r: "지진에 동요하면 안되죠" },
  { q: "초콜릿이 달다를 4글자로 하면", a: "가나다라", h: "XX다라", r: "가나초콜렛 달아" },
  { q: "영광굴비가 상하면?", a: "좌절굴비", h: "XX굴비", r: "영광의 반대는 좌절" },
  { q: "개리가 미국에 있는 온천에 가면?", a: "스파게리", h: "XX게리", r: "스파에 간 게리" },
  { q: "추장보다 높은 것은?", a: "고추장", h: "X추장", r: "추장보다 높은 고(高)추장" },
  { q: "아기공룡 둘리가 다니는 고등학교는?", a: "빙하타고", h: "XXX고", r: "빙하타고(高) ~ 내려와~" },
  { q: "건망증이 심한 사람들이 올라가는 산은?", a: "아차산", h: "XX산", r: "아차! 잊어버렸네~ 아차산" },
  { q: "[타자]의 반대말은?", a: "안타", h: "X타", r: "저 차 타자~ 아니 난 안타!" },
  { q: "[아기가 신을 낳았다]를 네글자로?", a: "갓난아기", h: "XX아기", r: "신(god)을 낳은 아기 - 갓 난 아기" },
  { q: "소랑 개랑 부딪히면?", a: "소개팅", h: "XX팅", r: "소와 개가 팅~ 하고 부딪혀서" },
  { q: "이에 스시가 끼면?", a: "esc", h: "Xsc", r: "이에 스시" },
  { q: "나의 울음으로 시작해 남의 울음으로 끝나는 것은?", a: "인생", h: "X생", r: "태어날 때 아기 울음에서 죽을 때 남들의 슬플 울음으로~" },
  { q: "물고기의 반대말은?", a: "불고기", h: "X고기", r: "물의 반대말은 불" },
  { q: "성적이 나와도 말을 못 하는 이유?", a: "내성적이라서", h: "XXX이라서", r: "내 성적 이라서" },
  { q: "[소가 웃는 소리]를 세글자로 하면?", a: "우하하", h: "우XX", r: "우(소, 牛)가 하하 웃어서 우하하" },
  { q: "이탈리아의 기후는?", a: "습하겠디", h: "XX겠디", r: "스파게티가 유명하니 습하겠디" },
  { q: "가수 비를 누른 가수는?", a: "클릭비", h: "XX비", r: "비를 클릭(click) 했으니 클릭비" },
  { q: "늘 나와 함께 있지만, 그늘에 가면 없어지는 것은?", a: "그림자", h: "XX자", r: "그림자는 그늘에 가면 사라집니다." },
  { q: "소녀가 강가로 여행을 가면?", a: "걸리버여행기", h: "XXX여행기", r: "걸(GIRL) 리버(RIVER) 여행기" },
  { q: "원더걸스 선예의 반대말은?", a: "앉은예", h: "XX예", r: "일어서다의 반대는 앉다" },
  { q: "뉴스에 잘못 나온 음악 연주는?", a: "오보에연주", h: "XX에연주", r: "잘못된 보도(오보)에 연주" },
  { q: "제시카 알바가 더 열심히 일하면 어떻게 될까?", a: "제시카 정규직", h: "제시카 XX직", r: "알바가 일을 열심히 하면 정규직이 되겠죠?" },
  { q: "송해가 알을 낳으면?", a: "아리송해", h: "XX송해", r: "알이(아리~) 송해라서 아리송해" },
  { q: "어린 시절 비는?", a: "아이비", h: "아이X", r: "어린 아이 비" },
  { q: "[서울이 산이 없다]를 다섯 글자로?", a: "서울산업대", h: "서울X업대", r: "서울에 산이 없데~ 서울산업대" },
  { q: "늘 고장나는 배를 세글자로?", a: "불량배", h: "XX배", r: "배가 불량이라 불량배?" },
  { q: "병 음료 옆에 따라다니는 개는?", a: "병따개", h: "XX개", r: "콜라/사이다 병을 마실 때 꼭 필요한 병따개" },
  { q: "원숭이를 불에 구우면?", a: "구운몽", h: "XX몽", r: "불에 구운~ 원숭이(monkey)" },
  { q: "[오이가 무를 때렸다]를 네 글자로?", a: "오이무침", h: "오이XX", r: "오이가 무를 침(때림)" },
  { q: "[오리가 다섯마리 있다]를 두 글자로?", a: "오덕", h: "X덕", r: "다섯 마리의 덕(duck) - 오덕" },
  { q: "[시인 한 명]을 세글자로?", a: "원시인", h: "X시인", r: "시인이 한 명이니까 - 원시인!" },
  { q: "사각형 모양인데 원이라고 하는 것은?", a: "천원과 만원", h: "X원과 X원", r: "만원짜리는 사각형인데~ 만'원'이라고 부르네요" },
  { q: "항상 가운데 있는 동네는?", a: "중앙동", h: "XX동", r: "늘 중앙에 있어 중앙동!" },
  { q: "입이 S자로 되어있으면?", a: "EBS", h: "XBS", r: "입이 에스" },
  { q: "프랑스인이 빨래를 널고 하는 말은?", a: "마르세유", h: "XX세유", r: "빨래야 말라라, 마르세유" },
  { q: "효심 깊은 아들이 모여사는 동네는?", a: "효자동", h: "XX동", r: "효자들이 모여사는 동네 - 효자동" },
  { q: "꽃중에 가장 뜨거운 꽃은?", a: "불꽃", h: "X꽃", r: "이름만 들어도 뜨거운 불꽃!" },
  { q: "인사하면서 웃으면?", a: "하이킥", h: "XX킥", r: "인사(Hi)하면서 킥킥킥" },
  { q: "[눈, 구름, 칼]을 세글자로?", a: "설운도", h: "XX도", r: "설(雪) 운(雲) 도(刀)" },
  { q: "대통령선거의 반대말은?", a: "대통령 앉은거", h: "대통령 XX거", r: "일어서다의 반대는 앉다" },
  { q: "칼이 정색하면은?", a: "검정색", h: "XX색", r: "검이 정색" },
  { q: "노인들이 가장 좋아하는 폭포는?", a: "나이아가라", h: "나이아XX", r: "나이아 가라(가버려라!)하면 젊어질까요?" },
  { q: "인도는 항상 몇시 일까?", a: "인도 네시야", h: "인도 X시야", r: "인도네시아 - 인도는 4시야~" },
  { q: "야구모자를 때리면?", a: "야구아빠가달려옴", h: "XXXX가달려옴", r: "야구 모자를 때리면 야구 아빠가 화가나 달려오겠죠" },
  { q: "[양념게장]의 반대말은?", a: "후라이드게장", h: "XXXX게장", r: "양념치킨의 반대는 후라이드치킨, 그럼 양념게장의 반대는?" },
  { q: "참나무의 반대말은?", a: "뻥나무", h: "X나무", r: "참의 반대는 거짓(뻥)" },
  { q: "왕이 궁에 가기 싫어하는 말은?", a: "궁시렁", h: "궁XX", r: "궁시러 궁시렁" },
  { q: "박사와 학사는 밥을 많이 먹는다를 사자성어로?", a: "박학다식", h: "XX다식", r: "박(사)와 학(사)는 많은(다) 식사를 한다. 그래서 박학다식?" },
  { q: "코끼리 두 마리가 싸우다가 둘 다 코가 부러진 상황을 4글자로 하면?", a: "끼리끼리", h: "X리X리", r: "코를 빼면 끼리끼리" },
  { q: "매일 똑 같은 곳을 돌기만 하는 것은?", a: "시계바늘", h: "XX바늘", r: "시계의 시/분/초는 매일 같은 원을 그리며 돌죠~" },
  { q: "호주에서 쓰는 돈을 네글자로?", a: "호주머니", h: "XX머니", r: "호주의 머니(money)라 호주머니" },
  { q: "한 몸에 심장이 두 개인 사람은?", a: "임산부", h: "XX부", r: "엄마의 심장 + 아이의 심장 = 심장이 두 개" },
  { q: "김소월이 수능 수리 가형만 보는 이유는?", a: "나보기가역겨워", h: "나보기가XXX", r: "나보기가 역겨워 가실때에는" },
  { q: "식인종이 신입생을 보고 뭐라고 할까?", a: "햅쌀", h: "X쌀", r: "새로운 밥이라서 햅쌀" },
  { q: "치아에 인공지능 기능이 있으면?", a: "이가시리다", h: "이가XXX", r: "이가 Siri 죠" },
  { q: "예의바른 사람들만 사는 동네는?", a: "인사동", h: "XX동", r: "인사를 잘하는 예의바른 사람들의 마을 - 인사동" },
  { q: "우리나라 꼬마도 알지만, 외국인은 어른도 모르는 것은?", a: "한국어", h: "XX어", r: "한국 꼬마는 한국어를 알지만, 외국인은 모르겠죠?" },
  { q: "해와 달이 싸우면 누가 이길까?", a: "달", h: "X", r: "달이 뜨면 해가 지니까" },
  { q: "바람이 불면 흔들리지 않고, 바람이 안불면 흔들리는 것은?", a: "부채", h: "X채", r: "부채는 바람이 안불면 흔들죠~" },
  { q: "들어갈 때는 뻣뻣하고 나올 때는 물렁한 것은?", a: "껌", h: "X (씹는 거에요!)", r: "입에 들어가기 전에는 뻣뻣, 씹으면 물렁 - 껌" },
  { q: "사람들이 가장 좋아하는 벌은?", a: "재벌", h: "X벌", r: "누구나 재벌이 되고 싶죠~" },
  { q: "알파벳에서 'B' 다음은 무엇일까요?", a: "E", h: "X (비~이)", r: "[alphabet]에서 b 다음 글자는 e!" },
  { q: "비를 막아주는 산은?", a: "우산", h: "X산", r: "우산은 비를 막아줍니다" },
  { q: "모든 게 싫다고 하는 병은?", a: "실어증", h: "XX증", r: "싫어 싫어~ 실어증" },
  { q: "세상에서 가장 무서운 전화기는?", a: "무선전화기", h: "XX전화기", r: "무서운(무선~) 전화기" },
  { q: "[여러 명의 소년들이 낚시를 한다]를 다섯 글자로?", a: "보이스피싱", h: "보XX피X", r: "보이(boy)들이 모여서 낚시(fishing)을 하니 '보이스피싱'?" },
  { q: "[빗자루만 들고 가만히 서있는 여자]를 여섯글자로?", a: "쓸데없는 여자", h: "XX없는 여자", r: "쓸 곳이 없어서 가만히 서있겠죠?" },
  { q: "창문 100개 중 2개가 깨지면?", a: "윈도우98", h: "윈도우XX", r: "창문(윈도우) 100 - 2 = 98" },
  { q: "맞아도 죽지 않는 총은?", a: "눈총", h: "X총", r: "눈총을 맞아도 죽지 않습니다." },
  { q: "고개 숙이고 눈물 흘리는 것은?", a: "수도꼭지", h: "XX꼭지", r: "구부려져 물이 나오는 곳 - 수도꼭지" },
  { q: "말을 못 하는 시골 동네를 두 글자로 줄이면?", a: "읍읍", h: "X읍", r: "시골 읍면리, 읍읍" },
  { q: "머리가 3개인 사람을 세글자로?", a: "헤드셋", h: "XX셋", r: "머리(헤드)가 3개라 셋" },
  { q: "구두쇠가 가장 좋아하는 색은?", a: "인색", h: "X색", r: "돈쓰는데 인색한 사람 - 구두쇠" },
  { q: "세상에서 가장 건너기 어려운 고개는?", a: "보릿고개", h: "XX고개", r: "아무리 높은 고개도 보릿고개 보다는 쉽겠죠" },
  { q: "만 원과 천 원이 길에 있으면 무엇을 먼저 주워야 할까?", a: "둘 다 줍는다", h: "X X 줍는다", r: "하나 만 주울필요 있나요? 둘 다 가지면 더 좋죠~" },
  { q: "문과생이 수학 문제를 풀다 군대 간 이유는?", a: "대입을거꾸로함", h: "XXX거꾸로함", r: "대입을 거꾸로 해서" },
  { q: "머리는 영어로 'head' 그럼 가르마는 영어로?", a: "head line", h: "head XXXX", r: "머리를 가르는 선이니까 헤드라인!" },
  { q: "새와 말이 같이 있는 것을 세 글자로?", a: "조랑말", h: "XX말", r: "조(鳥) 랑 말(말 마,馬)" },
  { q: "누룽지를 영어로 표현하면?", a: "bobby brown", h: "bobby XXXXX", r: "노릇노릇~ 누룽지는 밥이 브라운 색이죠~ 밥이 브라운!" },
  { q: "폭우가 심하게 쏟아질 때 꼭 필요한 사람은?", a: "비서", h: "비X", r: "비야 그만 서, 비서" },
  { q: "탈모가 시작되면 생기는 매력은?", a: "헤어나올 수 없는 매력", h: "헤어XXXXXXX", r: "헤어(hair)나올수없는매력" },
  { q: "고슴도치가 동굴에 들어가 거품을 내며 목욕하는 것은?", a: "양치질", h: "양X질", r: "고슴도치 같은 칫솔이 입속(동굴)에서 칫솔질을 하는 것" },
  { q: "핀인데 머리에 꽂을 수 없는 핀은?", a: "필리핀", h: "XX핀", r: "필리핀은 너무 커서 머리에 꽂을 수 없습니다." },
  { q: "가운데 큰 구멍이 있지만 물에 가라앉지 않는 것은?", a: "튜브", h: "X브", r: "튜브는 물에 가라앉지 않습니다!" },
  { q: "나무가 다섯 개 있으면?", a: "트리오", h: "XX오", r: "나무(tree) 5" },
  { q: "[살아있는 토끼]를 세 글자로?", a: "산토끼", h: "X토끼", r: "살아있는 토끼 - 산토끼!" },
  { q: "식인종이 회사원을 보고 뭐라고 할까?", a: "일반미", h: "XX미", r: "일반적인 밥이라 일반미" },
  { q: "만나도 서로 인사하지 않는 것은?", a: "하이패스", h: "XX패스", r: "인사(Hi)를 패스~" },
  { q: "비가 와서 짜증 날 때 내는 소리는?", a: "BYC", h: "BXX", r: "비 와 이씨" },
  { q: "램은 중고상품이 2기가짜리밖에 없는 이유", a: "4기가애매하고 8기가애매해서", h: "XXX애매하고XXX애매해서", r: "중고나라에서 사거나 팔기가 애매해서" },
  { q: "세상에서 가장 예의 바른 경찰은?", a: "사와디캅", h: "XXX캅", r: "태국 인사말 사와디캅" },
  { q: "재수 없는 게 좋은 사람은?", a: "고삼수험생", h: "XXX험생", r: "고삼 수험생은 재수가 싫어해요" },
  { q: "세상에서 가장 오래된 치킨은?", a: "구닥다리", h: "XX다리", r: "오래된 구 닥다리" },
  { q: "[무가 눈물을 흘리다]를 세 글자로?", a: "무뚝뚝", h: "X뚝뚝", r: "무가 눈물을 뚝뚝~" },
  { q: "용궁 음악회에 토끼를 초대한 이유는?", a: "간주는토끼가", h: "XXX토끼가", r: "간주는 토끼가 해야 해서" },
  { q: "쿵 씨의 딸 이름?", a: "샤바라", h: "XX라", r: "쿵 딸이 샤바라" },
  { q: "불과 가장 친한 비는?", a: "성냥개비", h: "XXX비", r: "불을 불러오는 성냥개비" },
  { q: "로봇 형사 '가제트'의 성은?", a: "마징", h: "마X", r: "성을 합치니, 마징가제트!" },
  { q: "피자가 웃으면?", a: "피자헛", h: "피자X", r: "피자가 헛헛헛" },
  { q: "세상에서 가장 지루한 중학교는?", a: "로딩중", h: "XX중", r: "언제까지 기다려야 하나~ 지겨운 로딩 중" },
  { q: "앞으로 나가면 지고, 뒤로 물러서면 이기는 것은?", a: "줄다리기", h: "X다리기", r: "줄다리기는 뒤로 물러설수록 우리팀이 승리할 수 있습니다." },
  { q: "세상에서 가장 야한 닭은?", a: "홀닭", h: "X닭", r: "홀닭 벗으면 야하죠" },
  { q: "비 와 햇볕을 막을 수 있는 선율은?", a: "파라솔", h: "XX솔", r: "도레미파솔라시도~ 파라솔~" },
  { q: "쓰기는 분명히 썼는데 읽을 수 없는 것은?", a: "모자", h: "모X", r: "모자는 쓰는 것만 가능합니다" },
  { q: "겉은 보름달이고 속은 반달인 것은?", a: "귤", h: "X (과일입니다!)", r: "겉은 둥글고(보름달) 속은 반달 모양이죠~" },
  { q: "우리나라 아이들이 가장 좋아하는 기름은?", a: "5월5일", h: "5월XX", r: "어린이날은 5월 5일(oil)" },
  { q: "올라 갈 수는 없고 내려가기만 하는 것은?", a: "폭포", h: "X포", r: "폭포는 늘 위에서 아래로 떨어지죠~" },
  { q: "[형이 용을 줬다]를 세글자로?", a: "용준형", h: "XX형", r: "우리 형이 용줬어~ 용을 준 형 - 용준형" },
  { q: "선거 운동의 반대말은?", a: "앉은거 운동", h: "XX거 운동", r: "일어서다의 반대는 앉다~" },
  { q: "[곰돌이 푸와 고양이]를 두 글자로?", a: "푸켓", h: "푸X", r: "푸와 고양이(켓) - 푸켓" },
  { q: "과거 때문에 성공한 사람은?", a: "암행어사", h: "XX어사", r: "과거시험을 통과하면 암행어사가 될 수 있습니다!" },
  { q: "춤 중에서 가장 멋없는 춤은?", a: "엉거주춤", h: "XX주춤", r: "엉거주춤은 멋이 었죠" },
  { q: "스님을 때리는 사람은?", a: "중매쟁이", h: "XX쟁이", r: "중에게 매질을 하는 매쟁이" },
  { q: "사과가 굴러가다가 돌 때문에 파였어를 4글자로?", a: "파인애플", h: "XX애플", r: "굴러가다가 파인 사과(apple)" },
  { q: "부부가 산에 올라갔다를 한 글자로 하면?", a: "쁏", h: "X", r: "ㅅ위에 부부, 쁏" },
  { q: "내 것인데 남들이 더 많이 쓰는 것은?", a: "이름", h: "X름", r: "나보다 남들이 내 이름을 더 자주 부르죠~" },
  { q: "방석인데 편하게 앉을 수 없는 방석은?", a: "가시방석", h: "XX방석", r: "가시방석처럼 불편한 자리가 또 있을까요?" },
];
// ===== 캐치마인드 퀴즈 DB (이미지 기반) =====

var CATCHMIND_BASE_URL = "https://pkeony.github.io/singbot-images";

var CATCHMIND_DB = [
  { id: 1, answer: "북극곰" },
  { id: 2, answer: "황제펭귄" },
  { id: 3, answer: "붕어빵" },
  { id: 4, answer: "불고기" },
  { id: 5, answer: "새총" },
  { id: 6, answer: "말씨름" },
  { id: 7, answer: "만두피" },
  { id: 8, answer: "물장구" },
  { id: 9, answer: "돌고래" },
  { id: 10, answer: "다이소" },
  { id: 11, answer: "달팽이" },
  { id: 12, answer: "백호" },
  { id: 13, answer: "북유럽" },
  { id: 14, answer: "돈다발" },
  { id: 15, answer: "방탄유리" },
  { id: 16, answer: "블루투스" },
  { id: 17, answer: "삼계탕" },
  { id: 18, answer: "공개투표" },
  { id: 19, answer: "사냥개" },
  { id: 20, answer: "파노라마" },
  { id: 21, answer: "대머리독수리" },
  { id: 22, answer: "벌침" },
  { id: 23, answer: "배신" },
  { id: 24, answer: "공책" },
  { id: 25, answer: "독감" },
  { id: 26, answer: "발레" },
  { id: 27, answer: "구명보트" },
  { id: 28, answer: "세탁소" },
  { id: 29, answer: "반창고" },
  { id: 30, answer: "포크레인" },
  { id: 31, answer: "호랑나비" },
  { id: 32, answer: "페이스북" },
  { id: 33, answer: "별자리" },
  { id: 34, answer: "스타일" },
  { id: 35, answer: "초등학교" },
  { id: 36, answer: "키보드" },
  { id: 37, answer: "콩쥐팥쥐" },
  { id: 38, answer: "캡사이신" },
  { id: 39, answer: "히든카드" },
  { id: 40, answer: "옆구리" },
  { id: 41, answer: "황소개구리" },
  { id: 42, answer: "해파리" },
  { id: 43, answer: "소매" },
  { id: 44, answer: "배탈" },
  { id: 45, answer: "가오리" },
  { id: 46, answer: "백댄서" },
  { id: 47, answer: "줄다리기" },
  { id: 48, answer: "손난로" },
  { id: 49, answer: "전갈자리" },
  { id: 50, answer: "개나리" },
  { id: 51, answer: "수제비" },
  { id: 52, answer: "조각배" },
  { id: 53, answer: "광대뼈" },
  { id: 54, answer: "스타벅스" },
  { id: 55, answer: "도시락" },
  { id: 56, answer: "운동화" },
  { id: 57, answer: "소용돌이" },
  { id: 58, answer: "해리포터" },
  { id: 59, answer: "성탄절" },
  { id: 60, answer: "기억상실" },
  { id: 61, answer: "닭꼬치" },
  { id: 62, answer: "양말" },
  { id: 63, answer: "귀신" },
  { id: 64, answer: "다이아몬드" },
  { id: 65, answer: "피구" },
  { id: 66, answer: "중학교" },
  { id: 67, answer: "거북선" },
  { id: 68, answer: "약속" },
  { id: 69, answer: "은행나무" },
  { id: 70, answer: "턱걸이" },
  { id: 71, answer: "종이컵" },
  { id: 72, answer: "소독약" },
  { id: 73, answer: "제비뽑기" },
  { id: 74, answer: "코인노래방" },
  { id: 75, answer: "불량식품" },
  { id: 76, answer: "검정색" },
  { id: 77, answer: "초코우유" },
  { id: 78, answer: "말뚝" },
  { id: 79, answer: "하모니카" },
  { id: 80, answer: "콩나물" },
  { id: 81, answer: "불곰" },
  { id: 82, answer: "오리너구리" },
  { id: 83, answer: "손톱" },
  { id: 84, answer: "말싸움" },
  { id: 85, answer: "무더위" },
  { id: 86, answer: "열매" },
  { id: 87, answer: "서양" },
  { id: 88, answer: "절약" },
  { id: 89, answer: "고추잠자리" },
  { id: 90, answer: "닭똥집" },
  { id: 91, answer: "아이돌" },
  { id: 92, answer: "쫄면" },
  { id: 93, answer: "보조개" },
  { id: 94, answer: "소송" },
  { id: 95, answer: "신세계" },
  { id: 96, answer: "사계절" },
  { id: 97, answer: "골든벨" },
  { id: 98, answer: "카페인" },
  { id: 99, answer: "편의점" },
  { id: 100, answer: "홍길동전" },
  { id: 101, answer: "수면양말" },
  { id: 102, answer: "말벌" },
  { id: 103, answer: "눈병" },
  { id: 104, answer: "용왕" },
  { id: 105, answer: "경찰서" },
  { id: 106, answer: "고추장" },
  { id: 107, answer: "반성문" },
  { id: 108, answer: "비서" },
  { id: 109, answer: "반말" },
  { id: 110, answer: "도서관" },
  { id: 111, answer: "나이" },
  { id: 112, answer: "녹차" },
  { id: 113, answer: "변비" },
  { id: 114, answer: "파스타" },
  { id: 115, answer: "연골" },
  { id: 116, answer: "쥐포" },
  { id: 117, answer: "짜임새" },
  { id: 118, answer: "집중력" },
  { id: 119, answer: "나무늘보" },
  { id: 120, answer: "신사임당" },
  { id: 121, answer: "꽃게" },
  { id: 122, answer: "불개미" },
  { id: 123, answer: "개학" },
  { id: 124, answer: "밤낚시" },
  { id: 125, answer: "해시계" },
  { id: 126, answer: "금발" },
  { id: 127, answer: "약국" },
  { id: 128, answer: "물렁뼈" },
  { id: 129, answer: "책꽂이" },
  { id: 130, answer: "월드컵" },
  { id: 131, answer: "잔털" },
  { id: 132, answer: "핀셋" },
  { id: 133, answer: "울산" },
  { id: 134, answer: "아이유" },
  { id: 135, answer: "감자탕" },
  { id: 136, answer: "애국자" },
  { id: 137, answer: "알콩달콩" },
  { id: 138, answer: "집들이" },
  { id: 139, answer: "두부한모" },
  { id: 140, answer: "차돌박이" },
  { id: 141, answer: "컵케익" },
  { id: 142, answer: "성공" },
  { id: 143, answer: "핫도그" },
  { id: 144, answer: "불사신" },
  { id: 145, answer: "물구나무" },
  { id: 146, answer: "회전문" },
  { id: 147, answer: "창던지기" },
  { id: 148, answer: "푸들" },
  { id: 149, answer: "헤드셋" },
  { id: 150, answer: "톱니바퀴" },
  { id: 151, answer: "지하철" },
  { id: 152, answer: "골절" },
  { id: 153, answer: "수학여행" },
  { id: 154, answer: "속셈" },
  { id: 155, answer: "매운탕" },
  { id: 156, answer: "미더덕" },
  { id: 157, answer: "두개골" },
  { id: 158, answer: "카놀라유" },
  { id: 159, answer: "우왕좌왕" },
  { id: 160, answer: "불가사리" },
  { id: 161, answer: "가위바위보" },
  { id: 162, answer: "먹이사슬" },
  { id: 163, answer: "강아지풀" },
  { id: 164, answer: "새해" },
  { id: 165, answer: "벌떼" },
  { id: 166, answer: "무술" },
  { id: 167, answer: "눈물" },
  { id: 168, answer: "문서" },
  { id: 169, answer: "땅강아지" },
  { id: 170, answer: "공통점" },
  { id: 171, answer: "팬미팅" },
  { id: 172, answer: "홍콩" },
  { id: 173, answer: "동물원" },
  { id: 174, answer: "기억력" },
  { id: 175, answer: "등나무" },
  { id: 176, answer: "공상과학" },
  { id: 177, answer: "팔로워" },
  { id: 178, answer: "포도주" },
  { id: 179, answer: "우편함" },
  { id: 180, answer: "원자폭탄" },
  { id: 181, answer: "불똥" },
  { id: 182, answer: "세발낙지" },
  { id: 183, answer: "왕개미" },
  { id: 184, answer: "문자" },
  { id: 185, answer: "야구" },
  { id: 186, answer: "바다코끼리" },
  { id: 187, answer: "연승" },
  { id: 188, answer: "키위" },
  { id: 189, answer: "먹구름" },
  { id: 190, answer: "매너" },
  { id: 191, answer: "묵은지" },
  { id: 192, answer: "충무김밥" },
  { id: 193, answer: "만우절" },
  { id: 194, answer: "지구본" },
  { id: 195, answer: "바지락" },
  { id: 196, answer: "에프킬라" },
  { id: 197, answer: "집안일" },
  { id: 198, answer: "두루치기" },
  { id: 199, answer: "인도네시아" },
  { id: 200, answer: "방울뱀" },
  { id: 201, answer: "바나나보트" },
  { id: 202, answer: "퇴학" },
  { id: 203, answer: "오해" },
  { id: 204, answer: "가발" },
  { id: 205, answer: "파마" },
  { id: 206, answer: "바이킹" },
  { id: 207, answer: "열매" },
  { id: 208, answer: "가로수" },
  { id: 209, answer: "벤치" },
  { id: 210, answer: "난장판" },
  { id: 211, answer: "바가지" },
  { id: 212, answer: "파프리카" },
  { id: 213, answer: "운동장" },
  { id: 214, answer: "발명품" },
  { id: 215, answer: "북쪽" },
  { id: 216, answer: "약혼자" },
  { id: 217, answer: "가지나물" },
  { id: 218, answer: "너도밤나무" },
  { id: 219, answer: "고소공포증" },
  { id: 220, answer: "나비넥타이" },
  { id: 221, answer: "산낙지" },
  { id: 222, answer: "무당벌레" },
  { id: 223, answer: "물총새" },
  { id: 224, answer: "검사" },
  { id: 225, answer: "아이크림" },
  { id: 226, answer: "개구리" },
  { id: 227, answer: "빙고" },
  { id: 228, answer: "도라지" },
  { id: 229, answer: "바이올린" },
  { id: 230, answer: "킹콩" },
  { id: 231, answer: "뱀파이어" },
  { id: 232, answer: "자몽" },
  { id: 233, answer: "딸기시럽" },
  { id: 234, answer: "설거지" },
  { id: 235, answer: "라이벌" },
  { id: 236, answer: "산소마스크" },
  { id: 237, answer: "총총걸음" },
  { id: 238, answer: "점프수트" },
  { id: 239, answer: "해운대" },
  { id: 240, answer: "주먹밥" },
  { id: 241, answer: "바다사자" },
  { id: 242, answer: "발야구" },
  { id: 243, answer: "이용" },
  { id: 244, answer: "무용" },
  { id: 245, answer: "소파" },
  { id: 246, answer: "코너킥" },
  { id: 247, answer: "물건" },
  { id: 248, answer: "무승부" },
  { id: 249, answer: "우주복" },
  { id: 250, answer: "갈비" },
  { id: 251, answer: "김밥천국" },
  { id: 252, answer: "산산조각" },
  { id: 253, answer: "식인종" },
  { id: 254, answer: "탄수화물" },
  { id: 255, answer: "가격표" },
  { id: 256, answer: "초인종" },
  { id: 257, answer: "다시다" },
  { id: 258, answer: "보일러" },
  { id: 259, answer: "소설가" },
  { id: 260, answer: "스타킹" },
  { id: 261, answer: "국위선양" },
  { id: 262, answer: "가로수" },
  { id: 263, answer: "해파리" },
  { id: 264, answer: "오리너구리" },
  { id: 265, answer: "화약" },
  { id: 266, answer: "물안개" },
  { id: 267, answer: "수리영역" },
  { id: 268, answer: "하모니카" },
  { id: 269, answer: "구명보트" },
  { id: 270, answer: "소양강" },
  { id: 271, answer: "공자" },
  { id: 272, answer: "고인돌" },
  { id: 273, answer: "가지나물" },
  { id: 274, answer: "고추장" },
  { id: 275, answer: "제육덮밥" },
  { id: 276, answer: "얼음낚시" },
  { id: 277, answer: "사랑싸움" },
  { id: 278, answer: "검정색" },
  { id: 279, answer: "감옥" },
  { id: 280, answer: "엄마" },
  { id: 281, answer: "할로윈" },
  { id: 282, answer: "으악새" },
  { id: 283, answer: "난장판" },
  { id: 284, answer: "흰수염고래" },
  { id: 285, answer: "갑오징어" },
  { id: 286, answer: "벌칙" },
  { id: 287, answer: "중국어" },
  { id: 288, answer: "고르곤졸라" },
  { id: 289, answer: "백사자" },
  { id: 290, answer: "반성문" },
  { id: 291, answer: "버슬" },
  { id: 292, answer: "과자" },
  { id: 293, answer: "돌직구" },
  { id: 294, answer: "장편소설" },
  { id: 295, answer: "공중변소" },
  { id: 296, answer: "이불" },
  { id: 297, answer: "하모니카" },
  { id: 298, answer: "울음바다" },
  { id: 299, answer: "말벌" },
  { id: 300, answer: "히트카드" },
  { id: 301, answer: "쥐약" },
  { id: 302, answer: "여우비" },
  { id: 303, answer: "신사임당" },
  { id: 304, answer: "쇠고집" },
  { id: 305, answer: "돈다발" },
  { id: 306, answer: "당근주스" },
  { id: 307, answer: "모래사장" },
  { id: 308, answer: "혼밥" },
  { id: 309, answer: "소매" },
  { id: 310, answer: "홈런" },
  { id: 311, answer: "구구콘" },
  { id: 312, answer: "가이드북" },
  { id: 313, answer: "거북선" },
  { id: 314, answer: "검사" },
  { id: 315, answer: "견인자동차" },
  { id: 316, answer: "게릴라" },
  { id: 317, answer: "코인노래방" },
  { id: 318, answer: "싸움닭" },
  { id: 319, answer: "배낭여행" },
  { id: 320, answer: "하이라이스" },
  { id: 321, answer: "스타일" },
  { id: 322, answer: "집중력" },
  { id: 323, answer: "향수" },
  { id: 324, answer: "핸드크림" },
  { id: 325, answer: "충무김밥" },
  { id: 326, answer: "나팔꽃" },
  { id: 327, answer: "가루약" },
  { id: 328, answer: "수도" },
  { id: 329, answer: "소화제" },
  { id: 330, answer: "수제비" },
  { id: 331, answer: "왕개미" },
  { id: 332, answer: "코뿔소" },
  { id: 333, answer: "은하수" },
  { id: 334, answer: "해리포터" },
  { id: 335, answer: "오렌지주스" },
  { id: 336, answer: "하프마라톤" },
  { id: 337, answer: "문구" },
  { id: 338, answer: "마법의성" },
  { id: 339, answer: "용의자" },
  { id: 340, answer: "김밥천국" },
  { id: 341, answer: "해수면" },
  { id: 342, answer: "메주" },
  { id: 343, answer: "그림자" },
  { id: 344, answer: "교육자" },
  { id: 345, answer: "숙취" },
  { id: 346, answer: "개회식" },
  { id: 347, answer: "말뚝" },
  { id: 348, answer: "스타벅스" },
  { id: 349, answer: "열매" },
  { id: 350, answer: "피카소" },
  { id: 351, answer: "소화제" },
  { id: 352, answer: "참치샌드위치" },
  { id: 353, answer: "코스모스" },
  { id: 354, answer: "배신" },
  { id: 355, answer: "가족" },
  { id: 356, answer: "검문소" },
  { id: 357, answer: "손톱" },
  { id: 358, answer: "공고문" },
  { id: 359, answer: "거북선" },
  { id: 360, answer: "각시탈" },
  { id: 361, answer: "하이네켄" },
  { id: 362, answer: "계피가루" },
  { id: 363, answer: "올빼미" },
  { id: 364, answer: "게이트볼" },
  { id: 365, answer: "감독" },
  { id: 366, answer: "소독약" },
  { id: 367, answer: "횡단보도" },
  { id: 368, answer: "개기일식" },
  { id: 369, answer: "강남스타일" },
  { id: 370, answer: "개나리" },
  { id: 371, answer: "개구리" },
  { id: 372, answer: "감옥" },
  { id: 373, answer: "엘프" },
  { id: 374, answer: "유자나무" },
  { id: 375, answer: "기린" },
  { id: 376, answer: "간호사" },
  { id: 377, answer: "노끈" },
  { id: 378, answer: "네트워크" },
  { id: 379, answer: "골든벨" },
  { id: 380, answer: "곡괭이" },
  { id: 381, answer: "홍길동전" },
  { id: 382, answer: "화이트데이" },
  { id: 383, answer: "허니문" },
  { id: 384, answer: "호랑나비" },
  { id: 385, answer: "연말" },
  { id: 386, answer: "미역" },
  { id: 387, answer: "가오리" },
  { id: 388, answer: "핸드크림" },
  { id: 389, answer: "해산물" },
  { id: 390, answer: "해산물" },
  { id: 391, answer: "향수" },
  { id: 392, answer: "거북선" },
  { id: 393, answer: "소화제" },
  { id: 394, answer: "세탁소" },
  { id: 395, answer: "회전초밥" },
  { id: 396, answer: "수면양말" },
  { id: 397, answer: "바위섬" },
  { id: 398, answer: "스톱워치" },
  { id: 399, answer: "로즈마리" },
  { id: 400, answer: "바탕" },
  { id: 401, answer: "톱니바퀴" },
  { id: 402, answer: "쥐며느리" },
  { id: 403, answer: "경찰서" },
  { id: 404, answer: "춤사위" },
  { id: 405, answer: "전화국" },
  { id: 406, answer: "전갈자리" },
  { id: 407, answer: "묘비" },
  { id: 408, answer: "양말" },
  { id: 409, answer: "뽀뽀" },
  { id: 410, answer: "고스톱" },
  { id: 411, answer: "해물누룽지탕" },
  { id: 412, answer: "어린왕자" },
  { id: 413, answer: "직녀" },
  { id: 414, answer: "다운로드" },
  { id: 415, answer: "옹변" },
  { id: 416, answer: "영계백숙" },
  { id: 417, answer: "초능력" },
  { id: 418, answer: "샌드위치" },
  { id: 419, answer: "방귀" },
  { id: 420, answer: "돈가스" },
  { id: 421, answer: "책꽂이" },
  { id: 422, answer: "삼국시대" },
  { id: 423, answer: "우편함" },
  { id: 424, answer: "보디빌딩" },
  { id: 425, answer: "사면초가" },
  { id: 426, answer: "제철소" },
  { id: 427, answer: "내성발톱" },
  { id: 428, answer: "지역감정" },
  { id: 429, answer: "우편함" },
  { id: 430, answer: "책꽂이" },
  { id: 431, answer: "제비뽑기" },
  { id: 432, answer: "월드컵" },
  { id: 433, answer: "고추잠자리" },
  { id: 434, answer: "차인표" },
  { id: 435, answer: "종이" },
  { id: 436, answer: "중고생" },
  { id: 437, answer: "돈나무" },
  { id: 438, answer: "성탄절" },
  { id: 439, answer: "피규어" },
  { id: 440, answer: "피서" },
  { id: 441, answer: "깨달음" },
  { id: 442, answer: "창세기" },
  { id: 443, answer: "중고차" },
  { id: 444, answer: "자녀" },
  { id: 445, answer: "소송" },
  { id: 446, answer: "발달" },
  { id: 447, answer: "방울꽃" },
  { id: 448, answer: "불사신" },
  { id: 449, answer: "불사조" },
  { id: 450, answer: "꽈배기" },
  { id: 451, answer: "딸기시럽" },
  { id: 452, answer: "홀로그램" },
  { id: 453, answer: "모스부호" },
  { id: 454, answer: "도원결의" },
  { id: 455, answer: "창의성" },
  { id: 456, answer: "비자금" },
  { id: 457, answer: "하울링" },
  { id: 458, answer: "하프마라톤" },
  { id: 459, answer: "입국" },
  { id: 460, answer: "소개팅" },
  { id: 461, answer: "비중" },
  { id: 462, answer: "우유식빵" },
  { id: 463, answer: "마우스패드" },
  { id: 464, answer: "에어백" },
  { id: 465, answer: "오이소박이" },
  { id: 466, answer: "창업자" },
  { id: 467, answer: "씨앗" },
  { id: 468, answer: "애교" },
  { id: 469, answer: "오리온" },
  { id: 470, answer: "선글라스" },
  { id: 471, answer: "티켓" },
  { id: 472, answer: "부채춤" },
  { id: 473, answer: "구사일생" },
  { id: 474, answer: "터치감" },
  { id: 475, answer: "야생화" },
  { id: 476, answer: "정학교" },
  { id: 477, answer: "소용돌이" },
  { id: 478, answer: "줄다리기" },
  { id: 479, answer: "붕장어" },
  { id: 480, answer: "스타벅스" },
  { id: 481, answer: "듣기평가" },
  { id: 482, answer: "경운기" },
  { id: 483, answer: "삼치" },
  { id: 484, answer: "설사" },
  { id: 485, answer: "동사" },
  { id: 486, answer: "종이컵" },
  { id: 487, answer: "적자생존" },
  { id: 488, answer: "잔치국수" },
  { id: 489, answer: "장구" },
  { id: 490, answer: "장수풍뎅이" },
  { id: 491, answer: "청룡" },
  { id: 492, answer: "민트초코" },
  { id: 493, answer: "전화위복" },
  { id: 494, answer: "인도양" },
  { id: 495, answer: "요리사" },
  { id: 496, answer: "원자폭탄" },
  { id: 497, answer: "블루칩" },
  { id: 498, answer: "반딧불이" },
  { id: 499, answer: "굴렁쇠" },
  { id: 500, answer: "동공" },
  { id: 501, answer: "총각김치" },
  { id: 502, answer: "우체국" },
  { id: 503, answer: "카풀" },
  { id: 504, answer: "블록체인" },
  { id: 505, answer: "초밥" },
  { id: 506, answer: "방화벽" },
  { id: 507, answer: "손수건" },
  { id: 508, answer: "참조물" },
  { id: 509, answer: "짚신벌레" },
  { id: 510, answer: "다슬기" },
  { id: 511, answer: "불가사의" },
  { id: 512, answer: "독창" },
  { id: 513, answer: "벌떡" },
  { id: 514, answer: "철학" },
  { id: 515, answer: "비트박스" },
  { id: 516, answer: "금발" },
  { id: 517, answer: "군사력" },
  { id: 518, answer: "아랫니" },
  { id: 519, answer: "우유식빵" },
  { id: 520, answer: "인쇄소" },
  { id: 521, answer: "황해" },
  { id: 522, answer: "기계체조" },
  { id: 523, answer: "아이디어" },
  { id: 524, answer: "은행나무" },
  { id: 525, answer: "데칼코마니" },
  { id: 526, answer: "만년설" },
  { id: 527, answer: "나홀로집에" },
  { id: 528, answer: "용병" },
  { id: 529, answer: "진로" },
  { id: 530, answer: "공중떡볶이" },
  { id: 531, answer: "벚꽃" },
  { id: 532, answer: "화장지" },
  { id: 533, answer: "호박" },
  { id: 534, answer: "비서" },
  { id: 535, answer: "조미료" },
  { id: 536, answer: "샬바" },
  { id: 537, answer: "가루약" },
  { id: 538, answer: "더둘이" },
  { id: 539, answer: "돈가스" },
  { id: 540, answer: "사이트" },
  { id: 541, answer: "보쌈" },
  { id: 542, answer: "용왕" },
  { id: 543, answer: "개똥벌레" },
  { id: 544, answer: "우주" },
  { id: 545, answer: "등겹질" },
  { id: 546, answer: "밀크커피" },
  { id: 547, answer: "원고지" },
  { id: 548, answer: "일간신문" },
  { id: 549, answer: "마이동풍" },
  { id: 550, answer: "가격표" },
  { id: 551, answer: "붉은악마" },
  { id: 552, answer: "등산모자" },
  { id: 553, answer: "대포" },
  { id: 554, answer: "컵케익" },
  { id: 555, answer: "사탕수수" },
  { id: 556, answer: "울버린" },
  { id: 557, answer: "드래곤" },
  { id: 558, answer: "개구리알" },
  { id: 559, answer: "개기름" },
  { id: 560, answer: "사막여우" },
  { id: 561, answer: "범위" },
  { id: 562, answer: "약혼자" },
  { id: 563, answer: "수포" },
  { id: 564, answer: "미어캣" },
  { id: 565, answer: "박물관" },
  { id: 566, answer: "용왕" },
  { id: 567, answer: "개똥벌레" },
  { id: 568, answer: "포장마차" },
  { id: 569, answer: "마음속" },
  { id: 570, answer: "병사" },
  { id: 571, answer: "모아이" },
  { id: 572, answer: "달마대사" },
  { id: 573, answer: "삼겹살" },
  { id: 574, answer: "소시지" },
  { id: 575, answer: "목침" },
  { id: 576, answer: "마피아" },
  { id: 577, answer: "만경창파" },
  { id: 578, answer: "죽부인" },
  { id: 579, answer: "청룡" },
  { id: 580, answer: "점토" },
  { id: 581, answer: "워터파크" },
  { id: 582, answer: "사신" },
  { id: 583, answer: "복사뼈" },
  { id: 584, answer: "일벌" },
  { id: 585, answer: "도끼" },
  { id: 586, answer: "청국장" },
  { id: 587, answer: "창던지기" },
  { id: 588, answer: "잉카" },
  { id: 589, answer: "손난로" },
  { id: 590, answer: "복주머니" },
  { id: 591, answer: "삐삐" },
  { id: 592, answer: "돈키호테" },
  { id: 593, answer: "포장도로" },
  { id: 594, answer: "새소리" },
  { id: 595, answer: "운동화" },
  { id: 596, answer: "저격수" },
  { id: 597, answer: "겨울잠" },
  { id: 598, answer: "피어싱" },
  { id: 599, answer: "머랭쿠키" },
  { id: 600, answer: "사표" },
  { id: 601, answer: "은방울꽃" },
  { id: 602, answer: "골절" },
  { id: 603, answer: "관우" },
  { id: 604, answer: "전기면도기" },
  { id: 605, answer: "매운탕" },
  { id: 606, answer: "샌드백" },
  { id: 607, answer: "우상" },
  { id: 608, answer: "집사" },
  { id: 609, answer: "인간문화재" },
  { id: 610, answer: "두통" },
  { id: 611, answer: "삼루수" },
  { id: 612, answer: "로봇청소기" },
  { id: 613, answer: "타짜" },
  { id: 614, answer: "지하철" },
  { id: 615, answer: "동료" },
  { id: 616, answer: "동아줄" },
  { id: 617, answer: "베이글" },
  { id: 618, answer: "조류독감" },
  { id: 619, answer: "개구리" },
  { id: 620, answer: "일출" },
  { id: 621, answer: "냉커피" },
  { id: 622, answer: "우거지" },
  { id: 623, answer: "좌충우돌" },
  { id: 624, answer: "원빈" },
  { id: 625, answer: "랜드마크" },
  { id: 626, answer: "청계천" },
];
// ===== 운세 텍스트 DB (조합형) =====

// 총운 사자성어 (25개)
var FORTUNE_WORD = [
  "일석이조", "자가당착", "전화위복", "새옹지마", "유비무환",
  "대기만성", "순풍만범", "고진감래", "심기일전", "화이부동",
  "우공이산", "금상첨화", "지피지기", "호시탐탐", "백문불여일견",
  "이심전심", "과유불급", "결자해지", "타산지석", "청출어람",
  "동병상련", "백절불굴", "명경지수", "일취월장", "선견지명"
];

// 도입부 (20개) - 오늘의 기운/분위기
var FORTUNE_INTRO = [
  "오늘은 새로운 시작을 알리는 기운이 감돌고 있습니다.",
  "오늘 당신에게는 특별한 에너지가 흐르고 있습니다.",
  "평소와는 다른 분위기의 하루가 될 것입니다.",
  "오늘은 차분하게 자신을 돌아보기 좋은 날입니다.",
  "활기찬 기운이 당신을 감싸고 있는 하루입니다.",
  "오늘은 변화의 바람이 불어오는 날입니다.",
  "조용하지만 강한 힘이 당신을 이끄는 하루입니다.",
  "오늘은 그동안 쌓아온 것들이 빛을 발하는 날입니다.",
  "신중한 판단이 필요한 하루가 될 것입니다.",
  "뜻밖의 행운이 찾아올 수 있는 날입니다.",
  "오늘은 마음의 여유를 가지는 것이 중요합니다.",
  "당신의 직감이 빛나는 하루가 될 것입니다.",
  "오늘은 주변 사람들과의 관계가 핵심이 되는 날입니다.",
  "미래를 위한 씨앗을 뿌리기 좋은 날입니다.",
  "오늘은 도전정신이 빛나는 날이 될 것입니다.",
  "평온한 기운 속에서 깊은 깨달음을 얻을 수 있습니다.",
  "당신의 노력이 결실을 맺기 시작하는 하루입니다.",
  "오늘은 감사한 마음을 가지면 행운이 배가됩니다.",
  "중요한 결정을 내리기에 적합한 날입니다.",
  "오늘은 작은 것에서 큰 기쁨을 찾을 수 있는 날입니다."
];

// 본문 조언 (30개)
var FORTUNE_BODY = [
  "의욕이 넘치고 능력도 뛰어나지만, 아직은 때를 기다려야 합니다. 눈앞의 이익보다 먼 미래의 큰 그림을 보세요.",
  "주변 사람들의 조언에 귀를 기울이면 뜻밖의 해답을 찾을 수 있습니다. 혼자 고민하지 마세요.",
  "작은 친절이 큰 인연으로 돌아올 수 있습니다. 주변 사람들에게 따뜻한 말 한마디 건네보세요.",
  "지나친 욕심은 화를 부릅니다. 적당한 선에서 만족할 줄 아는 지혜가 필요합니다.",
  "꾸준히 해온 일이 있다면 포기하지 마세요. 곧 결과가 나타나기 시작할 것입니다.",
  "새로운 도전을 시작하기에 좋은 때입니다. 머뭇거리지 말고 첫 걸음을 내딛으세요.",
  "감정에 휘둘리지 말고 이성적으로 판단하세요. 냉정한 분석이 최선의 결과를 가져옵니다.",
  "오늘은 체력을 비축하고 내실을 다지는 데 집중하세요. 무리한 활동은 피하는 것이 좋습니다.",
  "예상치 못한 변수가 생길 수 있으니 항상 플랜 B를 준비해두세요.",
  "자신의 장점을 적극적으로 어필하면 좋은 기회를 잡을 수 있습니다.",
  "오래된 습관을 바꿔보세요. 작은 변화가 큰 차이를 만들어낼 것입니다.",
  "타인의 성공을 부러워하기보다 자신만의 길을 꾸준히 걸어가세요.",
  "중요한 약속이나 계약이 있다면 꼼꼼하게 확인하세요. 세부사항이 결과를 좌우합니다.",
  "오늘은 배움의 자세가 중요합니다. 모르는 것을 인정하면 더 빠르게 성장합니다.",
  "스트레스를 받더라도 감정을 잘 다스리세요. 인내심이 당신의 무기입니다.",
  "갈림길에 서 있다면 가슴이 뛰는 쪽을 선택하세요. 후회 없는 하루가 될 것입니다.",
  "오늘 만나는 사람이 미래의 중요한 인연이 될 수 있습니다. 첫인상에 신경 쓰세요.",
  "완벽을 추구하기보다 일단 시작하는 것이 중요합니다. 행동이 답인 하루입니다.",
  "자신을 너무 몰아세우지 마세요. 쉬어갈 줄 아는 것도 능력입니다.",
  "동료나 가족과의 소통이 중요한 날입니다. 진심을 담아 대화하세요.",
  "과거의 실수에 연연하지 말고 앞으로 나아가세요. 지금 이 순간이 가장 중요합니다.",
  "금전적인 결정은 서두르지 마세요. 한 박자 쉬어가면 더 나은 선택을 할 수 있습니다.",
  "자신감을 가지세요. 당신의 능력은 스스로 생각하는 것보다 훨씬 뛰어납니다.",
  "오늘은 리더십을 발휘하기 좋은 날입니다. 주도적으로 나서면 좋은 결과가 있을 것입니다.",
  "불필요한 갈등은 피하세요. 한 발 양보하면 열 발의 이익이 돌아옵니다.",
  "오늘은 직감을 믿어보세요. 논리적으로 설명할 수 없지만 느낌이 맞을 때가 있습니다.",
  "정리정돈이 필요한 날입니다. 주변을 깔끔히 하면 마음도 정돈되고 좋은 기운이 들어옵니다.",
  "남들과 비교하지 마세요. 어제의 나와 오늘의 나를 비교하는 것이 진정한 성장입니다.",
  "겸손한 자세를 유지하세요. 교만은 실패를 부르고, 겸손은 기회를 부릅니다.",
  "마음먹은 일이 있다면 오늘 바로 실행에 옮기세요. 미루면 기회가 사라질 수 있습니다."
];

// 마무리 (20개) - 당부/격려
var FORTUNE_CLOSING = [
  "오늘 하루도 당신을 응원합니다!",
  "좋은 하루 보내세요. 행운이 함께할 것입니다.",
  "자신을 믿고 한 걸음 더 나아가세요.",
  "오늘의 선택이 내일의 행복을 결정합니다.",
  "당신은 충분히 잘하고 있습니다. 화이팅!",
  "소중한 사람들과 함께하는 시간을 만들어보세요.",
  "오늘 하루 수고한 자신에게 작은 선물을 주세요.",
  "내일은 더 좋은 일이 기다리고 있을 것입니다.",
  "당신의 밝은 에너지가 주변을 환하게 밝힐 것입니다.",
  "포기하지 않는 한, 실패는 없습니다.",
  "오늘의 작은 노력이 미래의 큰 성과로 돌아올 것입니다.",
  "웃음을 잃지 마세요. 웃는 얼굴에 행운이 찾아옵니다.",
  "당신에게 주어진 오늘이라는 선물을 소중히 여기세요.",
  "마음의 평화를 유지하면 모든 일이 잘 풀릴 것입니다.",
  "오늘 하루가 당신의 인생에서 특별한 날이 되길 바랍니다.",
  "걱정보다 기대를 더 많이 하는 하루가 되세요.",
  "당신의 꿈에 한 발짝 더 가까워지는 하루입니다.",
  "어떤 일이 있어도 당신은 해낼 수 있습니다.",
  "감사하는 마음으로 하루를 시작하면 모든 것이 달라집니다.",
  "오늘 하루, 자신에게 조금 더 너그러워져 보세요."
];

var LUCKY_COLORS = [
  "빨간색", "파란색", "노란색", "초록색", "보라색",
  "주황색", "핑크색", "하늘색", "흰색", "검은색",
  "금색", "은색", "민트색", "연보라색", "코랄색",
  "네이비", "베이지", "카키색", "터콰이즈", "로즈골드"
];
// ===== 싱봇 유틸리티 =====

var CHOSUNG = [
  "ㄱ","ㄲ","ㄴ","ㄷ","ㄸ","ㄹ","ㅁ","ㅂ","ㅃ","ㅅ","ㅆ",
  "ㅇ","ㅈ","ㅉ","ㅊ","ㅋ","ㅌ","ㅍ","ㅎ"
];

function getChosung(char) {
  var code = char.charCodeAt(0) - 0xAC00;
  if (code < 0 || code > 11171) return char;
  return CHOSUNG[Math.floor(code / 588)];
}

function hashCode(str) {
  var hash = 0;
  for (var i = 0; i < str.length; i++) {
    var c = str.charCodeAt(i);
    hash = ((hash << 5) - hash) + c;
    hash = hash & hash;
  }
  return hash;
}

function todayString() {
  var d = new Date();
  return d.getFullYear() + "-" + (d.getMonth() + 1) + "-" + d.getDate();
}

function todaySeed() {
  var d = new Date();
  return d.getFullYear() * 10000 + (d.getMonth() + 1) * 100 + d.getDate();
}

function normalize(s) {
  return s.replace(/\s/g, "").toLowerCase();
}

// "천일(천일염)" → "천일" (괄호 설명 제거)
function stripParens(s) {
  var idx = s.indexOf("(");
  if (idx > 0) return s.substring(0, idx).trim();
  return s;
}

function pickRandom(arr) {
  return arr[Math.floor(Math.random() * arr.length)];
}

function generateBar(score) {
  var filled = Math.floor(score / 10);
  var bar = "";
  for (var i = 0; i < 10; i++) {
    bar += (i < filled ? "■" : "□");
  }
  return bar;
}
// ===== 싱봇 상태 관리 =====

var roomState = {};
var scoreData = {};

var DATA_DIR = (function() {
  try {
    return FileStream.getSdcardPath() + "/singbot/";
  } catch (e) {
    try {
      return android.os.Environment.getExternalStorageDirectory().getAbsolutePath() + "/singbot/";
    } catch (e2) {
      return "/sdcard/singbot/";
    }
  }
})();

function initRoom(room) {
  if (!roomState[room]) {
    roomState[room] = {
      activeQuiz: null,
      usedQuestions: { nonsense: [], catchmind: [] }
    };
  }
}

function loadScores() {
  try {
    var raw = FileStream.read(DATA_DIR + "scores.json");
    if (raw) scoreData = JSON.parse(raw);
  } catch (e) {
    scoreData = {};
  }
}

function saveScores() {
  try {
    FileStream.createDir(DATA_DIR);
    FileStream.write(DATA_DIR + "scores.json", JSON.stringify(scoreData));
  } catch (e) {
    // 메모리에 유지, 다음 저장 시 재시도
  }
}

function addScore(room, quizType, sender, isCorrect) {
  if (!scoreData[room]) scoreData[room] = {};
  if (!scoreData[room][quizType]) scoreData[room][quizType] = {};
  if (!scoreData[room][quizType][sender]) {
    scoreData[room][quizType][sender] = { correct: 0, total: 0 };
  }
  if (isCorrect) scoreData[room][quizType][sender].correct++;
  scoreData[room][quizType][sender].total++;
  saveScores();
}

function getLeaderboard(room, quizType) {
  if (!scoreData[room] || !scoreData[room][quizType]) return [];
  var scores = scoreData[room][quizType];
  var entries = [];
  for (var name in scores) {
    if (scores.hasOwnProperty(name)) {
      entries.push({
        name: name,
        correct: scores[name].correct,
        total: scores[name].total
      });
    }
  }
  entries.sort(function(a, b) { return b.correct - a.correct; });
  return entries;
}

// 스크립트 초기화 시 점수 로드
try { loadScores(); } catch (e) {}
// ===== VS 선택 =====

var VS_MESSAGES = [
  "고민 끝! 오늘은 '{choice}'(으)로 갑시다!",
  "제가 골랐습니다! '{choice}'!!!",
  "운명의 선택은... '{choice}'!",
  "'{choice}'(이)가 당신을 부르고 있어요!",
  "두구두구두구... '{choice}'!",
  "이건 무조건 '{choice}'이죠!",
  "'{choice}' 말고 다른 게 있나요?",
  "고민하지 마세요. '{choice}'입니다.",
  "하늘이 정해준 답... '{choice}'!",
  "싱봇의 촉이 말합니다... '{choice}'!"
];

function handleVsPick(room, msg, sender, replier) {
  var input = msg.replace(/^(vs|VS)\s*/, "").trim();
  if (!input) {
    replier.reply("[ 싱봇 VS ]\n\n사용법: vs 선택지1 선택지2 선택지3\n예시: vs 김치찜 백반 된장찌개");
    return;
  }

  var options;
  if (input.indexOf(",") !== -1) {
    options = input.split(",");
  } else {
    options = input.split(/\s+/);
  }

  var cleaned = [];
  for (var i = 0; i < options.length; i++) {
    var opt = options[i].trim();
    if (opt.length > 0) cleaned.push(opt);
  }

  if (cleaned.length < 2) {
    replier.reply("2개 이상의 선택지를 입력해주세요!");
    return;
  }

  var chosen = pickRandom(cleaned);
  var template = pickRandom(VS_MESSAGES);

  var reply = "[ 싱봇 VS ]\n\n";
  reply += cleaned.join(" vs ") + "\n\n";
  reply += template.replace(/\{choice\}/g, chosen);

  replier.reply(reply);
}
// ===== 오늘의 한마디 =====

function handleDailyQuote(room, msg, sender, replier) {
  var idx = todaySeed() % QUOTES_DB.length;
  var quote = QUOTES_DB[idx];

  replier.reply(
    "[ 싱봇 오늘의 한마디 ]\n\n" +
    "\"" + quote.text + "\"\n\n" +
    "- " + quote.author + " -"
  );
}
// ===== 넌센스 퀴즈 =====

function generateNonsenseHint(answer, level, naverHint) {
  // level 1: 네이버 제공 힌트 (백과XX 같은)
  if (level === 1 && naverHint) {
    return naverHint;
  }

  var chars = stripParens(answer).split("");
  var masked = [];

  if (level <= 1) {
    for (var i = 0; i < chars.length; i++) masked.push("●");
  } else if (level === 2) {
    for (var i = 0; i < chars.length; i++) {
      if (i === 0 || i === chars.length - 1) masked.push(chars[i]);
      else masked.push("●");
    }
  } else {
    var hideIdx = 1 + Math.floor(Math.random() * Math.max(1, chars.length - 2));
    if (chars.length <= 2) hideIdx = chars.length - 1;
    for (var i = 0; i < chars.length; i++) {
      masked.push(i === hideIdx ? "●" : chars[i]);
    }
  }
  return masked.join(" ");
}

function handleNonsenseStart(room, msg, sender, replier) {
  initRoom(room);

  if (roomState[room].activeQuiz) {
    replier.reply("이미 퀴즈가 진행 중입니다! '정답 [답]'으로 답해주세요.");
    return;
  }

  var used = roomState[room].usedQuestions.nonsense;
  var available = [];
  for (var i = 0; i < NONSENSE_DB.length; i++) {
    if (used.indexOf(i) === -1) available.push(i);
  }
  if (available.length === 0) {
    roomState[room].usedQuestions.nonsense = [];
    for (var i = 0; i < NONSENSE_DB.length; i++) available.push(i);
  }

  var idx = pickRandom(available);
  var quiz = NONSENSE_DB[idx];
  roomState[room].usedQuestions.nonsense.push(idx);

  var hint = generateNonsenseHint(quiz.a, 0, quiz.h);

  roomState[room].activeQuiz = {
    type: "nonsense",
    question: quiz.q,
    answer: quiz.a,
    naverHint: quiz.h || "",
    reason: quiz.r || "",
    hintLevel: 0,
    startTime: Date.now()
  };

  replier.reply(
    "[ 싱봇 넌센스 퀴즈 ]\n\n" +
    quiz.q + "\n\n" +
    "힌트: " + hint + "\n" +
    "⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼\n" +
    "정답 [답] | 힌트 | 포기\n" +
    "넌센스점수판\n" +
    "⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼"
  );
}

function handleNonsenseLeaderboard(room, msg, sender, replier) {
  var entries = getLeaderboard(room, "nonsense");
  if (entries.length === 0) {
    replier.reply("아직 기록이 없어요! '넌센스'를 입력해서 퀴즈를 시작해보세요.");
    return;
  }

  var MEDALS = ["🥇", "🥈", "🥉"];
  var text = "[ 싱봇 넌센스 점수판 ]\n\n";
  var limit = Math.min(entries.length, 10);
  for (var i = 0; i < limit; i++) {
    var prefix = i < 3 ? MEDALS[i] : (i + 1) + "위";
    var rate = Math.round(entries[i].correct / entries[i].total * 100);
    text += prefix + " " + entries[i].name + " - " + entries[i].correct + "문제 정답";
    text += " (정답률 " + rate + "%)\n";
  }

  replier.reply(text);
}
// ===== 캐치마인드 퀴즈 (이미지 기반) =====

function generateCatchmindHint(answer, level) {
  var chars = answer.split("");
  var result = [];

  if (level <= 0) {
    for (var i = 0; i < chars.length; i++) result.push("●");
  } else if (level === 1) {
    // 초성 힌트
    for (var i = 0; i < chars.length; i++) {
      if (i % 2 === 0) result.push(getChosung(chars[i]));
      else result.push("●");
    }
  } else if (level === 2) {
    // 일부 글자 공개
    for (var i = 0; i < chars.length; i++) {
      if (i % 2 === 0) result.push(chars[i]);
      else result.push("●");
    }
  } else {
    // 하나만 숨기기
    var hideIdx = Math.floor(Math.random() * chars.length);
    for (var i = 0; i < chars.length; i++) {
      result.push(i === hideIdx ? "●" : chars[i]);
    }
  }
  return result.join(" ");
}

function handleCatchmindStart(room, msg, sender, replier) {
  initRoom(room);

  if (roomState[room].activeQuiz) {
    replier.reply("이미 퀴즈가 진행 중입니다! '정답 [답]'으로 답해주세요.");
    return;
  }

  // 안 쓴 문제 찾기
  var used = roomState[room].usedQuestions.catchmind;
  var available = [];
  for (var i = 0; i < CATCHMIND_DB.length; i++) {
    if (used.indexOf(i) === -1) available.push(i);
  }
  if (available.length === 0) {
    roomState[room].usedQuestions.catchmind = [];
    for (var i = 0; i < CATCHMIND_DB.length; i++) available.push(i);
  }

  var idx = pickRandom(available);
  var quiz = CATCHMIND_DB[idx];
  roomState[room].usedQuestions.catchmind.push(idx);

  var hint = generateCatchmindHint(quiz.answer, 0);

  roomState[room].activeQuiz = {
    type: "catchmind",
    question: "캐치마인드",
    answer: quiz.answer,
    hintLevel: 0,
    startTime: Date.now(),
    quizId: quiz.id
  };

  // OG 미리보기용 URL 전송
  var quizUrl = CATCHMIND_BASE_URL + "/q/" + quiz.id + ".html";
  replier.reply(quizUrl);
  replier.reply(
    "[ 싱봇 캐치마인드 ]\n\n" +
    hint + "\n" +
    "⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼\n" +
    "정답 [답] | 힌트 | 포기\n" +
    "캐치점수판\n" +
    "⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼⎼"
  );
}

function handleCatchmindLeaderboard(room, msg, sender, replier) {
  var entries = getLeaderboard(room, "catchmind");
  if (entries.length === 0) {
    replier.reply("아직 기록이 없어요! '캐치마인드'를 입력해서 퀴즈를 시작해보세요.");
    return;
  }

  var MEDALS = ["🥇", "🥈", "🥉"];
  var text = "[ 싱봇 캐치마인드 점수판 ]\n\n";
  var limit = Math.min(entries.length, 10);
  for (var i = 0; i < limit; i++) {
    var prefix = i < 3 ? MEDALS[i] : (i + 1) + "위";
    var rate = Math.round(entries[i].correct / entries[i].total * 100);
    text += prefix + " " + entries[i].name + " - " + entries[i].correct + "문제 정답";
    text += " (정답률 " + rate + "%)\n";
  }

  replier.reply(text);
}
// ===== 운세 =====

var DDI_ANIMALS = ["원숭이","닭","개","돼지","쥐","소","호랑이","토끼","용","뱀","말","양"];

function getConstellation(month, day) {
  var md = month * 100 + day;
  if (md >= 1222 || md <= 119) return "염소";
  if (md <= 218) return "물병";
  if (md <= 320) return "물고기";
  if (md <= 419) return "양";
  if (md <= 520) return "황소";
  if (md <= 621) return "쌍둥이";
  if (md <= 722) return "게";
  if (md <= 822) return "사자";
  if (md <= 922) return "처녀";
  if (md <= 1022) return "천칭";
  if (md <= 1121) return "전갈";
  return "사수";
}

// 네이버 별자리 운세 API (pkid=105) - 검증됨
function fetchNaverStarFortune(constellation) {
  try {
    var query = java.net.URLEncoder.encode(constellation + "자리 운세", "UTF-8");
    var url = "https://ts-proxy.naver.com/content/apirender.nhn?where=m&pkid=105&key=FortuneAPI&q=" + query;
    var response = org.jsoup.Jsoup.connect(url)
      .ignoreContentType(true)
      .userAgent("Mozilla/5.0")
      .get().text();
    var data = JSON.parse(response);
    if (!data.result || data.result.length === 0) return null;

    var doc = org.jsoup.Jsoup.parse(data.result[0]);
    var content = doc.select("p.text_box").text();
    if (content && content.length > 10) return content;
    return null;
  } catch (e) {
    return null;
  }
}

function handleFortune(room, msg, sender, replier) {
  var input = msg.replace(/^(운세|오늘의운세)[,\s]*/, "").trim();
  var name, year, month, day, gender;

  // 쉼표 형식: 여자,19901122
  var commaMatch = input.match(/^(남|여|남자|여자)[,\s]+(\d{8})$/) ||
                   input.match(/^(\d{8})[,\s]+(남|여|남자|여자)$/);
  // 공백 형식: 여자 19901122
  var spaceMatch = !commaMatch && (
    input.match(/^(남|여|남자|여자)\s+(\d{8})$/) ||
    input.match(/^(\d{8})\s+(남|여|남자|여자)$/)
  );

  var match = commaMatch || spaceMatch;
  if (match) {
    if (/^\d/.test(match[1])) { var t = match[1]; match[1] = match[2]; match[2] = t; }
    name = sender;
    gender = match[1].charAt(0);
    var ds = match[2];
    year = parseInt(ds.substring(0, 4));
    month = parseInt(ds.substring(4, 6));
    day = parseInt(ds.substring(6, 8));
  } else {
    replier.reply(
      "[ 싱봇 운세 ]\n\n" +
      "사용법:\n" +
      "  운세 여자,19901122\n" +
      "  운세 남 19950315\n" +
      "  오늘의운세,여,19901122"
    );
    return;
  }

  var star = getConstellation(month, day);
  var ddi = DDI_ANIMALS[year % 12];
  var genderText = gender === "여" ? "여자" : "남자";

  var today = new Date();
  var todayStr = today.getFullYear() + "." +
    (today.getMonth() + 1 < 10 ? "0" : "") + (today.getMonth() + 1) + "." +
    (today.getDate() < 10 ? "0" : "") + today.getDate();

  // 네이버 별자리 운세 API (메신저봇R에서만)
  var fortuneText = null;
  if (typeof org !== "undefined" && typeof org.jsoup !== "undefined") {
    fortuneText = fetchNaverStarFortune(star);
  }

  var result = "[ " + name + " ]님의 오늘의운세 ~ ♬\n\n";
  result += todayStr + " | " + star + "자리 | " + ddi + "띠 | " + genderText + "\n";
  result += "━━━━━━━━━━━━━━━━━━\n\n";

  if (fortuneText) {
    result += fortuneText;
  } else {
    // 폴백: 알고리즘 조합
    var baseKey = name + year + month + day + gender + todayString();
    var s1 = Math.abs(hashCode(baseKey + "word"));
    var s2 = Math.abs(hashCode(baseKey + "intro"));
    var s3 = Math.abs(hashCode(baseKey + "body"));
    var s4 = Math.abs(hashCode(baseKey + "close"));

    result += "총운: \"" + FORTUNE_WORD[s1 % FORTUNE_WORD.length] + "\"\n\n";
    result += FORTUNE_INTRO[s2 % FORTUNE_INTRO.length] + " ";
    result += FORTUNE_BODY[s3 % FORTUNE_BODY.length] + " ";
    result += FORTUNE_CLOSING[s4 % FORTUNE_CLOSING.length];
  }

  result += "\n\n━━━━━━━━━━━━━━━━━━\n";

  var s5 = Math.abs(hashCode(name + year + month + day + gender + todayString() + "luck"));
  result += "🔮 행운지수: " + generateBar((s5 % 100) + 1) + " " + ((s5 % 100) + 1) + "점\n";
  result += "🎨 행운의 색: " + LUCKY_COLORS[Math.abs(hashCode(name + year + todayString() + "color")) % LUCKY_COLORS.length] + "\n";
  result += "🔢 행운의 숫자: " + ((Math.abs(hashCode(name + day + todayString() + "num")) % 45) + 1);

  replier.reply(result);
}
// ===== Gemma AI 대화 =====

var GEMMA_API_KEY = "AIzaSyCejLMCDgAe6-YEgW0fS2oSPF0_6xPwiY4";
var GEMMA_MODEL = "gemma-4-31b-it";
var GEMMA_ENDPOINT = "https://generativelanguage.googleapis.com/v1beta/models/" + GEMMA_MODEL + ":generateContent?key=" + GEMMA_API_KEY;

var chatHistory = {};

function handleChat(room, msg, sender, replier) {
  var prompt = msg.replace(/^~\s*/, "").trim();
  if (!prompt) {
    replier.reply("사용법: ~ <질문>\n예) ~ 오늘 뭐 먹지?");
    return;
  }

  // 대화 기록 관리 (방별, 유저별, 최근 10턴)
  var historyKey = room + ":" + sender;
  if (!chatHistory[historyKey]) chatHistory[historyKey] = [];

  chatHistory[historyKey].push({ role: "user", parts: [{ text: prompt }] });

  if (chatHistory[historyKey].length > 20) {
    chatHistory[historyKey] = chatHistory[historyKey].slice(-20);
  }

  try {
    var Jsoup = org.jsoup.Jsoup;
    var Method = org.jsoup.Connection.Method;

    var contents = [
      { role: "user", parts: [{ text: "너는 카카오톡 채팅봇이야. 친구처럼 반말로 짧게 대답해. 3줄 이내로. 이모지 가끔 써." }] },
      { role: "model", parts: [{ text: "ㅇㅋ 알겠어!" }] }
    ].concat(chatHistory[historyKey]);

    var body = JSON.stringify({
      contents: contents,
      generationConfig: {
        maxOutputTokens: 200,
        thinkingConfig: { thinkingLevel: "MINIMAL" }
      }
    });

    var response = Jsoup.connect(GEMMA_ENDPOINT)
      .header("Content-Type", "application/json")
      .requestBody(body)
      .method(Method.POST)
      .ignoreContentType(true)
      .ignoreHttpErrors(true)
      .timeout(120000)
      .execute();

    var statusCode = response.statusCode();
    var json = JSON.parse(response.body());

    if (statusCode < 200 || statusCode >= 300) {
      replier.reply("API 오류: HTTP " + statusCode);
      chatHistory[historyKey].pop();
      return;
    }

    var answer = "";
    var candidates = json.candidates || [];
    for (var i = 0; i < candidates.length; i++) {
      var parts = (candidates[i].content && candidates[i].content.parts) || [];
      for (var j = 0; j < parts.length; j++) {
        if (parts[j].thought === true) continue;
        if (typeof parts[j].text === "string" && parts[j].text.length > 0) {
          answer += parts[j].text;
        }
      }
    }

    if (!answer) {
      replier.reply("응답을 받지 못했어요.");
      chatHistory[historyKey].pop();
      return;
    }

    chatHistory[historyKey].push({ role: "model", parts: [{ text: answer }] });
    replier.reply(answer);
  } catch (e) {
    replier.reply("오류 발생: " + e.toString());
    chatHistory[historyKey].pop();
  }
}

function handleChatReset(room, msg, sender, replier) {
  var historyKey = room + ":" + sender;
  chatHistory[historyKey] = [];
  replier.reply(sender + "님의 대화 기록을 초기화했어요.");
}
// ===== 광산 게임 데이터 =====

var MineData = {

  // --- 자원 정의 ---
  resources: {
    stone:     { name: "돌",       tier: 1, sellPrice: 2,    emoji: "🪨" },
    copperOre: { name: "구리광석", tier: 1, sellPrice: 5,    emoji: "🟤" },
    ironOre:   { name: "철광석",   tier: 1, sellPrice: 10,   emoji: "⬛" },
    silverOre: { name: "은광석",   tier: 2, sellPrice: 25,   emoji: "⬜" },
    goldOre:   { name: "금광석",   tier: 2, sellPrice: 50,   emoji: "🟡" },
    crystal:   { name: "수정",     tier: 2, sellPrice: 80,   emoji: "🔮" },
    ruby:      { name: "루비",     tier: 3, sellPrice: 200,  emoji: "🔴" },
    sapphire:  { name: "사파이어", tier: 3, sellPrice: 250,  emoji: "🔵" },
    diamond:   { name: "다이아몬드", tier: 3, sellPrice: 500, emoji: "💎" },
    abyssStone:{ name: "심연석",   tier: 3, sellPrice: 1000, emoji: "🌑" }
  },

  // 이름 → ID 역매핑
  resourceIdMap: {},

  // --- 광산 지역 ---
  areas: [
    {
      name: "초보 광산",
      emoji: "⛏���",
      depthRange: [1, 50],
      unlockDepth: 0,
      unlockPickaxe: 0,
      unlockPrestige: 0,
      drops: [
        { id: "stone",     rate: 0.80, min: 2, max: 6 },
        { id: "copperOre", rate: 0.15, min: 1, max: 2 }
      ],
      goldRange: [5, 15]
    },
    {
      name: "구리 광산",
      emoji: "🟤",
      depthRange: [51, 100],
      unlockDepth: 50,
      unlockPickaxe: 0,
      unlockPrestige: 0,
      drops: [
        { id: "stone",     rate: 0.60, min: 2, max: 5 },
        { id: "copperOre", rate: 0.50, min: 2, max: 5 },
        { id: "ironOre",   rate: 0.10, min: 1, max: 2 }
      ],
      goldRange: [10, 30]
    },
    {
      name: "철 광산",
      emoji: "⬛",
      depthRange: [101, 200],
      unlockDepth: 100,
      unlockPickaxe: 5,
      unlockPrestige: 0,
      drops: [
        { id: "stone",     rate: 0.50, min: 3, max: 8 },
        { id: "copperOre", rate: 0.35, min: 2, max: 5 },
        { id: "ironOre",   rate: 0.25, min: 1, max: 3 },
        { id: "silverOre", rate: 0.08, min: 1, max: 2 },
        { id: "goldOre",   rate: 0.02, min: 1, max: 1 }
      ],
      goldRange: [15, 40]
    },
    {
      name: "금 광산",
      emoji: "🟡",
      depthRange: [201, 350],
      unlockDepth: 200,
      unlockPickaxe: 10,
      unlockPrestige: 0,
      drops: [
        { id: "stone",     rate: 0.40, min: 3, max: 6 },
        { id: "ironOre",   rate: 0.35, min: 2, max: 4 },
        { id: "silverOre", rate: 0.25, min: 1, max: 3 },
        { id: "goldOre",   rate: 0.18, min: 1, max: 2 },
        { id: "crystal",   rate: 0.05, min: 1, max: 1 }
      ],
      goldRange: [30, 80]
    },
    {
      name: "수정 동굴",
      emoji: "🔮",
      depthRange: [351, 500],
      unlockDepth: 350,
      unlockPickaxe: 18,
      unlockPrestige: 0,
      drops: [
        { id: "ironOre",   rate: 0.30, min: 2, max: 4 },
        { id: "silverOre", rate: 0.25, min: 2, max: 3 },
        { id: "goldOre",   rate: 0.20, min: 1, max: 3 },
        { id: "crystal",   rate: 0.15, min: 1, max: 2 },
        { id: "ruby",      rate: 0.05, min: 1, max: 1 },
        { id: "sapphire",  rate: 0.03, min: 1, max: 1 }
      ],
      goldRange: [50, 120]
    },
    {
      name: "용암 갱도",
      emoji: "🌋",
      depthRange: [501, 700],
      unlockDepth: 500,
      unlockPickaxe: 25,
      unlockPrestige: 0,
      drops: [
        { id: "silverOre", rate: 0.25, min: 2, max: 4 },
        { id: "goldOre",   rate: 0.20, min: 2, max: 3 },
        { id: "crystal",   rate: 0.15, min: 1, max: 3 },
        { id: "ruby",      rate: 0.08, min: 1, max: 2 },
        { id: "sapphire",  rate: 0.06, min: 1, max: 1 },
        { id: "diamond",   rate: 0.03, min: 1, max: 1 }
      ],
      goldRange: [80, 200]
    },
    {
      name: "심연 광맥",
      emoji: "🌑",
      depthRange: [701, 999],
      unlockDepth: 700,
      unlockPickaxe: 35,
      unlockPrestige: 1,
      drops: [
        { id: "goldOre",    rate: 0.25, min: 2, max: 4 },
        { id: "crystal",    rate: 0.20, min: 2, max: 3 },
        { id: "ruby",       rate: 0.12, min: 1, max: 2 },
        { id: "sapphire",   rate: 0.10, min: 1, max: 2 },
        { id: "diamond",    rate: 0.06, min: 1, max: 1 },
        { id: "abyssStone", rate: 0.03, min: 1, max: 1 }
      ],
      goldRange: [150, 500]
    }
  ],

  // --- 곡괭이 이미지 ---
  pickaxeImageBase: "https://pkeony.github.io/singbot-images",

  // --- 곡괭이 티어 ---
  pickaxeTiers: [
    { name: "나무 곡괭이",   emoji: "🪓", levels: [1, 5],   basePower: 1,   growth: 1,   image: "pickaxe_wood"       },
    { name: "돌 곡괭이",     emoji: "⛏️", levels: [6, 10],  basePower: 7,   growth: 2,   image: "pickaxe_stone"      },
    { name: "구리 곡괭이",   emoji: "🔧", levels: [11, 15], basePower: 18,  growth: 3,   image: "pickaxe_iron"       },
    { name: "철 곡괭이",     emoji: "⚒️", levels: [16, 20], basePower: 35,  growth: 4,   image: "pickaxe_steel"      },
    { name: "은 곡괭이",     emoji: "🔨", levels: [21, 25], basePower: 60,  growth: 6,   image: "pickaxe_mithril"    },
    { name: "금 곡괭이",     emoji: "✨", levels: [26, 30], basePower: 100, growth: 8,   image: "pickaxe_orichalcum" },
    { name: "수정 곡괭이",   emoji: "💜", levels: [31, 35], basePower: 155, growth: 11,  image: "pickaxe_runic"      },
    { name: "다이아 곡괭이", emoji: "💎", levels: [36, 40], basePower: 230, growth: 18,  image: "pickaxe_ultimate"   }
  ],

  // --- 깊이 보스 ---
  depthBosses: {
    50:  { name: "바위 골렘",     requiredPower: 5,   reward: { gold: 200 }  },
    100: { name: "구리 수호자",   requiredPower: 15,  reward: { gold: 500 }  },
    200: { name: "철갑 거북",     requiredPower: 40,  reward: { gold: 1500 } },
    350: { name: "수정 정령",     requiredPower: 80,  reward: { gold: 4000 } },
    500: { name: "용암 드래곤",   requiredPower: 130, reward: { gold: 10000 } },
    700: { name: "심연의 파수꾼", requiredPower: 200, reward: { gold: 30000 } }
  },

  // --- NPC 상점 ---
  shopItems: [
    { name: "에너지 음료",  price: 500,  desc: "체력 +5 즉시 회복",        effect: "stamina", value: 5   },
    { name: "행운의 부적",  price: 2000, desc: "희귀 드롭률 +30% (10회)", effect: "luck",    value: 10  },
    { name: "깊이 폭탄",   price: 3000, desc: "깊이 +10 즉시 진행",       effect: "depth",   value: 10  },
    { name: "광부 도시락",  price: 1000, desc: "방치 수입 +50% (2시간)",   effect: "idle",    value: 120 }
  ],

  // --- 도감 보상 ---
  codexRewards: [
    { threshold: 3,  desc: "행운 +5%",          bonus: "luck",       value: 0.05 },
    { threshold: 5,  desc: "방치 수입 +20%",    bonus: "idle",       value: 0.20 },
    { threshold: 7,  desc: "채굴력 +10%",       bonus: "power",      value: 0.10 },
    { threshold: 10, desc: "스태미나 +10 & 전용 스킨", bonus: "stamina", value: 10 }
  ],

  // --- 장비 ---
  equipment: {
    helmet: [
      { name: "가죽 안전모",   tier: 1, emoji: "🪖", image: "helmet_1", staminaBonus: 3,  pvpDef: 5,   cost: { gold: 2000,  resources: [{ name: "철광석", count: 15 }, { name: "구리광석", count: 20 }] } },
      { name: "강철 안전모",   tier: 2, emoji: "⛑️", image: "helmet_2", staminaBonus: 6,  pvpDef: 12,  cost: { gold: 15000, resources: [{ name: "은광석", count: 10 }, { name: "철광석", count: 30 }] } },
      { name: "다이아 안전모", tier: 3, emoji: "👑", image: "helmet_3", staminaBonus: 10, pvpDef: 25,  cost: { gold: 80000, resources: [{ name: "다이아몬드", count: 3 }, { name: "금광석", count: 20 }] } }
    ],
    lamp: [
      { name: "양초 램프",     tier: 1, emoji: "🕯️", image: "lamp_1", luckBonus: 0.05,  cost: { gold: 1500,  resources: [{ name: "구리광석", count: 15 }, { name: "돌", count: 30 }] } },
      { name: "가스 램프",     tier: 2, emoji: "🔦", image: "lamp_2", luckBonus: 0.12,  cost: { gold: 12000, resources: [{ name: "금광석", count: 8 }, { name: "수정", count: 5 }] } },
      { name: "수정 램프",     tier: 3, emoji: "💡", image: "lamp_3", luckBonus: 0.25,  cost: { gold: 70000, resources: [{ name: "수정", count: 15 }, { name: "루비", count: 3 }] } }
    ],
    boots: [
      { name: "가죽 장화",     tier: 1, emoji: "🥾", image: "boots_1", idleBonus: 0.15, depthBonus: 1,  cost: { gold: 1800,  resources: [{ name: "철광석", count: 10 }, { name: "구리광석", count: 15 }] } },
      { name: "강철 장화",     tier: 2, emoji: "👢", image: "boots_2", idleBonus: 0.30, depthBonus: 2,  cost: { gold: 14000, resources: [{ name: "은광석", count: 8 }, { name: "금광석", count: 5 }] } },
      { name: "용암 장화",     tier: 3, emoji: "🔥", image: "boots_3", idleBonus: 0.50, depthBonus: 3,  cost: { gold: 75000, resources: [{ name: "루비", count: 5 }, { name: "사파이어", count: 3 }] } }
    ]
  },

  // --- 프레스티지 상점 ---
  prestigeShop: [
    { id: "miningPower", name: "채굴 효율",   maxLevel: 5, costs: [5, 10, 20, 40, 80],  desc: "채굴력 +10%/레벨" },
    { id: "idleRate",    name: "방치 수입",   maxLevel: 5, costs: [5, 10, 20, 40, 80],  desc: "방치 수입 +20%/레벨" },
    { id: "luck",        name: "행운",       maxLevel: 5, costs: [3, 8, 15, 30, 60],   desc: "희귀 드롭률 +5%/레벨" },
    { id: "stamina",     name: "체력 증가",   maxLevel: 3, costs: [5, 15, 35],          desc: "최대 체력 +5/레벨" },
    { id: "startDepth",  name: "시작 보너스", maxLevel: 3, costs: [8, 20, 50],          desc: "환생 후 깊이 25/50/100 시작" }
  ],

  prestigeStartDepths: [0, 25, 50, 100],

  // --- 프레스티지 조건 ---
  prestigeRequirements: { minDepth: 500, minPickaxe: 25 },

  // --- 시장 기본가 (일일 변동 기준) ---
  marketBasePrices: {
    stone: 2, copperOre: 5, ironOre: 10, silverOre: 25,
    goldOre: 50, crystal: 80, ruby: 200, sapphire: 250,
    diamond: 500, abyssStone: 1000
  },

  // --- 길드 ---
  guild: {
    createCost: 50000,
    createPrestigeReq: 3,
    maxMembers: 10,
    levelUpExp: [1000, 3000, 6000, 10000, 16000, 24000, 35000, 50000, 70000, 100000],
    perks: {
      miningPower: 0.02,  // 레벨당 +2%
      idleRate: 0.05,     // 2레벨당 +5%
      luck: 0.03          // 3레벨당 +3%
    }
  },

  // --- 채굴 이벤트 ---
  miningEvents: {
    chance: 0.12,
    types: [
      { id: "treasure_chest", name: "보물 상자",    emoji: "🎁", weight: 30, goldRange: [100, 1000] },
      { id: "trap",           name: "함정!",        emoji: "💥", weight: 20, goldLossPercent: [0.03, 0.08], maxLoss: 2000 },
      { id: "merchant",       name: "떠돌이 상인",  emoji: "🧙", weight: 15 },
      { id: "rich_vein",      name: "풍부한 광맥",  emoji: "✨", weight: 25 },
      { id: "mysterious_ore",  name: "신비한 광석",  emoji: "🌀", weight: 10 }
    ]
  },

  // --- 도박장 ---
  gambling: {
    minBet: 100,
    maxBet: 50000,
    cooldownMs: 30000,
    roulette: [
      { label: "💀 꽝",     multiplier: 0,   weight: 38 },
      { label: "😢 반타",   multiplier: 0.5, weight: 20 },
      { label: "😊 1.5배", multiplier: 1.5, weight: 20 },
      { label: "🎉 2배",   multiplier: 2,   weight: 12 },
      { label: "🔥 3배",   multiplier: 3,   weight: 7 },
      { label: "💎 5배",   multiplier: 5,   weight: 3 }
    ]
  },

  // --- 일일퀘스트 ---
  dailyQuests: {
    questsPerDay: 3,
    completionBonusGold: 800,
    completionBonusStarFragments: 1,
    tierMultiplier: [0.5, 0.5, 1.0, 1.0, 1.5, 1.5, 2.0, 2.0],
    types: [
      { id: "mine_count",      desc: "채굴 {n}회 하기",           paramRange: [3, 8],     rewardGold: [150, 300] },
      { id: "collect_resource", desc: "{resource} {n}개 수집하기", paramRange: [5, 15],    rewardGold: [200, 400] },
      { id: "earn_gold",       desc: "골드 {n}G 획득하기",         paramRange: [500, 2000], rewardGold: [150, 300] },
      { id: "reach_depth",     desc: "깊이 {n} 도달하기",          paramRange: [10, 50],   rewardGold: [250, 500] }
    ]
  },

  // --- 칭호 ---
  titles: [
    { id: "novice_miner",  name: "초보 광부",     emoji: "⛏️",  condition: { type: "totalMined", value: 10 },          desc: "총 10회 채굴" },
    { id: "veteran_miner", name: "베테랑 광부",   emoji: "⛏️",  condition: { type: "totalMined", value: 100 },         desc: "총 100회 채굴" },
    { id: "legend_miner",  name: "전설의 광부",   emoji: "⛏️",  condition: { type: "totalMined", value: 1000 },        desc: "총 1000회 채굴" },
    { id: "max_pickaxe",   name: "만렙 광부",     emoji: "💎",  condition: { type: "pickaxeLevel", value: 40 },        desc: "곡괭이 Lv.40" },
    { id: "explorer_100",  name: "탐험가",        emoji: "🗺️", condition: { type: "maxDepthReached", value: 100 },     desc: "깊이 100 도달" },
    { id: "explorer_500",  name: "심층 탐험가",   emoji: "🧭",  condition: { type: "maxDepthReached", value: 500 },    desc: "깊이 500 도달" },
    { id: "explorer_999",  name: "심연 정복자",   emoji: "🌑",  condition: { type: "maxDepthReached", value: 999 },    desc: "깊이 999 도달" },
    { id: "rich_10k",      name: "부자",          emoji: "💰",  condition: { type: "totalGoldEarned", value: 10000 },  desc: "총 10,000G 획득" },
    { id: "rich_100k",     name: "갑부",          emoji: "💰",  condition: { type: "totalGoldEarned", value: 100000 }, desc: "총 100,000G 획득" },
    { id: "rich_1m",       name: "재벌",          emoji: "👑",  condition: { type: "totalGoldEarned", value: 1000000 },desc: "총 1,000,000G 획득" },
    { id: "pvp_5wins",     name: "결투사",        emoji: "⚔️",  condition: { type: "pvpWins", value: 5 },              desc: "PvP 5승" },
    { id: "pvp_20wins",    name: "PvP 마스터",    emoji: "🏆",  condition: { type: "pvpWins", value: 20 },             desc: "PvP 20승" },
    { id: "prestige_1",    name: "환생자",        emoji: "⭐",  condition: { type: "prestigeCount", value: 1 },        desc: "첫 환생" },
    { id: "prestige_5",    name: "윤회의 달인",   emoji: "🌟",  condition: { type: "prestigeCount", value: 5 },        desc: "5회 환생" },
    { id: "codex_full",    name: "수집왕",        emoji: "📖",  condition: { type: "codexCount", value: 10 },          desc: "도감 완성" },
    { id: "gambler",       name: "도박꾼",        emoji: "🎲",  condition: { type: "gamblingTotal", value: 50000 },    desc: "총 도박 50,000G" },
    { id: "quest_master",  name: "퀘스트 마스터", emoji: "📋",  condition: { type: "questsCompleted", value: 30 },     desc: "퀘스트 30회 완료" }
  ],

  // --- 상수 ---
  config: {
    MAX_STAMINA_BASE: 20,
    STAMINA_REGEN_MS: 120000,        // 2분
    IDLE_CAP_MS: 8 * 3600000,        // 8시간
    IDLE_RATE_BASE: 0.5,             // 분당 기본 자원
    UPGRADE_BASE_COST: 100,
    UPGRADE_COST_MULTIPLIER: 1.35,
    UPGRADE_SUCCESS_RATE: [1.0, 1.0, 0.8, 0.8, 0.6, 0.6, 0.4, 0.4],
    DEPTH_GAIN_MIN: 1,
    DEPTH_GAIN_MAX: 3,
    STARTING_GOLD: 100
  }
};

// 역매핑 생성
(function() {
  for (var id in MineData.resources) {
    if (MineData.resources.hasOwnProperty(id)) {
      MineData.resourceIdMap[MineData.resources[id].name] = id;
    }
  }
})();
// ===== 싱봇 광산 게임 =====

var MINE_DATA_DIR = DATA_DIR + "mine_data/";
var mineAccounts = {};

// --- 유틸리티 ---

function sanitizeSender(sender) {
  return sender.replace(/[^a-zA-Z0-9가-힣]/g, "");
}

function saveMineAccount(sender, account) {
  mineAccounts[sender] = account;
  var fileName = sanitizeSender(sender) + ".json";
  try {
    FileStream.createDir(MINE_DATA_DIR);
    FileStream.write(MINE_DATA_DIR + fileName, JSON.stringify(account));
  } catch (e) {
    try {
      var folder = new java.io.File(MINE_DATA_DIR);
      if (!folder.exists()) folder.mkdirs();
      var file = new java.io.File(folder, fileName);
      var fos = new java.io.FileOutputStream(file);
      var writer = new java.io.OutputStreamWriter(fos, "UTF-8");
      writer.write(JSON.stringify(account));
      writer.close();
      fos.close();
    } catch (e2) {
      Log.e("광산 저장 오류: " + e2);
    }
  }
}

function loadMineAccount(sender) {
  if (mineAccounts[sender]) return mineAccounts[sender];
  var fileName = sanitizeSender(sender) + ".json";
  try {
    var raw = FileStream.read(MINE_DATA_DIR + fileName);
    if (raw) {
      mineAccounts[sender] = JSON.parse(raw);
      return mineAccounts[sender];
    }
  } catch (e) {}
  try {
    var file = new java.io.File(MINE_DATA_DIR, fileName);
    if (file.exists()) {
      var fis = new java.io.FileInputStream(file);
      var reader = new java.io.InputStreamReader(fis, "UTF-8");
      var br = new java.io.BufferedReader(reader);
      var data = "";
      var line;
      while ((line = br.readLine()) != null) data += line;
      br.close();
      reader.close();
      fis.close();
      mineAccounts[sender] = JSON.parse(data);
      return mineAccounts[sender];
    }
  } catch (e2) {}
  return null;
}

// 디스크에서 전체 계정 로드 (광부이름 역조회용)
function loadAllMineAccounts() {
  try {
    var dir = new java.io.File(MINE_DATA_DIR);
    if (!dir.exists()) return;
    var files = dir.listFiles();
    if (!files) return;
    for (var i = 0; i < files.length; i++) {
      var f = files[i];
      if (!f.getName().endsWith(".json")) continue;
      var senderName = f.getName().replace(".json", "");
      if (mineAccounts[senderName]) continue;
      try {
        var fis = new java.io.FileInputStream(f);
        var reader = new java.io.InputStreamReader(fis, "UTF-8");
        var br = new java.io.BufferedReader(reader);
        var data = "";
        var line;
        while ((line = br.readLine()) != null) data += line;
        br.close();
        mineAccounts[senderName] = JSON.parse(data);
      } catch (e2) {}
    }
  } catch (e) {}
}

// 광부이름으로 sender(카톡이름) 역조회
function findSenderByMineName(name) {
  for (var s in mineAccounts) {
    if (mineAccounts.hasOwnProperty(s) && mineAccounts[s].name === name) return s;
  }
  // 메모리에 없으면 디스크에서 전체 로드 후 재시도
  loadAllMineAccounts();
  for (var s in mineAccounts) {
    if (mineAccounts.hasOwnProperty(s) && mineAccounts[s].name === name) return s;
  }
  return null;
}

function createMineAccount(sender, name) {
  var now = Date.now();
  return {
    name: name,
    sender: sender,
    createdAt: now,
    lastActiveTime: now,
    lastStaminaRegen: now,
    gold: MineData.config.STARTING_GOLD,
    stamina: MineData.config.MAX_STAMINA_BASE,
    maxStamina: MineData.config.MAX_STAMINA_BASE,
    currentArea: 0,
    depth: 1,
    maxDepthReached: 1,
    pickaxeLevel: 1,
    equipment: { helmet: null, lamp: null, boots: null },
    resources: [],
    items: [],
    codex: {},
    idleRate: MineData.config.IDLE_RATE_BASE,
    prestigeCount: 0,
    starFragments: 0,
    prestigeBonuses: {
      miningPower: 0,
      idleRate: 0,
      luck: 0,
      stamina: 0,
      startDepth: 0
    },
    pvpWins: 0,
    pvpLosses: 0,
    guild: null,
    totalMined: 0,
    totalGoldEarned: 0,
    buffs: {},
    gamblingStats: { totalBet: 0, totalWon: 0, totalLost: 0, lastGambleTime: 0 },
    dailyQuests: { date: 0, quests: [], allClaimed: false },
    eventStats: { total: 0, byType: {} },
    pendingMerchant: null,
    titles: { unlocked: [], equipped: null },
    questsCompleted: 0
  };
}

// --- 자원/아이템 헬퍼 ---

function addResource(account, resourceName, count) {
  for (var i = 0; i < account.resources.length; i++) {
    if (account.resources[i].name === resourceName) {
      account.resources[i].count += count;
      return;
    }
  }
  account.resources.push({ name: resourceName, count: count });
}

function getResourceCount(account, resourceName) {
  for (var i = 0; i < account.resources.length; i++) {
    if (account.resources[i].name === resourceName) return account.resources[i].count;
  }
  return 0;
}

function removeResource(account, resourceName, count) {
  for (var i = 0; i < account.resources.length; i++) {
    if (account.resources[i].name === resourceName) {
      account.resources[i].count -= count;
      if (account.resources[i].count <= 0) {
        account.resources.splice(i, 1);
      }
      return true;
    }
  }
  return false;
}

function addItem(account, itemName, count) {
  for (var i = 0; i < account.items.length; i++) {
    if (account.items[i].name === itemName) {
      account.items[i].count += count;
      return;
    }
  }
  account.items.push({ name: itemName, count: count });
}

function getItemCount(account, itemName) {
  for (var i = 0; i < account.items.length; i++) {
    if (account.items[i].name === itemName) return account.items[i].count;
  }
  return 0;
}

function removeItem(account, itemName, count) {
  for (var i = 0; i < account.items.length; i++) {
    if (account.items[i].name === itemName) {
      account.items[i].count -= count;
      if (account.items[i].count <= 0) {
        account.items.splice(i, 1);
      }
      return true;
    }
  }
  return false;
}

// --- 장비 보너스 ---

function ensureEquipment(account) {
  if (!account.equipment) account.equipment = { helmet: null, lamp: null, boots: null };
}

function getEquipData(slot, tierIndex) {
  var items = MineData.equipment[slot];
  if (!items || tierIndex < 0 || tierIndex >= items.length) return null;
  return items[tierIndex];
}

function getEquippedData(account, slot) {
  ensureEquipment(account);
  var equipped = account.equipment[slot];
  if (!equipped) return null;
  var items = MineData.equipment[slot];
  for (var i = 0; i < items.length; i++) {
    if (items[i].name === equipped.name) return items[i];
  }
  return null;
}

function getEquipStaminaBonus(account) {
  var data = getEquippedData(account, "helmet");
  return data ? data.staminaBonus : 0;
}

function getEquipLuckBonus(account) {
  var data = getEquippedData(account, "lamp");
  return data ? data.luckBonus : 0;
}

function getEquipIdleBonus(account) {
  var data = getEquippedData(account, "boots");
  return data ? data.idleBonus : 0;
}

function getEquipDepthBonus(account) {
  var data = getEquippedData(account, "boots");
  return data ? data.depthBonus : 0;
}

// --- 곡괭이 계산 ---

function getPickaxeTier(level) {
  for (var i = MineData.pickaxeTiers.length - 1; i >= 0; i--) {
    if (level >= MineData.pickaxeTiers[i].levels[0]) return i;
  }
  return 0;
}

function getPickaxePower(account) {
  var tier = MineData.pickaxeTiers[getPickaxeTier(account.pickaxeLevel)];
  var levelInTier = account.pickaxeLevel - tier.levels[0];
  var base = tier.basePower + levelInTier * tier.growth;
  var prestigeBonus = 1 + account.prestigeBonuses.miningPower * 0.10;
  var codexBonus = getCodexBonus(account, "power");
  var guildBonus = getGuildBonus(account).miningPower;
  return Math.floor(base * prestigeBonus * (1 + codexBonus) * (1 + guildBonus));
}

function getPickaxeInfo(level) {
  var tierIdx = getPickaxeTier(level);
  var tier = MineData.pickaxeTiers[tierIdx];
  return { tier: tier, tierIndex: tierIdx };
}

function getUpgradeCost(level) {
  var goldCost = Math.floor(MineData.config.UPGRADE_BASE_COST * Math.pow(MineData.config.UPGRADE_COST_MULTIPLIER, level));
  var tierIdx = Math.floor(level / 5);
  var resourceCosts = [];
  if (tierIdx >= 1) resourceCosts.push({ name: "구리광석", count: tierIdx * 5 });
  if (tierIdx >= 2) resourceCosts.push({ name: "철광석", count: (tierIdx - 1) * 5 });
  if (tierIdx >= 3) resourceCosts.push({ name: "은광석", count: (tierIdx - 2) * 3 });
  return { gold: goldCost, resources: resourceCosts };
}

// --- 스태미나 ---

function getEffectiveMaxStamina(account) {
  var base = MineData.config.MAX_STAMINA_BASE;
  base += account.prestigeBonuses.stamina * 5;
  base += getEquipStaminaBonus(account);
  base += getCodexBonus(account, "stamina");
  return base;
}

function regenStamina(account) {
  account.maxStamina = getEffectiveMaxStamina(account);
  var now = Date.now();
  var elapsed = now - account.lastStaminaRegen;
  var regenTicks = Math.floor(elapsed / MineData.config.STAMINA_REGEN_MS);
  if (regenTicks > 0) {
    account.stamina = Math.min(account.maxStamina, account.stamina + regenTicks);
    account.lastStaminaRegen = now;
  }
}

// --- 방치 수입 ---

function calculateIdleIncome(account) {
  var now = Date.now();
  var elapsed = now - account.lastActiveTime;
  elapsed = Math.min(elapsed, MineData.config.IDLE_CAP_MS);
  var minutesAway = Math.floor(elapsed / 60000);
  if (minutesAway < 1) return null;

  var rate = account.idleRate * (1 + account.prestigeBonuses.idleRate * 0.20);
  var codexBonus = getCodexBonus(account, "idle");
  rate = rate * (1 + codexBonus);
  rate = rate * (1 + getEquipIdleBonus(account));
  rate = rate * (1 + getGuildBonus(account).idleRate);

  // 방치 버프 확인
  if (account.buffs.idle && account.buffs.idle.expires > now) {
    rate = rate * 1.5;
  }

  var amount = Math.floor(minutesAway * rate);
  if (amount <= 0) return null;

  var area = MineData.areas[account.currentArea];
  var basicDrop = area.drops[0];
  var resourceName = MineData.resources[basicDrop.id].name;

  return { resource: resourceName, amount: amount, minutes: minutesAway };
}

// --- 도감 보너스 ---

function getCodexCount(account) {
  var count = 0;
  for (var k in account.codex) {
    if (account.codex.hasOwnProperty(k) && account.codex[k]) count++;
  }
  return count;
}

function getCodexBonus(account, bonusType) {
  var count = getCodexCount(account);
  var total = 0;
  for (var i = 0; i < MineData.codexRewards.length; i++) {
    var reward = MineData.codexRewards[i];
    if (count >= reward.threshold && reward.bonus === bonusType) {
      total += reward.value;
    }
  }
  return total;
}

// --- 지역 전환 ---

function checkAreaUnlock(account) {
  for (var i = account.currentArea + 1; i < MineData.areas.length; i++) {
    var area = MineData.areas[i];
    if (account.depth >= area.unlockDepth &&
        account.pickaxeLevel >= area.unlockPickaxe &&
        account.prestigeCount >= area.unlockPrestige) {
      account.currentArea = i;
    } else {
      break;
    }
  }
}

// --- 깊이 보스 ---

function checkDepthBoss(account, oldDepth, newDepth) {
  for (var d in MineData.depthBosses) {
    if (MineData.depthBosses.hasOwnProperty(d)) {
      var bossDepth = parseInt(d);
      if (oldDepth < bossDepth && newDepth >= bossDepth) {
        var boss = MineData.depthBosses[d];
        var power = getPickaxePower(account);
        if (power >= boss.requiredPower) {
          account.gold += boss.reward.gold;
          account.totalGoldEarned += boss.reward.gold;
          return { defeated: true, boss: boss, depth: bossDepth, gold: boss.reward.gold };
        } else {
          account.depth = bossDepth - 1;
          return { defeated: false, boss: boss, depth: bossDepth, requiredPower: boss.requiredPower, currentPower: power };
        }
      }
    }
  }
  return null;
}

// ===== 하위 호환 초기화 =====

function ensureNewFields(account) {
  if (!account.gamblingStats) account.gamblingStats = { totalBet: 0, totalWon: 0, totalLost: 0, lastGambleTime: 0 };
  if (!account.dailyQuests) account.dailyQuests = { date: 0, quests: [], allClaimed: false };
  if (!account.eventStats) account.eventStats = { total: 0, byType: {} };
  if (!account.titles) account.titles = { unlocked: [], equipped: null };
  if (!account.questsCompleted) account.questsCompleted = 0;
}

// ===== Feature 3: 채굴 이벤트 =====

function rollMiningEvent(account, area) {
  if (Math.random() >= MineData.miningEvents.chance) return null;
  var types = MineData.miningEvents.types;
  var totalWeight = 0;
  for (var i = 0; i < types.length; i++) totalWeight += types[i].weight;
  var roll = Math.random() * totalWeight;
  var picked = types[0];
  for (var i = 0; i < types.length; i++) { roll -= types[i].weight; if (roll <= 0) { picked = types[i]; break; } }
  var result = { type: picked.id, name: picked.name, emoji: picked.emoji, details: {} };
  if (picked.id === "treasure_chest") {
    result.details.bonusGold = picked.goldRange[0] + Math.floor(Math.random() * (picked.goldRange[1] - picked.goldRange[0] + 1));
  } else if (picked.id === "trap") {
    var pct = picked.goldLossPercent[0] + Math.random() * (picked.goldLossPercent[1] - picked.goldLossPercent[0]);
    result.details.goldLoss = Math.min(Math.floor(account.gold * pct), picked.maxLoss);
    if (result.details.goldLoss < 1) result.details.goldLoss = 0;
  } else if (picked.id === "merchant") {
    var offer = MineData.shopItems[Math.floor(Math.random() * 2)];
    var discountPrice = Math.floor(offer.price * 0.5);
    result.details.offer = { name: offer.name, originalPrice: offer.price, price: discountPrice, effect: offer.effect, value: offer.value };
    account.pendingMerchant = { offer: result.details.offer, expireTime: Date.now() + 300000 };
  } else if (picked.id === "rich_vein") {
    result.details.multiplier = 2;
  } else if (picked.id === "mysterious_ore") {
    var higherArea = MineData.areas[Math.min(account.currentArea + 1, MineData.areas.length - 1)];
    var pick = higherArea.drops[Math.floor(Math.random() * higherArea.drops.length)];
    var res = MineData.resources[pick.id];
    result.details = { resourceId: pick.id, resourceName: res.name, resourceEmoji: res.emoji, count: 1 + Math.floor(Math.random() * 2) };
  }
  ensureNewFields(account);
  account.eventStats.total++;
  if (!account.eventStats.byType[picked.id]) account.eventStats.byType[picked.id] = 0;
  account.eventStats.byType[picked.id]++;
  return result;
}

function handleMerchantBuy(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureNewFields(account);
  if (!account.pendingMerchant || Date.now() > account.pendingMerchant.expireTime) {
    account.pendingMerchant = null; saveMineAccount(sender, account);
    replier.reply("[ 싱봇 광산 ] 떠돌이 상인이 없거나 시간이 만료되었습니다."); return;
  }
  var offer = account.pendingMerchant.offer;
  if (account.gold < offer.price) { replier.reply("[ 싱봇 광산 ] 골드 부족! (필요: " + offer.price + "G, 보유: " + account.gold + "G)"); return; }
  account.gold -= offer.price;
  var effectMsg = "";
  if (offer.effect === "stamina") { account.stamina = Math.min(account.maxStamina, account.stamina + offer.value); effectMsg = "체력 +" + offer.value; }
  else if (offer.effect === "luck") { if (!account.buffs.luck) account.buffs.luck = { uses: 0 }; account.buffs.luck.uses += offer.value; effectMsg = "행운 부적 " + offer.value + "회분"; }
  account.pendingMerchant = null; saveMineAccount(sender, account);
  replier.reply("[ 싱봇 광산 ] 🧙 상인에게 구매 완료!\n\n" + offer.name + " (" + offer.price + "G)\n" + effectMsg + "\n💰 잔여: " + account.gold + "G");
}

// ===== Feature 4: 칭호 시스템 =====

function getCodexCount(account) { var c = 0; for (var k in account.codex) { if (account.codex.hasOwnProperty(k) && account.codex[k]) c++; } return c; }

function checkTitleUnlocks(account) {
  ensureNewFields(account);
  var newTitles = [];
  for (var i = 0; i < MineData.titles.length; i++) {
    var title = MineData.titles[i];
    if (account.titles.unlocked.indexOf(title.id) >= 0) continue;
    var cond = title.condition; var val = 0;
    if (cond.type === "codexCount") val = getCodexCount(account);
    else if (cond.type === "gamblingTotal") val = account.gamblingStats ? account.gamblingStats.totalBet : 0;
    else if (cond.type === "questsCompleted") val = account.questsCompleted || 0;
    else val = account[cond.type] || 0;
    if (val >= cond.value) { account.titles.unlocked.push(title.id); newTitles.push(title); }
  }
  return newTitles;
}

function getTitleById(id) { for (var i = 0; i < MineData.titles.length; i++) { if (MineData.titles[i].id === id) return MineData.titles[i]; } return null; }
function getEquippedTitleDisplay(account) { ensureNewFields(account); if (!account.titles.equipped) return ""; var t = getTitleById(account.titles.equipped); return t ? t.emoji + " " + t.name : ""; }
function titleUnlockText(newTitles) { if (newTitles.length === 0) return ""; var t = "\n🏅 새 칭호 획득!\n"; for (var i = 0; i < newTitles.length; i++) t += "  " + newTitles[i].emoji + " " + newTitles[i].name + "\n"; return t; }

function handleTitleList(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureNewFields(account); checkTitleUnlocks(account); saveMineAccount(sender, account);
  var text = "[ 싱봇 광산 ] 🏅 칭호 목록\n━━━━━━━━━━━━━━━━━━\n";
  text += "🏷️ 장착중: " + (getEquippedTitleDisplay(account) || "없음") + "\n\n";
  for (var i = 0; i < MineData.titles.length; i++) { var t = MineData.titles[i]; text += (account.titles.unlocked.indexOf(t.id) >= 0 ? "✅ " : "🔒 ") + t.emoji + " " + t.name + " — " + t.desc + "\n"; }
  text += "\n'ㄱㅅ칭호장착 [칭호명]'으로 장착";
  replier.reply(text);
}

function handleTitleEquip(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureNewFields(account);
  var input = msg.replace(/^(광산|ㄱㅅ|ㄳ)칭호장착\s*/, "").trim();
  if (!input) { replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ칭호장착 [칭호명]\n해제: ㄱㅅ칭호장착 해제"); return; }
  if (input === "해제") { account.titles.equipped = null; saveMineAccount(sender, account); replier.reply("[ 싱봇 광산 ] 칭호를 해제했습니다."); return; }
  var found = null;
  for (var i = 0; i < MineData.titles.length; i++) { if (MineData.titles[i].name === input) { found = MineData.titles[i]; break; } }
  if (!found) { replier.reply("[ 싱봇 광산 ] 존재하지 않는 칭호입니다. 'ㄱㅅ칭호'로 확인하세요."); return; }
  if (account.titles.unlocked.indexOf(found.id) < 0) { replier.reply("[ 싱봇 광산 ] 미해금 칭호입니다.\n조건: " + found.desc); return; }
  account.titles.equipped = found.id; saveMineAccount(sender, account);
  replier.reply("[ 싱봇 광산 ] 🏷️ 칭호 장착!\n" + found.emoji + " " + found.name);
}

// ===== Feature 1: 도박장 =====

function handleGamble(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureNewFields(account);
  var args = msg.replace(/^(광산|ㄱㅅ|ㄳ)도박\s*/, "").trim().split(/\s+/);
  var amount = parseInt(args[0]); var choice = args[1] || null;
  if (!amount || isNaN(amount) || amount < MineData.gambling.minBet) {
    replier.reply("[ 싱봇 광산 ] 🎰 도박장\n━━━━━━━━━━━━━━━━━━\n🎲 홀짝: ㄱㅅ도박 [금액] [홀/짝]\n  → 승리 시 1.95배\n\n🎰 룰렛: ㄱㅅ도박 [금액]\n  → x0 ~ x5 배율\n\n최소 " + MineData.gambling.minBet + "G / 최대 " + MineData.gambling.maxBet + "G"); return;
  }
  if (amount > MineData.gambling.maxBet) amount = MineData.gambling.maxBet;
  if (account.gold < amount) { replier.reply("[ 싱봇 광산 ] 골드 부족! (필요: " + amount + "G, 보유: " + account.gold + "G)"); return; }
  var now = Date.now();
  if (now - account.gamblingStats.lastGambleTime < MineData.gambling.cooldownMs) { replier.reply("[ 싱봇 광산 ] " + Math.ceil((MineData.gambling.cooldownMs - (now - account.gamblingStats.lastGambleTime)) / 1000) + "초 후에 다시 도박할 수 있습니다."); return; }
  account.gold -= amount; account.gamblingStats.totalBet += amount; account.gamblingStats.lastGambleTime = now;
  var text = "";
  if (choice === "홀" || choice === "짝") {
    var dice = 1 + Math.floor(Math.random() * 6); var isOdd = dice % 2 === 1;
    var won = (choice === "홀" && isOdd) || (choice === "짝" && !isOdd);
    var payout = won ? Math.floor(amount * 1.95) : 0;
    text = "[ 싱봇 광산 ] 🎲 홀짝\n━━━━━━━━━━━━━━━━━━\n배팅: " + amount + "G | 선택: " + choice + "\n🎲 주사위: [" + dice + "] → " + (isOdd ? "홀" : "짝") + "!\n\n";
    if (won) { account.gold += payout; account.gamblingStats.totalWon += payout; text += "🎉 승리! +" + payout + "G (1.95배)\n"; }
    else { account.gamblingStats.totalLost += amount; text += "😢 패배! -" + amount + "G\n"; }
  } else {
    var slots = MineData.gambling.roulette; var tw = 0;
    for (var i = 0; i < slots.length; i++) tw += slots[i].weight;
    var r = Math.random() * tw; var slot = slots[0];
    for (var i = 0; i < slots.length; i++) { r -= slots[i].weight; if (r <= 0) { slot = slots[i]; break; } }
    var payout = Math.floor(amount * slot.multiplier);
    text = "[ 싱봇 광산 ] 🎰 배율 룰렛\n━━━━━━━━━━━━━━━━━━\n배팅: " + amount + "G\n🎰 결과: " + slot.label + " (x" + slot.multiplier + ")\n\n";
    if (payout > 0) { account.gold += payout; if (payout > amount) { account.gamblingStats.totalWon += payout; text += "🎉 +" + payout + "G!\n"; } else { account.gamblingStats.totalLost += (amount - payout); text += "😐 " + payout + "G 회수 (-" + (amount - payout) + "G)\n"; } }
    else { account.gamblingStats.totalLost += amount; text += "💀 전액 잃었습니다! -" + amount + "G\n"; }
  }
  text += "💰 보유: " + account.gold + "G";
  var newTitles = checkTitleUnlocks(account); text += titleUnlockText(newTitles);
  saveMineAccount(sender, account); replier.reply(text);
}

function handleGambleStats(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureNewFields(account); var s = account.gamblingStats; var net = s.totalWon - s.totalLost;
  replier.reply("[ 싱봇 광산 ] 🎰 도박 기록\n━━━━━━━━━━━━━━━━━━\n총 배팅: " + s.totalBet + "G\n총 수익: " + s.totalWon + "G\n총 손실: " + s.totalLost + "G\n순이익: " + (net >= 0 ? "+" : "") + net + "G");
}

// ===== Feature 2: 일일퀘스트 =====

function seededRandom(seed) { return (seed * 1103515245 + 12345) & 0x7fffffff; }

function generateDailyQuests(account) {
  ensureNewFields(account); var today = todaySeed();
  if (account.dailyQuests.date === today) return;
  var seed = today + hashCode(account.sender || account.name);
  var types = MineData.dailyQuests.types; var questCount = MineData.dailyQuests.questsPerDay;
  var indices = []; for (var i = 0; i < types.length; i++) indices.push(i);
  for (var i = indices.length - 1; i > 0; i--) { seed = seededRandom(seed); var j = (seed >>> 0) % (i + 1); var tmp = indices[i]; indices[i] = indices[j]; indices[j] = tmp; }
  var quests = [];
  for (var q = 0; q < questCount && q < indices.length; q++) {
    var type = types[indices[q]];
    seed = seededRandom(seed); var paramVal = type.paramRange[0] + ((seed >>> 0) % (type.paramRange[1] - type.paramRange[0] + 1));
    seed = seededRandom(seed); var baseReward = type.rewardGold[0] + ((seed >>> 0) % (type.rewardGold[1] - type.rewardGold[0] + 1));
    var tierIdx = getPickaxeInfo(account.pickaxeLevel).tierIndex; var tierMult = MineData.dailyQuests.tierMultiplier[tierIdx] || 1;
    var rewardGold = Math.floor(baseReward * tierMult);
    var desc = type.desc.replace("{n}", paramVal); var resourceName = null;
    if (type.id === "collect_resource") { var area = MineData.areas[account.currentArea]; seed = seededRandom(seed); var res = MineData.resources[area.drops[(seed >>> 0) % area.drops.length].id]; resourceName = res.name; desc = desc.replace("{resource}", resourceName); }
    quests.push({ typeId: type.id, desc: desc, target: paramVal, progress: 0, resourceName: resourceName, rewardGold: rewardGold, completed: false, claimed: false });
  }
  account.dailyQuests = { date: today, quests: quests, allClaimed: false };
}

function checkQuestProgress(account, mineResult) {
  ensureNewFields(account); generateDailyQuests(account);
  var quests = account.dailyQuests.quests;
  for (var i = 0; i < quests.length; i++) {
    var q = quests[i]; if (q.completed) continue;
    if (q.typeId === "mine_count") q.progress++;
    else if (q.typeId === "collect_resource" && mineResult.drops) { for (var j = 0; j < mineResult.drops.length; j++) { if (mineResult.drops[j].name === q.resourceName) q.progress += mineResult.drops[j].count; } }
    else if (q.typeId === "earn_gold") q.progress += mineResult.gold || 0;
    else if (q.typeId === "reach_depth") { if (account.depth > q.progress) q.progress = account.depth; }
    if (q.progress >= q.target) q.completed = true;
  }
}

function handleDailyQuest(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureNewFields(account); generateDailyQuests(account); saveMineAccount(sender, account);
  var quests = account.dailyQuests.quests;
  var text = "[ 싱봇 광산 ] 📋 오늘의 퀘스트\n━━━━━━━━━━━━━━━━━━\n\n"; var allClaimed = true;
  for (var i = 0; i < quests.length; i++) {
    var q = quests[i]; var prog = Math.min(q.progress, q.target);
    var bar = ""; var filled = Math.floor(prog / q.target * 10);
    for (var b = 0; b < 10; b++) bar += b < filled ? "■" : "□";
    text += (i + 1) + "️⃣ " + q.desc + "\n   [" + bar + "] " + prog + "/" + q.target;
    if (q.claimed) text += " ✅ 수령완료"; else if (q.completed) { text += " ✅"; allClaimed = false; } else allClaimed = false;
    text += "\n   보상: 💰 " + q.rewardGold + "G\n\n";
  }
  text += "🎁 올클리어: 💰 " + MineData.dailyQuests.completionBonusGold + "G + 🌟 " + MineData.dailyQuests.completionBonusStarFragments + "\n";
  if (!allClaimed) text += "'ㄱㅅ퀘스트보상'으로 보상 수령";
  replier.reply(text);
}

function handleDailyQuestClaim(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureNewFields(account); generateDailyQuests(account);
  var quests = account.dailyQuests.quests; var claimed = 0;
  var text = "[ 싱봇 광산 ] 📋 퀘스트 보상\n━━━━━━━━━━━━━━━━━━\n";
  for (var i = 0; i < quests.length; i++) { var q = quests[i]; if (q.completed && !q.claimed) { q.claimed = true; account.gold += q.rewardGold; account.totalGoldEarned += q.rewardGold; account.questsCompleted = (account.questsCompleted || 0) + 1; claimed++; text += "✅ " + q.desc + " → +" + q.rewardGold + "G\n"; } }
  if (claimed === 0) { replier.reply("[ 싱봇 광산 ] 수령할 보상이 없습니다!"); return; }
  var allClaimed = true; for (var i = 0; i < quests.length; i++) { if (!quests[i].claimed) { allClaimed = false; break; } }
  if (allClaimed && !account.dailyQuests.allClaimed) { account.dailyQuests.allClaimed = true; var tierIdx = getPickaxeInfo(account.pickaxeLevel).tierIndex; var tierMult = MineData.dailyQuests.tierMultiplier[tierIdx] || 1; var bonusGold = Math.floor(MineData.dailyQuests.completionBonusGold * tierMult); account.gold += bonusGold; account.totalGoldEarned += bonusGold; account.starFragments += MineData.dailyQuests.completionBonusStarFragments; text += "\n🎁 올클리어!\n  💰 +" + bonusGold + "G + 🌟 +" + MineData.dailyQuests.completionBonusStarFragments + "\n"; }
  text += "\n💰 보유: " + account.gold + "G";
  var newTitles = checkTitleUnlocks(account); text += titleUnlockText(newTitles);
  saveMineAccount(sender, account); replier.reply(text);
}

// --- 핵심: 채굴 ---

function doMine(account) {
  regenStamina(account);
  if (account.stamina <= 0) {
    var nextRegen = Math.ceil((MineData.config.STAMINA_REGEN_MS - (Date.now() - account.lastStaminaRegen)) / 60000);
    return { success: false, msg: "체력이 부족합니다! " + nextRegen + "분 후 회복.\n체력: 0/" + account.maxStamina };
  }

  account.stamina--;
  account.lastActiveTime = Date.now();
  account.totalMined++;

  var area = MineData.areas[account.currentArea];
  var drops = [];
  var newDiscoveries = [];

  // 행운 보너스
  var luckBonus = account.prestigeBonuses.luck * 0.05;
  luckBonus += getCodexBonus(account, "luck");
  luckBonus += getEquipLuckBonus(account);
  luckBonus += getGuildBonus(account).luck;
  if (account.buffs.luck && account.buffs.luck.uses > 0) {
    luckBonus += 0.30;
    account.buffs.luck.uses--;
    if (account.buffs.luck.uses <= 0) delete account.buffs.luck;
  }

  // 드롭 롤
  for (var i = 0; i < area.drops.length; i++) {
    var drop = area.drops[i];
    if (Math.random() < drop.rate + luckBonus) {
      var amount = drop.min + Math.floor(Math.random() * (drop.max - drop.min + 1));
      var powerMultiplier = 1 + account.prestigeBonuses.miningPower * 0.10;
      amount = Math.max(1, Math.floor(amount * powerMultiplier));
      var resourceName = MineData.resources[drop.id].name;
      addResource(account, resourceName, amount);
      drops.push({ name: resourceName, count: amount, emoji: MineData.resources[drop.id].emoji });

      // 도감 발견
      if (!account.codex[drop.id]) {
        account.codex[drop.id] = true;
        newDiscoveries.push(resourceName);
      }
    }
  }

  // 골드
  var goldEarned = area.goldRange[0] + Math.floor(Math.random() * (area.goldRange[1] - area.goldRange[0] + 1));
  account.gold += goldEarned;
  account.totalGoldEarned += goldEarned;

  // 깊이 진행
  var oldDepth = account.depth;
  var depthGain = MineData.config.DEPTH_GAIN_MIN +
    Math.floor(Math.random() * (MineData.config.DEPTH_GAIN_MAX - MineData.config.DEPTH_GAIN_MIN + 1));
  depthGain += getEquipDepthBonus(account);
  account.depth += depthGain;

  // 지역 최대 깊이 제한
  var maxAreaDepth = area.depthRange[1];
  if (account.depth > maxAreaDepth) account.depth = maxAreaDepth;

  if (account.depth > account.maxDepthReached) account.maxDepthReached = account.depth;

  // 보스 체크
  var bossResult = checkDepthBoss(account, oldDepth, account.depth);

  // 지역 전환 체크
  checkAreaUnlock(account);

  // 채굴 이벤트
  ensureNewFields(account);
  var miningEvent = rollMiningEvent(account, area);
  if (miningEvent) {
    if (miningEvent.type === "rich_vein") { for (var i = 0; i < drops.length; i++) { addResource(account, drops[i].name, drops[i].count); drops[i].count *= 2; } }
    else if (miningEvent.type === "trap") { account.gold -= miningEvent.details.goldLoss; if (account.gold < 0) account.gold = 0; }
    else if (miningEvent.type === "treasure_chest") { account.gold += miningEvent.details.bonusGold; account.totalGoldEarned += miningEvent.details.bonusGold; }
    else if (miningEvent.type === "mysterious_ore") {
      addResource(account, miningEvent.details.resourceName, miningEvent.details.count);
      drops.push({ name: miningEvent.details.resourceName, count: miningEvent.details.count, emoji: miningEvent.details.resourceEmoji });
      if (!account.codex[miningEvent.details.resourceId]) { account.codex[miningEvent.details.resourceId] = true; newDiscoveries.push(miningEvent.details.resourceName); }
    }
  }

  var mineResult = { success: true, drops: drops, gold: goldEarned, depthGain: account.depth - oldDepth, newDiscoveries: newDiscoveries, bossResult: bossResult, miningEvent: miningEvent };
  checkQuestProgress(account, mineResult);
  return mineResult;
}

// ===== 커맨드 핸들러 =====

function handleMineRegister(room, msg, sender, replier) {
  var existing = loadMineAccount(sender);
  if (existing) {
    replier.reply("[ 싱봇 광산 ] 이미 등록된 광부입니다!\n광부명: " + existing.name + "\n'채굴'로 바로 시작하세요.");
    return;
  }
  var name = msg.replace(/^(광산|ㄱㅅ|ㄳ)등록\s*/, "").trim();
  if (!name || name.length < 1 || name.length > 10) {
    replier.reply("[ 싱봇 광산 ]\n\n사용법: ㄱㅅ등록 [광부이름]\n예) ㄱㅅ등록 뚱이\n\n이름은 1~10자");
    return;
  }
  var account = createMineAccount(sender, name);
  saveMineAccount(sender, account);
  replier.reply(
    "[ 싱봇 광산 ] ⛏️ 광부 등록 완료!\n\n" +
    "광부명: " + name + "\n" +
    "곡괭이: 나무 곡괭이 Lv.1\n" +
    "시작 골드: " + account.gold + "G\n" +
    "체력: " + account.stamina + "/" + account.maxStamina + "\n\n" +
    "'채굴'로 광물을 캐세요!\n" +
    "'ㄱㅅ도움말'로 명령어를 확인하세요."
  );
}

function handleMine(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) {
    replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!");
    return;
  }

  // 방치 수입
  var idle = calculateIdleIncome(account);
  var idleMsg = "";
  if (idle) {
    addResource(account, idle.resource, idle.amount);
    idleMsg = "💤 방치 수입 (" + idle.minutes + "분): " + idle.resource + " +" + idle.amount + "\n\n";
  }

  var result = doMine(account);
  saveMineAccount(sender, account);

  if (!result.success) {
    replier.reply("[ 싱봇 광산 ]\n\n" + idleMsg + result.msg);
    return;
  }

  var area = MineData.areas[account.currentArea];
  var text = "[ 싱봇 광산 ] ⛏️ 채굴!\n\n";
  if (idleMsg) text += idleMsg;

  text += area.emoji + " " + area.name + " | 깊이 " + account.depth;
  if (result.depthGain > 0) text += " (+" + result.depthGain + ")";
  text += "\n";
  text += "체력: " + account.stamina + "/" + account.maxStamina + "\n";
  text += "━━━━━━━━━━━━━━━━━━\n";

  // 드롭
  if (result.drops.length > 0) {
    for (var i = 0; i < result.drops.length; i++) {
      var d = result.drops[i];
      text += d.emoji + " " + d.name + " x" + d.count + "\n";
    }
  } else {
    text += "이번엔 빈 광맥이었습니다...\n";
  }

  text += "💰 +" + result.gold + "G (보유: " + account.gold + "G)\n";

  // 새 발견
  if (result.newDiscoveries.length > 0) {
    text += "\n🆕 새로운 광물 발견!\n";
    for (var i = 0; i < result.newDiscoveries.length; i++) {
      text += "  → " + result.newDiscoveries[i] + " (도감 등록!)\n";
    }
  }

  // 보스
  if (result.bossResult) {
    text += "\n";
    if (result.bossResult.defeated) {
      text += "⚔️ [" + result.bossResult.boss.name + "] 격파!\n";
      text += "보상: " + result.bossResult.gold + "G\n";
    } else {
      text += "🚫 [" + result.bossResult.boss.name + "] 등장!\n";
      text += "필요 파워: " + result.bossResult.requiredPower + " (현재: " + result.bossResult.currentPower + ")\n";
      text += "곡괭이를 강화하고 다시 도전하세요!";
    }
  }

  // 채굴 이벤트 표시
  if (result.miningEvent) {
    var ev = result.miningEvent;
    text += "\n━━━ " + ev.emoji + " " + ev.name + " ━━━\n";
    if (ev.type === "treasure_chest") text += "보물 발견! 💰 +" + ev.details.bonusGold + "G\n";
    else if (ev.type === "trap") text += "골드 -" + ev.details.goldLoss + "G! 조심하세요!\n";
    else if (ev.type === "merchant") text += "\"" + ev.details.offer.name + "을(를) " + ev.details.offer.price + "G에 팔지...\"\n(원가 " + ev.details.offer.originalPrice + "G → 50% 할인!)\n'ㄱㅅ상인구매'로 구매 (5분 제한)\n";
    else if (ev.type === "rich_vein") text += "모든 드롭이 2배!\n";
    else if (ev.type === "mysterious_ore") text += ev.details.resourceEmoji + " " + ev.details.resourceName + " x" + ev.details.count + " 획득!\n";
  }

  // 칭호 체크
  ensureNewFields(account);
  var newTitles = checkTitleUnlocks(account);
  text += titleUnlockText(newTitles);

  replier.reply(text);
}

function handleMineInfo(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) {
    replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!");
    return;
  }
  regenStamina(account);

  var tierInfo = getPickaxeInfo(account.pickaxeLevel);
  var power = getPickaxePower(account);
  var area = MineData.areas[account.currentArea];
  var codexCount = getCodexCount(account);
  var totalResources = Object.keys(MineData.resources).length;

  ensureNewFields(account);
  var text = "[ 싱봇 광산 ] " + account.name + "\n";
  var titleDisp = getEquippedTitleDisplay(account);
  if (titleDisp) text += "🏷️ " + titleDisp + "\n";
  text += "━━━━━━━━━━━━━━━━━━\n";
  text += "⛏️ " + tierInfo.tier.emoji + " " + tierInfo.tier.name + " Lv." + account.pickaxeLevel + "\n";
  text += "   파워: " + power + "\n";
  text += "💰 골드: " + account.gold + "G\n";
  text += "❤️ 체력: " + account.stamina + "/" + account.maxStamina + "\n";
  text += area.emoji + " " + area.name + " (깊이 " + account.depth + ")\n";
  text += "📖 도감: " + codexCount + "/" + totalResources + "\n";
  text += "⛏️ 총 채굴: " + account.totalMined + "회\n";
  if (account.prestigeCount > 0) {
    text += "⭐ 프레스티지: " + account.prestigeCount + "회\n";
    text += "🌟 별의 파편: " + account.starFragments + "\n";
  }

  replier.reply(text);
}

function handlePickaxeUpgrade(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) {
    replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!");
    return;
  }

  if (account.pickaxeLevel >= 40) {
    replier.reply("[ 싱봇 광산 ] 곡괭이가 이미 최대 레벨입니다! (Lv.40)");
    return;
  }

  var cost = getUpgradeCost(account.pickaxeLevel);
  var currentTier = getPickaxeInfo(account.pickaxeLevel);
  var nextLevel = account.pickaxeLevel + 1;
  var nextTier = getPickaxeInfo(nextLevel);

  // 바로 강화 실행 (자원 부족 시 부족 메시지)
  if (account.gold < cost.gold) {
    replier.reply("[ 싱봇 광산 ] 골드가 부족합니다! (필요: " + cost.gold + "G, 보유: " + account.gold + "G)");
    return;
  }
  for (var i = 0; i < cost.resources.length; i++) {
    var r = cost.resources[i];
    if (getResourceCount(account, r.name) < r.count) {
      replier.reply("[ 싱봇 광산 ] " + r.name + "이(가) 부족합니다! (필요: " + r.count + ", 보유: " + getResourceCount(account, r.name) + ")");
      return;
    }
  }

  // 비용 차감
  account.gold -= cost.gold;
  for (var i = 0; i < cost.resources.length; i++) {
    removeResource(account, cost.resources[i].name, cost.resources[i].count);
  }

  // 강화 성공 판정
  var successRate = MineData.config.UPGRADE_SUCCESS_RATE[currentTier.tierIndex];
  if (successRate === undefined) successRate = 1.0;
  var succeeded = Math.random() < successRate;

  if (!succeeded) {
    saveMineAccount(sender, account);
    replier.reply(
      "[ 싱봇 광산 ] 💔 강화 실패!\n\n" +
      currentTier.tier.emoji + " Lv." + account.pickaxeLevel + " 유지\n" +
      "성공률: " + Math.round(successRate * 100) + "%\n" +
      "자원과 골드가 소모되었습니다.\n" +
      "💰 잔여: " + account.gold + "G"
    );
    return;
  }

  account.pickaxeLevel = nextLevel;
  saveMineAccount(sender, account);

  var newPower = getPickaxePower(account);

  // 티어 변경 시 새 곡괭이 이미지 전송
  if (currentTier.tierIndex !== nextTier.tierIndex) {
    var imgUrl = getPickaxeImageUrl(nextTier.tierIndex);
    if (imgUrl) replier.reply(imgUrl);
  }

  var text = "[ 싱봇 광산 ] ⛏️ 강화 성공!\n\n";
  if (currentTier.tierIndex !== nextTier.tierIndex) {
    text += "🎉 " + nextTier.tier.name + " 획득!\n";
  }
  text += nextTier.tier.emoji + " Lv." + nextLevel + " | 파워: " + newPower + "\n";
  if (successRate < 1.0) text += "성공률: " + Math.round(successRate * 100) + "%\n";
  text += "💰 잔여 골드: " + account.gold + "G";
  replier.reply(text);
}

function getPickaxeImageUrl(tierIndex) {
  var tier = MineData.pickaxeTiers[tierIndex];
  if (!tier || !tier.image) return null;
  return MineData.pickaxeImageBase + "/mine/" + tier.image + ".html?v=2";
}

function handlePickaxeInfo(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) {
    replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!");
    return;
  }

  var tierInfo = getPickaxeInfo(account.pickaxeLevel);
  var power = getPickaxePower(account);
  var tier = tierInfo.tier;

  // OG 미리보기 이미지 전송
  var imageUrl = getPickaxeImageUrl(tierInfo.tierIndex);
  if (imageUrl) replier.reply(imageUrl);

  var text = "[ 싱봇 광산 ] 곡괭이 정보\n";
  text += "━━━━━━━━━━━━━━━━━━\n";
  text += tier.emoji + " " + tier.name + "\n";
  text += "레벨: " + account.pickaxeLevel + " / 40\n";
  text += "파워: " + power + "\n";
  text += "티어: " + (tierInfo.tierIndex + 1) + " / " + MineData.pickaxeTiers.length + "\n";

  // 다음 레벨 비용
  if (account.pickaxeLevel < 40) {
    var cost = getUpgradeCost(account.pickaxeLevel);
    text += "\n다음 강화 비용:\n";
    text += "  💰 " + cost.gold + "G\n";
    for (var i = 0; i < cost.resources.length; i++) {
      text += "  " + cost.resources[i].name + " x" + cost.resources[i].count + "\n";
    }
  }

  replier.reply(text);
}

function handleMineInventory(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) {
    replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!");
    return;
  }

  var text = "[ 싱봇 광산 ] " + account.name + "의 인벤토리\n";
  text += "━━━━━━━━━━━━━━━━━━\n";

  if (account.resources.length === 0 && account.items.length === 0) {
    text += "텅 비었습니다!";
    replier.reply(text);
    return;
  }

  if (account.resources.length > 0) {
    text += "📦 광물:\n";
    for (var i = 0; i < account.resources.length; i++) {
      var r = account.resources[i];
      var id = MineData.resourceIdMap[r.name];
      var emoji = id ? MineData.resources[id].emoji : "•";
      text += "  " + emoji + " " + r.name + " x" + r.count + "\n";
    }
  }

  if (account.items.length > 0) {
    text += "\n🎒 아이템:\n";
    for (var i = 0; i < account.items.length; i++) {
      text += "  • " + account.items[i].name + " x" + account.items[i].count + "\n";
    }
  }

  text += "\n💰 골드: " + account.gold + "G";
  replier.reply(text);
}

function handleMineCodex(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) {
    replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!");
    return;
  }

  var totalResources = 0;
  var codexCount = getCodexCount(account);
  var text = "[ 싱봇 광산 ] 광물 도감\n";
  text += "━━━━━━━━━━━━━━━━━━\n";

  var tierNames = ["Common", "Uncommon", "Rare"];
  for (var t = 1; t <= 3; t++) {
    var tierLabel = t === 1 ? "⬜ Common" : (t === 2 ? "🟦 Uncommon" : "🟪 Rare");
    text += "\n" + tierLabel + "\n";
    for (var id in MineData.resources) {
      if (MineData.resources.hasOwnProperty(id) && MineData.resources[id].tier === t) {
        totalResources++;
        var res = MineData.resources[id];
        if (account.codex[id]) {
          text += "  " + res.emoji + " " + res.name + " [발견]\n";
        } else {
          text += "  ❓ ??? [미발견]\n";
        }
      }
    }
  }

  text += "\n진행: " + codexCount + "/" + totalResources + "\n";

  // 보상 표시
  text += "\n🎁 보상:\n";
  for (var i = 0; i < MineData.codexRewards.length; i++) {
    var reward = MineData.codexRewards[i];
    var achieved = codexCount >= reward.threshold;
    text += "  " + (achieved ? "✅" : "⬜") + " " + reward.threshold + "종 — " + reward.desc + "\n";
  }

  replier.reply(text);
}

function handleMineShop(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) {
    replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!");
    return;
  }

  var text = "[ 싱봇 광산 ] 상점\n";
  text += "━━━━━━━━━━━━━━━━━━\n";
  text += "💰 보유: " + account.gold + "G\n\n";

  for (var i = 0; i < MineData.shopItems.length; i++) {
    var item = MineData.shopItems[i];
    text += (i + 1) + ". " + item.name + " — " + item.price + "G\n";
    text += "   " + item.desc + "\n";
  }

  text += "\n'ㄱㅅ구매 [아이템명]'으로 구매";
  replier.reply(text);
}

function handleMineBuy(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) {
    replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!");
    return;
  }

  var itemName = msg.replace(/^(광산|ㄱㅅ|ㄳ)구매\s*/, "").trim();
  if (!itemName) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ구매 [아이템명]\n예) ㄱㅅ구매 에너지 음료");
    return;
  }

  var shopItem = null;
  for (var i = 0; i < MineData.shopItems.length; i++) {
    if (MineData.shopItems[i].name === itemName) {
      shopItem = MineData.shopItems[i];
      break;
    }
  }

  if (!shopItem) {
    replier.reply("[ 싱봇 광산 ] 상점에 없는 아이템입니다. 'ㄱㅅ상점'으로 목록을 확인하세요.");
    return;
  }

  if (account.gold < shopItem.price) {
    replier.reply("[ 싱봇 광산 ] 골드가 부족합니다! (필요: " + shopItem.price + "G, 보유: " + account.gold + "G)");
    return;
  }

  account.gold -= shopItem.price;

  // 효과 적용
  var effectMsg = "";
  if (shopItem.effect === "stamina") {
    account.stamina = Math.min(account.maxStamina, account.stamina + shopItem.value);
    effectMsg = "체력 +" + shopItem.value + " (현재: " + account.stamina + "/" + account.maxStamina + ")";
  } else if (shopItem.effect === "luck") {
    account.buffs.luck = { uses: shopItem.value };
    effectMsg = "희귀 드롭률 +30% (채굴 " + shopItem.value + "회)";
  } else if (shopItem.effect === "depth") {
    account.depth += shopItem.value;
    if (account.depth > account.maxDepthReached) account.maxDepthReached = account.depth;
    checkAreaUnlock(account);
    effectMsg = "깊이 +" + shopItem.value + " (현재: " + account.depth + ")";
  } else if (shopItem.effect === "idle") {
    account.buffs.idle = { expires: Date.now() + shopItem.value * 60000 };
    effectMsg = "방치 수입 +50% (" + Math.floor(shopItem.value / 60) + "시간)";
  }

  saveMineAccount(sender, account);
  replier.reply("[ 싱봇 광산 ] 구매 완료!\n\n" + shopItem.name + " 사용!\n" + effectMsg + "\n💰 잔여: " + account.gold + "G");
}

function handleMineSell(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) {
    replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!");
    return;
  }

  var input = msg.replace(/^(광산|ㄱㅅ|ㄳ)판매\s*/, "").trim();
  if (!input) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ판매 [자원명] [수량]\n예) ㄱㅅ판매 돌 10\n전부 판매: ㄱㅅ판매 돌 전부");
    return;
  }

  // "자원명 수량" 또는 "자원명 전부"
  var parts = input.split(/\s+/);
  var resourceName = parts[0];
  var sellAll = parts.length > 1 && parts[parts.length - 1] === "전부";
  var count = sellAll ? 0 : (parts.length > 1 ? parseInt(parts[parts.length - 1]) : 0);

  var id = MineData.resourceIdMap[resourceName];
  if (!id) {
    replier.reply("[ 싱봇 광산 ] 알 수 없는 자원입니다: " + resourceName);
    return;
  }

  var has = getResourceCount(account, resourceName);
  if (has <= 0) {
    replier.reply("[ 싱봇 광산 ] " + resourceName + "을(를) 보유하고 있지 않습니다.");
    return;
  }

  if (sellAll) count = has;
  if (count <= 0 || isNaN(count)) {
    replier.reply("[ 싱봇 광산 ] 수량을 입력해주세요.\n예) ㄱㅅ판매 돌 10");
    return;
  }
  if (count > has) count = has;

  var unitPrice = MineData.resources[id].sellPrice;
  var totalGold = unitPrice * count;

  removeResource(account, resourceName, count);
  account.gold += totalGold;
  account.totalGoldEarned += totalGold;
  saveMineAccount(sender, account);

  replier.reply(
    "[ 싱봇 광산 ] 판매 완료!\n\n" +
    MineData.resources[id].emoji + " " + resourceName + " x" + count + " → " + totalGold + "G\n" +
    "(개당 " + unitPrice + "G)\n" +
    "💰 보유: " + account.gold + "G"
  );
}

function handleMineHelp(room, msg, sender, replier) {
  replier.reply(
    "[ 싱봇 광산 도움말 ] ⛏️\n\n" +
    "* ㄱㅅ / ㄳ 둘 다 가능\n\n" +
    "기본:\n" +
    "  ㄱㅅ등록 [이름] — 광부 등록\n" +
    "  채굴 — 광물 캐기\n" +
    "  ㄱㅅ정보 — 내 정보\n\n" +
    "장비:\n" +
    "  곡괭이강화 — 곡괭이 업그레이드\n" +
    "  곡괭이정보 — 곡괭이 상세\n" +
    "  ㄱㅅ장비 — 장착 장비 확인\n" +
    "  ㄱㅅ제작목록 — 제작 가능 목록\n" +
    "  ㄱㅅ제작 [장비명] — 장비 제작\n" +
    "  ㄱㅅ해제 [슬롯] — 장비 해제\n\n" +
    "자원:\n" +
    "  ㄱㅅ인벤 — 보유 자원\n" +
    "  ㄱㅅ도감 — 광물 도감\n\n" +
    "경제:\n" +
    "  ㄱㅅ상점 — NPC 상점\n" +
    "  ㄱㅅ구매 [아이템] — 상점 구매\n" +
    "  ㄱㅅ판매 [자원] [수량] — NPC 판매\n" +
    "  ㄱㅅ시장 — 오늘의 시세 (매일 변동)\n" +
    "  ㄱㅅ시장판매 [자원] [수량] — 시장가 판매\n\n" +
    "거래:\n" +
    "  ㄱㅅ거래신청 [상대] — 거래 요청\n" +
    "  ㄱㅅ거래수락/거절 — 응답\n" +
    "  ㄱㅅ거래올리기 [자원] [수량]\n" +
    "  ㄱㅅ거래골드 [수량]\n" +
    "  ㄱㅅ거래확인/취소\n\n" +
    "환생:\n" +
    "  ㄱㅅ환생 — 프레스티지 (깊이500+)\n" +
    "  ㄱㅅ환생상점 — 영구 보너스 구매\n" +
    "  ㄱㅅ환생구매 [보너스명]\n\n" +
    "대결:\n" +
    "  ㄱㅅ대결 [상대] — PvP 채굴 대결\n" +
    "  ㄱㅅ대결수락/거절\n" +
    "  ㄱㅅ캐기 — 대결 중 채굴\n" +
    "  ㄱㅅ항복 — 대결 포기\n" +
    "  ㄱㅅ랭킹 — 랭킹 보기\n\n" +
    "길드:\n" +
    "  ㄱㅅ길드생성 [이름] — 길드 생성\n" +
    "  ㄱㅅ길드가입 [길드명] — 가입 신청\n" +
    "  ㄱㅅ길드수락 [이름] — 가입 수락\n" +
    "  ㄱㅅ길드채굴 — 길드 광산 채굴\n" +
    "  ㄱㅅ길드정보 — 길드 정보\n" +
    "  ㄱㅅ길드탈퇴 — 길드 탈퇴\n\n" +
    "도박:\n" +
    "  ㄱㅅ도박 [금액] — 배율 룰렛\n" +
    "  ㄱㅅ도박 [금액] [홀/짝] — 홀짝\n" +
    "  ㄱㅅ도박기록 — 도박 통계\n\n" +
    "퀘스트:\n" +
    "  ㄱㅅ퀘스트 — 일일퀘스트 확인\n" +
    "  ㄱㅅ퀘스트보상 — 보상 수령\n\n" +
    "칭호:\n" +
    "  ㄱㅅ칭호 — 칭호 목록\n" +
    "  ㄱㅅ칭호장착 [이름] — 칭호 장착"
  );
}

// ===== 장비 시스템 =====

function handleMineEquipInfo(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureEquipment(account);

  var text = "[ 싱봇 광산 ] " + account.name + "의 장비\n";
  text += "━━━━━━━━━━━━━━━━━━\n";

  var slots = ["helmet", "lamp", "boots"];
  var slotNames = { helmet: "안전모", lamp: "램프", boots: "장화" };
  for (var s = 0; s < slots.length; s++) {
    var slot = slots[s];
    var equipped = account.equipment[slot];
    text += "\n" + slotNames[slot] + ": ";
    if (equipped) {
      var data = getEquippedData(account, slot);
      text += (data ? data.emoji + " " : "") + equipped.name + " (Tier " + equipped.tier + ")\n";
      if (data) {
        if (data.staminaBonus) text += "  → 체력 +" + data.staminaBonus + "\n";
        if (data.pvpDef) text += "  → 방어 +" + data.pvpDef + "\n";
        if (data.luckBonus) text += "  → 행운 +" + Math.round(data.luckBonus * 100) + "%\n";
        if (data.idleBonus) text += "  → 방치 +" + Math.round(data.idleBonus * 100) + "%\n";
        if (data.depthBonus) text += "  → 깊이 +" + data.depthBonus + "\n";
      }
    } else {
      text += "없음\n";
    }
  }

  text += "\n'ㄱㅅ제작 [장비명]'으로 제작\n'ㄱㅅ제작목록'으로 제작 가능 목록";
  replier.reply(text);

  // 장착 중인 장비 이미지 전송
  for (var s = 0; s < slots.length; s++) {
    var data = getEquippedData(account, slots[s]);
    if (data && data.image) {
      replier.reply(MineData.pickaxeImageBase + "/mine/" + data.image + ".html?v=2");
    }
  }
}

function handleMineCraftList(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  var text = "[ 싱봇 광산 ] 제작 목록\n";
  text += "━━━━━━━━━━━━━━━━━━\n";

  var slots = ["helmet", "lamp", "boots"];
  var slotNames = { helmet: "🪖 안전모", lamp: "🔦 램프", boots: "🥾 장화" };
  for (var s = 0; s < slots.length; s++) {
    var slot = slots[s];
    text += "\n" + slotNames[slot] + ":\n";
    var items = MineData.equipment[slot];
    for (var i = 0; i < items.length; i++) {
      var item = items[i];
      text += "  " + item.emoji + " " + item.name + " (T" + item.tier + ")\n";
      text += "    ";
      if (item.staminaBonus) text += "체력+" + item.staminaBonus + " ";
      if (item.pvpDef) text += "방어+" + item.pvpDef + " ";
      if (item.luckBonus) text += "행운+" + Math.round(item.luckBonus * 100) + "% ";
      if (item.idleBonus) text += "방치+" + Math.round(item.idleBonus * 100) + "% ";
      if (item.depthBonus) text += "깊이+" + item.depthBonus + " ";
      text += "\n";
      text += "    💰 " + item.cost.gold + "G";
      for (var r = 0; r < item.cost.resources.length; r++) {
        text += " + " + item.cost.resources[r].name + " x" + item.cost.resources[r].count;
      }
      text += "\n";
    }
  }

  text += "\n'ㄱㅅ제작 [장비명]'으로 제작";
  replier.reply(text);
}

function handleMineCraft(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureEquipment(account);

  var input = msg.replace(/^(광산|ㄱㅅ|ㄳ)제작\s*/, "").trim();
  if (!input) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ제작 [장비명]\n예) ㄱㅅ제작 가죽 안전모\n\n'ㄱㅅ제작목록'으로 목록 확인");
    return;
  }

  // 장비 찾기
  var targetItem = null;
  var targetSlot = null;
  var slots = ["helmet", "lamp", "boots"];
  for (var s = 0; s < slots.length; s++) {
    var items = MineData.equipment[slots[s]];
    for (var i = 0; i < items.length; i++) {
      if (items[i].name === input) {
        targetItem = items[i];
        targetSlot = slots[s];
        break;
      }
    }
    if (targetItem) break;
  }

  if (!targetItem) {
    replier.reply("[ 싱봇 광산 ] 알 수 없는 장비: " + input + "\n'ㄱㅅ제작목록'으로 확인하세요.");
    return;
  }

  // 비용 확인
  if (account.gold < targetItem.cost.gold) {
    replier.reply("[ 싱봇 광산 ] 골드 부족! (필요: " + targetItem.cost.gold + "G, 보유: " + account.gold + "G)");
    return;
  }
  for (var r = 0; r < targetItem.cost.resources.length; r++) {
    var req = targetItem.cost.resources[r];
    if (getResourceCount(account, req.name) < req.count) {
      replier.reply("[ 싱봇 광산 ] " + req.name + " 부족! (필요: " + req.count + ", 보유: " + getResourceCount(account, req.name) + ")");
      return;
    }
  }

  // 비용 차감
  account.gold -= targetItem.cost.gold;
  for (var r = 0; r < targetItem.cost.resources.length; r++) {
    removeResource(account, targetItem.cost.resources[r].name, targetItem.cost.resources[r].count);
  }

  // 장착 (기존 장비 대체)
  account.equipment[targetSlot] = { name: targetItem.name, tier: targetItem.tier };
  account.maxStamina = getEffectiveMaxStamina(account);
  saveMineAccount(sender, account);

  // 장비 이미지 전송
  if (targetItem.image) {
    replier.reply(MineData.pickaxeImageBase + "/mine/" + targetItem.image + ".html?v=2");
  }

  var text = "[ 싱봇 광산 ] 제작 & 장착 완료!\n\n";
  text += targetItem.emoji + " " + targetItem.name + "\n";
  if (targetItem.staminaBonus) text += "  → 체력 +" + targetItem.staminaBonus + " (최대: " + account.maxStamina + ")\n";
  if (targetItem.luckBonus) text += "  → 행운 +" + Math.round(targetItem.luckBonus * 100) + "%\n";
  if (targetItem.idleBonus) text += "  → 방치 +" + Math.round(targetItem.idleBonus * 100) + "%\n";
  if (targetItem.depthBonus) text += "  → 깊이 +" + targetItem.depthBonus + "\n";
  text += "\n💰 잔여: " + account.gold + "G";
  replier.reply(text);
}

function handleMineUnequip(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }
  ensureEquipment(account);

  var input = msg.replace(/^(광산|ㄱㅅ|ㄳ)해제\s*/, "").trim();
  var slotMap = { "안전모": "helmet", "헬멧": "helmet", "램프": "lamp", "장화": "boots" };
  var slot = slotMap[input];

  if (!slot) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ해제 [안전모/램프/장화]");
    return;
  }

  if (!account.equipment[slot]) {
    replier.reply("[ 싱봇 광산 ] 해당 슬롯에 장비가 없습니다.");
    return;
  }

  var removed = account.equipment[slot].name;
  account.equipment[slot] = null;
  account.maxStamina = getEffectiveMaxStamina(account);
  if (account.stamina > account.maxStamina) account.stamina = account.maxStamina;
  saveMineAccount(sender, account);

  replier.reply("[ 싱봇 광산 ] " + removed + " 해제 완료.");
}

// ===== 거래 시스템 =====

var mineTradeRequests = {};
var mineTradeSessions = {};

function handleMineTradeRequest(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  var input = msg.replace(/^(광산|ㄱㅅ|ㄳ)거래신청\s*/, "").trim();
  if (!input) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ거래신청 [상대 광부이름]");
    return;
  }

  // 광부이름으로 먼저 찾고, 없으면 카톡이름으로 시도
  var target = findSenderByMineName(input);
  if (!target) target = input;
  if (target === sender) {
    replier.reply("[ 싱봇 광산 ] 자기 자신과 거래할 수 없습니다.");
    return;
  }
  if (mineTradeSessions[sender]) {
    replier.reply("[ 싱봇 광산 ] 이미 진행 중인 거래가 있습니다.");
    return;
  }

  mineTradeRequests[target] = { from: sender, room: room, time: Date.now() };
  replier.reply("[ 싱봇 광산 ] " + account.name + "님이 " + (loadMineAccount(target) ? loadMineAccount(target).name : target) + "님에게 거래를 신청했습니다!\n" + target + "님은 'ㄱㅅ거래수락' 또는 'ㄱㅅ거래거절'을 입력하세요.");
}

function handleMineTradeAccept(room, msg, sender, replier) {
  var req = mineTradeRequests[sender];
  if (!req) {
    replier.reply("[ 싱봇 광산 ] 받은 거래 요청이 없습니다.");
    return;
  }
  // 5분 타임아웃
  if (Date.now() - req.time > 300000) {
    delete mineTradeRequests[sender];
    replier.reply("[ 싱봇 광산 ] 거래 요청�� 만료되었습니다.");
    return;
  }

  var sessionId = req.from + ":" + sender + ":" + Date.now();
  var session = {
    id: sessionId,
    p1: { sender: req.from, resources: [], gold: 0, confirmed: false },
    p2: { sender: sender, resources: [], gold: 0, confirmed: false }
  };
  mineTradeSessions[req.from] = sessionId;
  mineTradeSessions[sender] = sessionId;
  mineTradeSessions[sessionId] = session;
  delete mineTradeRequests[sender];

  replier.reply(
    "[ 싱봇 광산 ] 거래 시작!\n\n" +
    session.p1.sender + " ↔ " + session.p2.sender + "\n\n" +
    "'ㄱㅅ거래올리기 [자원] [수량]' — 자원 제안\n" +
    "'ㄱㅅ거래골드 [수량]' — 골드 제안\n" +
    "'ㄱㅅ거래확인' — 거래 확정 (양쪽 모두)\n" +
    "'ㄱㅅ거래취소' — 거래 취소"
  );
}

function handleMineTradeDecline(room, msg, sender, replier) {
  if (mineTradeRequests[sender]) {
    var from = mineTradeRequests[sender].from;
    delete mineTradeRequests[sender];
    replier.reply("[ 싱봇 광산 ] " + sender + "님이 " + from + "님의 거래를 거절했습니다.");
  } else {
    replier.reply("[ 싱봇 광산 ] 받은 거래 요청이 없습니다.");
  }
}

function getMineTradeSession(sender) {
  var sessionId = mineTradeSessions[sender];
  if (!sessionId) return null;
  var session = mineTradeSessions[sessionId];
  if (!session) return null;
  return session;
}

function getMyTradeSlot(session, sender) {
  return session.p1.sender === sender ? session.p1 : session.p2;
}

function handleMineTradeOffer(room, msg, sender, replier) {
  var session = getMineTradeSession(sender);
  if (!session) { replier.reply("[ 싱봇 광산 ] 진행 중인 거래가 없습니다."); return; }

  var input = msg.replace(/^(광산|ㄱㅅ|ㄳ)거래올리기\s*/, "").trim();
  var parts = input.split(/\s+/);
  if (parts.length < 2) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ거래올리기 [자원명] [수량]");
    return;
  }
  var resourceName = parts[0];
  var count = parseInt(parts[1]);
  if (isNaN(count) || count <= 0) {
    replier.reply("[ 싱봇 광산 ] 올바른 수량을 입력하세요.");
    return;
  }

  var account = loadMineAccount(sender);
  if (getResourceCount(account, resourceName) < count) {
    replier.reply("[ 싱봇 광산 ] " + resourceName + " 부족! (보유: " + getResourceCount(account, resourceName) + ")");
    return;
  }

  var slot = getMyTradeSlot(session, sender);
  // 기존 제안에 추가
  var found = false;
  for (var i = 0; i < slot.resources.length; i++) {
    if (slot.resources[i].name === resourceName) {
      slot.resources[i].count += count;
      found = true;
      break;
    }
  }
  if (!found) slot.resources.push({ name: resourceName, count: count });
  slot.confirmed = false;
  // 상대도 미확정으로
  var otherSlot = session.p1.sender === sender ? session.p2 : session.p1;
  otherSlot.confirmed = false;

  replier.reply("[ 싱봇 광산 ] " + sender + "님이 " + resourceName + " x" + count + " 제안!\n" + formatTradeStatus(session));
}

function handleMineTradeGold(room, msg, sender, replier) {
  var session = getMineTradeSession(sender);
  if (!session) { replier.reply("[ 싱봇 광산 ] 진행 중인 거래가 없습니다."); return; }

  var amount = parseInt(msg.replace(/^(광산|ㄱㅅ|ㄳ)거래골드\s*/, "").trim());
  if (isNaN(amount) || amount <= 0) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ거래골드 [수량]");
    return;
  }

  var account = loadMineAccount(sender);
  if (account.gold < amount) {
    replier.reply("[ 싱봇 광산 ] 골드 부족! (보유: " + account.gold + "G)");
    return;
  }

  var slot = getMyTradeSlot(session, sender);
  slot.gold = amount;
  slot.confirmed = false;
  var otherSlot = session.p1.sender === sender ? session.p2 : session.p1;
  otherSlot.confirmed = false;

  replier.reply("[ 싱봇 광산 ] " + sender + "님이 " + amount + "G 제안!\n" + formatTradeStatus(session));
}

function handleMineTradeConfirm(room, msg, sender, replier) {
  var session = getMineTradeSession(sender);
  if (!session) { replier.reply("[ 싱봇 광산 ] 진행 중인 거래가 없습니다."); return; }

  var slot = getMyTradeSlot(session, sender);
  slot.confirmed = true;

  var otherSlot = session.p1.sender === sender ? session.p2 : session.p1;
  if (!otherSlot.confirmed) {
    replier.reply("[ 싱봇 광산 ] " + sender + "님 확인 완료! " + otherSlot.sender + "님의 확인을 기다리는 중...\n" + formatTradeStatus(session));
    return;
  }

  // 양쪽 확인 → 거래 실행
  var acc1 = loadMineAccount(session.p1.sender);
  var acc2 = loadMineAccount(session.p2.sender);

  // 최종 자원 검증
  for (var i = 0; i < session.p1.resources.length; i++) {
    var r = session.p1.resources[i];
    if (getResourceCount(acc1, r.name) < r.count) {
      replier.reply("[ 싱봇 광산 ] " + session.p1.sender + "님의 " + r.name + " 부족으로 거래 실패!");
      cleanupTradeSession(session);
      return;
    }
  }
  for (var i = 0; i < session.p2.resources.length; i++) {
    var r = session.p2.resources[i];
    if (getResourceCount(acc2, r.name) < r.count) {
      replier.reply("[ 싱봇 광산 ] " + session.p2.sender + "님의 " + r.name + " 부족으로 거래 실패!");
      cleanupTradeSession(session);
      return;
    }
  }
  if (acc1.gold < session.p1.gold || acc2.gold < session.p2.gold) {
    replier.reply("[ 싱봇 광산 ] 골드 부족으로 거래 실패!");
    cleanupTradeSession(session);
    return;
  }

  // 교환 실행
  // p1 → p2
  for (var i = 0; i < session.p1.resources.length; i++) {
    var r = session.p1.resources[i];
    removeResource(acc1, r.name, r.count);
    addResource(acc2, r.name, r.count);
  }
  if (session.p1.gold > 0) { acc1.gold -= session.p1.gold; acc2.gold += session.p1.gold; }
  // p2 → p1
  for (var i = 0; i < session.p2.resources.length; i++) {
    var r = session.p2.resources[i];
    removeResource(acc2, r.name, r.count);
    addResource(acc1, r.name, r.count);
  }
  if (session.p2.gold > 0) { acc2.gold -= session.p2.gold; acc1.gold += session.p2.gold; }

  saveMineAccount(session.p1.sender, acc1);
  saveMineAccount(session.p2.sender, acc2);
  cleanupTradeSession(session);

  replier.reply("[ 싱봇 광산 ] 거래 완료! 🤝\n\n" + session.p1.sender + " ↔ " + session.p2.sender);
}

function handleMineTradeCancel(room, msg, sender, replier) {
  var session = getMineTradeSession(sender);
  if (!session) { replier.reply("[ 싱봇 광산 ] 진행 중인 거래가 없습니다."); return; }
  cleanupTradeSession(session);
  replier.reply("[ 싱봇 광산 ] " + sender + "님이 거래를 취소했습니다.");
}

function cleanupTradeSession(session) {
  delete mineTradeSessions[session.p1.sender];
  delete mineTradeSessions[session.p2.sender];
  delete mineTradeSessions[session.id];
}

function formatTradeStatus(session) {
  var text = "\n━━━ 거래 현황 ━━━\n";
  text += session.p1.sender + (session.p1.confirmed ? " ✅" : " ⬜") + ":\n";
  if (session.p1.gold > 0) text += "  💰 " + session.p1.gold + "G\n";
  for (var i = 0; i < session.p1.resources.length; i++) {
    text += "  " + session.p1.resources[i].name + " x" + session.p1.resources[i].count + "\n";
  }
  text += session.p2.sender + (session.p2.confirmed ? " ✅" : " ⬜") + ":\n";
  if (session.p2.gold > 0) text += "  💰 " + session.p2.gold + "G\n";
  for (var i = 0; i < session.p2.resources.length; i++) {
    text += "  " + session.p2.resources[i].name + " x" + session.p2.resources[i].count + "\n";
  }
  return text;
}

// ===== 시장 시스��� (일일 변동 시세) =====

function getMarketPrice(resourceId) {
  var basePrice = MineData.marketBasePrices[resourceId];
  if (!basePrice) return 0;
  var seed = todaySeed() + Math.abs(hashCode(resourceId));
  var rng = (Math.abs(seed) % 100) / 100;
  var multiplier = 0.5 + rng * 1.5; // 0.5x ~ 2.0x
  return Math.max(1, Math.floor(basePrice * multiplier));
}

function handleMineMarket(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  var text = "[ 싱봇 광산 ] 오늘의 시장 시세\n";
  text += "━━━━━━━━━━━━━━━━━━\n";
  text += "📅 " + todayString() + " (매일 변동)\n\n";

  for (var id in MineData.marketBasePrices) {
    if (MineData.marketBasePrices.hasOwnProperty(id)) {
      var res = MineData.resources[id];
      var price = getMarketPrice(id);
      var basePrice = res.sellPrice;
      var diff = price - basePrice;
      var arrow = diff > 0 ? "📈" : (diff < 0 ? "📉" : "➡️");
      text += res.emoji + " " + res.name + ": " + price + "G " + arrow;
      if (diff !== 0) text += " (" + (diff > 0 ? "+" : "") + diff + ")";
      text += "\n";
    }
  }

  text += "\n기준가 대비 변동 표시\n";
  text += "'ㄱㅅ시장판매 [자원] [수량]'으로 시장가 판매";
  replier.reply(text);
}

function handleMineMarketSell(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  var input = msg.replace(/^(광산|ㄱㅅ|ㄳ)시장판매\s*/, "").trim();
  if (!input) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ시장판매 [자원명] [수량]\n예) ㄱㅅ시장판매 금광석 5\n전부: ㄱㅅ시장판매 금광석 전부");
    return;
  }

  var parts = input.split(/\s+/);
  var resourceName = parts[0];
  var id = MineData.resourceIdMap[resourceName];
  if (!id) {
    replier.reply("[ 싱봇 광산 ] 알 수 없는 자원: " + resourceName);
    return;
  }

  var has = getResourceCount(account, resourceName);
  if (has <= 0) {
    replier.reply("[ 싱봇 광산 ] " + resourceName + "을(를) 보유하고 있지 않습니다.");
    return;
  }

  var sellAll = parts.length > 1 && parts[1] === "전부";
  var count = sellAll ? has : (parts.length > 1 ? parseInt(parts[1]) : 0);
  if (isNaN(count) || count <= 0) {
    replier.reply("[ 싱봇 광산 ] 수량을 입력하세요.");
    return;
  }
  if (count > has) count = has;

  var unitPrice = getMarketPrice(id);
  var totalGold = unitPrice * count;
  var npcTotal = MineData.resources[id].sellPrice * count;
  var diff = totalGold - npcTotal;

  removeResource(account, resourceName, count);
  account.gold += totalGold;
  account.totalGoldEarned += totalGold;
  saveMineAccount(sender, account);

  var text = "[ 싱봇 광산 ] 시장 판매 완료!\n\n";
  text += MineData.resources[id].emoji + " " + resourceName + " x" + count + "\n";
  text += "시장가: " + unitPrice + "G × " + count + " = " + totalGold + "G\n";
  if (diff > 0) {
    text += "📈 NPC 대비 +" + diff + "G 이득!\n";
  } else if (diff < 0) {
    text += "📉 NPC 대비 " + diff + "G 손해...\n";
  }
  text += "💰 보유: " + account.gold + "G";
  replier.reply(text);
}

// ===== 프레스티지 시스템 =====

function handleMinePrestige(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  var req = MineData.prestigeRequirements;
  if (account.depth < req.minDepth || account.pickaxeLevel < req.minPickaxe) {
    replier.reply(
      "[ 싱봇 광산 ] 환생 조건 미달!\n\n" +
      "필요: 깊이 " + req.minDepth + "+ (현재: " + account.depth + ")\n" +
      "필요: 곡괭이 Lv." + req.minPickaxe + "+ (현재: Lv." + account.pickaxeLevel + ")"
    );
    return;
  }

  // "광산환생 확인" 이면 실행
  if (msg.indexOf("확인") === -1) {
    var fragments = Math.floor(account.depth / 50) + Math.floor(account.pickaxeLevel / 5);
    replier.reply(
      "[ 싱봇 광산 ] ⭐ 환생 안내\n" +
      "━━━━━━━━━━━━━━━━━━\n" +
      "획득 별의 파편: " + fragments + "개\n" +
      "(현재 보유: " + account.starFragments + "개)\n\n" +
      "🔄 리셋 항목:\n" +
      "  깊이, 곡괭이, 자원, 장비, 골드\n\n" +
      "✅ 유지 항목:\n" +
      "  도감, 별의 파편, 영구 보너스, 통계\n\n" +
      "'ㄱㅅ환생 확인'으로 환생 진행"
    );
    return;
  }

  // 환생 실행
  var fragments = Math.floor(account.depth / 50) + Math.floor(account.pickaxeLevel / 5);
  account.starFragments += fragments;
  account.prestigeCount++;

  // 리셋
  var startDepth = MineData.prestigeStartDepths[account.prestigeBonuses.startDepth] || 1;
  account.depth = Math.max(1, startDepth);
  account.maxDepthReached = Math.max(account.maxDepthReached, account.depth);
  account.pickaxeLevel = 1;
  account.currentArea = 0;
  account.resources = [];
  account.items = [];
  account.equipment = { helmet: null, lamp: null, boots: null };
  account.gold = MineData.config.STARTING_GOLD;
  account.buffs = {};
  account.maxStamina = getEffectiveMaxStamina(account);
  account.stamina = account.maxStamina;
  account.lastActiveTime = Date.now();
  account.lastStaminaRegen = Date.now();

  // 지역 재계산
  checkAreaUnlock(account);

  saveMineAccount(sender, account);

  replier.reply(
    "[ 싱봇 광산 ] ⭐ 환생 완료!\n" +
    "━━━━━━━━━━━━━━━━━━\n" +
    "환생 횟수: " + account.prestigeCount + "회\n" +
    "별의 파편: +" + fragments + " (보유: " + account.starFragments + ")\n" +
    "시작 깊이: " + account.depth + "\n\n" +
    "'ㄱㅅ환생상점'에서 영구 보너스를 구매하세요!"
  );
}

function handleMinePrestigeShop(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  var text = "[ 싱봇 광산 ] ⭐ 환생 상점\n";
  text += "━━━━━━━━━━━━━━━━━━\n";
  text += "🌟 보유 별의 파편: " + account.starFragments + "\n\n";

  var shop = MineData.prestigeShop;
  for (var i = 0; i < shop.length; i++) {
    var item = shop[i];
    var currentLevel = account.prestigeBonuses[item.id] || 0;
    text += (i + 1) + ". " + item.name;
    if (currentLevel >= item.maxLevel) {
      text += " [MAX]\n";
    } else {
      text += " (Lv." + currentLevel + "/" + item.maxLevel + ")\n";
      text += "   다음: 🌟" + item.costs[currentLevel] + " — " + item.desc + "\n";
    }
  }

  text += "\n'ㄱㅅ환생구매 [보너스명]'으로 구매";
  replier.reply(text);
}

function handleMinePrestigeBuy(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  var input = msg.replace(/^(광산|ㄱㅅ|ㄳ)환생구매\s*/, "").trim();
  if (!input) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ환생구매 [보너스명]\n예) ㄱㅅ환생구매 채굴 효율");
    return;
  }

  var shop = MineData.prestigeShop;
  var target = null;
  for (var i = 0; i < shop.length; i++) {
    if (shop[i].name === input || shop[i].name.indexOf(input) !== -1) {
      target = shop[i];
      break;
    }
  }

  if (!target) {
    replier.reply("[ 싱봇 광산 ] 해당 보너스를 찾을 수 없습니다. 'ㄱㅅ환생상점'에서 확인하세요.");
    return;
  }

  var currentLevel = account.prestigeBonuses[target.id] || 0;
  if (currentLevel >= target.maxLevel) {
    replier.reply("[ 싱봇 광산 ] " + target.name + "은(는) 이미 최대 레벨입니다!");
    return;
  }

  var cost = target.costs[currentLevel];
  if (account.starFragments < cost) {
    replier.reply("[ 싱봇 광산 ] 별의 파편 부족! (필요: " + cost + ", 보유: " + account.starFragments + ")");
    return;
  }

  account.starFragments -= cost;
  account.prestigeBonuses[target.id] = currentLevel + 1;

  // 체력 보너스 즉시 반영
  account.maxStamina = getEffectiveMaxStamina(account);

  saveMineAccount(sender, account);

  replier.reply(
    "[ 싱봇 광산 ] ⭐ 구매 완료!\n\n" +
    target.name + " Lv." + (currentLevel + 1) + "/" + target.maxLevel + "\n" +
    target.desc + "\n" +
    "🌟 잔여: " + account.starFragments
  );
}

// ===== PvP 광산 대결 =====

var mineDuelRequests = {};
var mineDuelSessions = {};

function pvpMineTurn(account) {
  var base = account.pickaxeLevel * 2;
  var flatLuck = 10 + Math.floor(Math.random() * 30);         // 10~39 (레벨 무관 공평한 운)
  var scaleLuck = Math.floor(Math.random() * account.pickaxeLevel); // 레벨 비례 운
  var prestigeBonus = 1 + account.prestigeBonuses.miningPower * 0.10;
  var critChance = 0.15 + (account.prestigeBonuses.luck * 0.01);
  var crit = Math.random() < critChance ? 1.8 : 1;
  return Math.floor((base + flatLuck + scaleLuck) * prestigeBonus * crit);
}

function handleMineDuel(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  if (mineDuelSessions[sender]) {
    replier.reply("[ 싱봇 광산 ] 이미 대결 진행 중입니다. 'ㄱㅅ캐기'로 채굴하세요.");
    return;
  }

  var input = msg.replace(/^(광산|ㄱㅅ|ㄳ)대결\s*/, "").trim();
  if (!input) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ대결 [상대 광부이름]");
    return;
  }

  // 광부이름으로 먼저 찾고, 없으면 카톡이름으로 시도
  var target = findSenderByMineName(input);
  if (!target) target = input;
  var targetAccount = loadMineAccount(target);
  if (!targetAccount) {
    replier.reply("[ 싱봇 광산 ] '" + input + "' 광부를 찾을 수 없습니다.");
    return;
  }
  if (target === sender) {
    replier.reply("[ 싱봇 광산 ] 자기 자신과 대결할 수 없습니다.");
    return;
  }

  mineDuelRequests[target] = { from: sender, room: room, time: Date.now() };
  replier.reply(
    "[ 싱봇 광산 ] ⚔️ " + account.name + "님이 " + targetAccount.name + "님에게 대결 신청!\n\n" +
    "5턴 채굴 대결 — 더 많이 캔 사람이 승리!\n" +
    target + "님은 'ㄱㅅ대결수락' 또는 'ㄱㅅ대결거절'을 입력하세요."
  );
}

function handleMineDuelAccept(room, msg, sender, replier) {
  var req = mineDuelRequests[sender];
  if (!req) {
    replier.reply("[ 싱봇 광산 ] 받은 대결 요청이 없습니다.");
    return;
  }
  if (Date.now() - req.time > 300000) {
    delete mineDuelRequests[sender];
    replier.reply("[ 싱봇 광산 ] 대결 요청이 만료되었습니다.");
    return;
  }

  var sessionId = req.from + ":" + sender + ":" + Date.now();
  var session = {
    id: sessionId,
    p1: { sender: req.from, total: 0, turns: 0 },
    p2: { sender: sender, total: 0, turns: 0 },
    maxTurns: 5,
    currentTurn: 0,
    room: room
  };
  mineDuelSessions[req.from] = sessionId;
  mineDuelSessions[sender] = sessionId;
  mineDuelSessions[sessionId] = session;
  delete mineDuelRequests[sender];

  replier.reply(
    "[ 싱봇 광산 ] ⚔️ 대결 시작!\n\n" +
    session.p1.sender + " vs " + session.p2.sender + "\n" +
    "5턴 채굴 대결! 'ㄱㅅ캐기'로 채굴하세요."
  );
}

function handleMineDuelDecline(room, msg, sender, replier) {
  if (mineDuelRequests[sender]) {
    var from = mineDuelRequests[sender].from;
    delete mineDuelRequests[sender];
    replier.reply("[ 싱봇 광산 ] " + sender + "님이 대결을 거절했습니다.");
  } else {
    replier.reply("[ 싱봇 광산 ] 받은 대결 요청이 없습니다.");
  }
}

function handleMineDig(room, msg, sender, replier) {
  var sessionId = mineDuelSessions[sender];
  if (!sessionId) { replier.reply("[ 싱봇 광산 ] 진행 중인 대결이 없습니다."); return; }
  var session = mineDuelSessions[sessionId];
  if (!session) { replier.reply("[ 싱봇 광산 ] 진행 중인 대결이 없습니다."); return; }

  var mySlot = session.p1.sender === sender ? session.p1 : session.p2;
  var otherSlot = session.p1.sender === sender ? session.p2 : session.p1;

  if (mySlot.turns > session.currentTurn) {
    replier.reply("[ 싱봇 광산 ] 이번 턴은 이미 채굴했습니다. " + otherSlot.sender + "님을 기다리는 중...");
    return;
  }

  var account = loadMineAccount(sender);
  var mined = pvpMineTurn(account);
  mySlot.total += mined;
  mySlot.turns++;

  // 양쪽 다 이번 턴 완료했는지 체크
  if (mySlot.turns > session.currentTurn && otherSlot.turns > session.currentTurn) {
    session.currentTurn++;
    var text = "[ 싱봇 광산 ] ⚔️ 턴 " + session.currentTurn + "/" + session.maxTurns + "\n";
    text += "━━━━━━━━━━━━━━━━━━\n";
    text += session.p1.sender + ": +" + getLastTurnAmount(session.p1, session.currentTurn) + " (총 " + session.p1.total + ")\n";
    text += session.p2.sender + ": +" + getLastTurnAmount(session.p2, session.currentTurn) + " (총 " + session.p2.total + ")\n";

    if (session.currentTurn >= session.maxTurns) {
      // 대결 종료
      text += "\n━━━━━━━━━━━━━━━━━━\n";
      var acc1 = loadMineAccount(session.p1.sender);
      var acc2 = loadMineAccount(session.p2.sender);
      if (session.p1.total > session.p2.total) {
        text += "🏆 " + session.p1.sender + " 승리!\n";
        acc1.pvpWins++;
        acc2.pvpLosses++;
      } else if (session.p2.total > session.p1.total) {
        text += "🏆 " + session.p2.sender + " 승리!\n";
        acc2.pvpWins++;
        acc1.pvpLosses++;
      } else {
        text += "🤝 무승부!\n";
      }
      text += session.p1.total + " vs " + session.p2.total;
      saveMineAccount(session.p1.sender, acc1);
      saveMineAccount(session.p2.sender, acc2);
      cleanupDuelSession(session);
    }

    replier.reply(text);
  } else {
    replier.reply("[ 싱봇 광산 ] ⛏️ 채굴! +" + mined + " (총 " + mySlot.total + ")\n" + otherSlot.sender + "님을 기다리는 중...");
  }
}

function getLastTurnAmount(slot, currentTurn) {
  // 직전 턴에서 캔 양 (total 차이로 계산은 복잡하므로 표시용)
  return slot.total;
}

function handleMineSurrender(room, msg, sender, replier) {
  var sessionId = mineDuelSessions[sender];
  if (!sessionId) { replier.reply("[ 싱봇 광산 ] 진행 중인 대결이 없습니다."); return; }
  var session = mineDuelSessions[sessionId];

  var other = session.p1.sender === sender ? session.p2.sender : session.p1.sender;
  var accWinner = loadMineAccount(other);
  var accLoser = loadMineAccount(sender);
  accWinner.pvpWins++;
  accLoser.pvpLosses++;
  saveMineAccount(other, accWinner);
  saveMineAccount(sender, accLoser);
  cleanupDuelSession(session);

  replier.reply("[ 싱봇 광산 ] " + sender + "님이 항복! 🏆 " + other + "님 승리!");
}

function cleanupDuelSession(session) {
  delete mineDuelSessions[session.p1.sender];
  delete mineDuelSessions[session.p2.sender];
  delete mineDuelSessions[session.id];
}

// ===== 랭킹 =====

var mineRankingCache = null;
var mineRankingCacheTime = 0;

function handleMineRanking(room, msg, sender, replier) {
  var now = Date.now();
  // 10분 캐시
  if (!mineRankingCache || now - mineRankingCacheTime > 600000) {
    mineRankingCache = buildMineRanking();
    mineRankingCacheTime = now;
  }

  var text = "[ 싱봇 광산 ] 🏆 랭킹\n";
  text += "━━━━━━━━━━━━━━━━━━\n\n";

  text += "⛏️ 최고 깊이:\n";
  for (var i = 0; i < mineRankingCache.depth.length && i < 5; i++) {
    var e = mineRankingCache.depth[i];
    text += "  " + (i + 1) + ". " + e.name + " — 깊이 " + e.value + "\n";
  }

  text += "\n🔨 곡괭이 레벨:\n";
  for (var i = 0; i < mineRankingCache.pickaxe.length && i < 5; i++) {
    var e = mineRankingCache.pickaxe[i];
    text += "  " + (i + 1) + ". " + e.name + " — Lv." + e.value + "\n";
  }

  text += "\n⭐ 프레스티지:\n";
  for (var i = 0; i < mineRankingCache.prestige.length && i < 5; i++) {
    var e = mineRankingCache.prestige[i];
    text += "  " + (i + 1) + ". " + e.name + " — " + e.value + "회\n";
  }

  text += "\n⚔️ PvP 승수:\n";
  for (var i = 0; i < mineRankingCache.pvp.length && i < 5; i++) {
    var e = mineRankingCache.pvp[i];
    text += "  " + (i + 1) + ". " + e.name + " — " + e.value + "승\n";
  }

  if (mineRankingCache.depth.length === 0) {
    text += "\n(아직 데이터가 없습니다)";
  }

  replier.reply(text);
}

function buildMineRanking() {
  var entries = [];
  for (var sender in mineAccounts) {
    if (mineAccounts.hasOwnProperty(sender)) {
      var acc = mineAccounts[sender];
      entries.push(acc);
    }
  }

  // 파일에서도 로드 시도 (캐시 안 된 유저)
  try {
    var files = [];
    try {
      var folder = new java.io.File(MINE_DATA_DIR);
      if (folder.exists()) files = folder.listFiles() || [];
    } catch (e) {}
    for (var i = 0; i < files.length; i++) {
      var f = files[i];
      if (f.getName().indexOf(".json") !== -1) {
        var senderName = f.getName().replace(".json", "");
        if (!mineAccounts[senderName]) {
          try {
            var fis = new java.io.FileInputStream(f);
            var reader = new java.io.InputStreamReader(fis, "UTF-8");
            var br = new java.io.BufferedReader(reader);
            var data = "";
            var line;
            while ((line = br.readLine()) != null) data += line;
            br.close();
            var acc = JSON.parse(data);
            entries.push(acc);
          } catch (e2) {}
        }
      }
    }
  } catch (e) {}

  var depth = [], pickaxe = [], prestige = [], pvp = [];
  for (var i = 0; i < entries.length; i++) {
    var acc = entries[i];
    depth.push({ name: acc.name, value: acc.maxDepthReached || acc.depth || 0 });
    pickaxe.push({ name: acc.name, value: acc.pickaxeLevel || 0 });
    if (acc.prestigeCount > 0) prestige.push({ name: acc.name, value: acc.prestigeCount });
    if (acc.pvpWins > 0) pvp.push({ name: acc.name, value: acc.pvpWins });
  }

  var sortDesc = function(a, b) { return b.value - a.value; };
  depth.sort(sortDesc);
  pickaxe.sort(sortDesc);
  prestige.sort(sortDesc);
  pvp.sort(sortDesc);

  return { depth: depth, pickaxe: pickaxe, prestige: prestige, pvp: pvp };
}

// ===== 길드 시스템 =====

var MINE_GUILD_FILE = DATA_DIR + "mine_guilds.json";
var mineGuilds = {};

function loadMineGuilds() {
  try {
    var raw = FileStream.read(MINE_GUILD_FILE);
    if (raw) mineGuilds = JSON.parse(raw);
  } catch (e) {
    try {
      var file = new java.io.File(MINE_GUILD_FILE);
      if (file.exists()) {
        var fis = new java.io.FileInputStream(file);
        var reader = new java.io.InputStreamReader(fis, "UTF-8");
        var br = new java.io.BufferedReader(reader);
        var data = "";
        var line;
        while ((line = br.readLine()) != null) data += line;
        br.close();
        mineGuilds = JSON.parse(data);
      }
    } catch (e2) {}
  }
}

function saveMineGuilds() {
  try {
    FileStream.createDir(DATA_DIR);
    FileStream.write(MINE_GUILD_FILE, JSON.stringify(mineGuilds));
  } catch (e) {
    try {
      var file = new java.io.File(MINE_GUILD_FILE);
      var fos = new java.io.FileOutputStream(file);
      var writer = new java.io.OutputStreamWriter(fos, "UTF-8");
      writer.write(JSON.stringify(mineGuilds));
      writer.close();
      fos.close();
    } catch (e2) {
      Log.e("길드 저장 오류: " + e2);
    }
  }
}

function getPlayerGuild(sender) {
  var account = loadMineAccount(sender);
  if (!account || !account.guild) return null;
  return mineGuilds[account.guild] || null;
}

function getGuildBonus(account) {
  if (!account.guild) return { miningPower: 0, idleRate: 0, luck: 0 };
  var guild = mineGuilds[account.guild];
  if (!guild) return { miningPower: 0, idleRate: 0, luck: 0 };
  var perks = MineData.guild.perks;
  return {
    miningPower: guild.level * perks.miningPower,
    idleRate: Math.floor(guild.level / 2) * perks.idleRate,
    luck: Math.floor(guild.level / 3) * perks.luck
  };
}

function handleMineGuildCreate(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  if (account.guild) {
    replier.reply("[ 싱봇 광산 ] 이미 길드에 가입되어 있습니다. (" + account.guild + ")");
    return;
  }
  if (account.prestigeCount < MineData.guild.createPrestigeReq) {
    replier.reply("[ 싱봇 광산 ] 길드 생성에는 프레스티지 " + MineData.guild.createPrestigeReq + "회 이상이 필요합니다. (현재: " + account.prestigeCount + "회)");
    return;
  }

  var guildName = msg.replace(/^(광산|ㄱㅅ|ㄳ)길드생성\s*/, "").trim();
  if (!guildName || guildName.length < 2 || guildName.length > 8) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ길드생성 [길드명]\n길드명: 2~8자");
    return;
  }
  if (mineGuilds[guildName]) {
    replier.reply("[ 싱봇 광산 ] 이미 존재하는 길드명입니다.");
    return;
  }
  if (account.gold < MineData.guild.createCost) {
    replier.reply("[ 싱봇 광산 ] 골드 부족! (필요: " + MineData.guild.createCost + "G, 보유: " + account.gold + "G)");
    return;
  }

  account.gold -= MineData.guild.createCost;
  account.guild = guildName;

  mineGuilds[guildName] = {
    name: guildName,
    leader: sender,
    members: [sender],
    level: 1,
    exp: 0,
    guildDepth: 0,
    totalMined: 0,
    joinRequests: [],
    createdAt: Date.now()
  };

  saveMineAccount(sender, account);
  saveMineGuilds();

  replier.reply(
    "[ 싱봇 광산 ] 🏰 길드 생성 완료!\n\n" +
    "길드명: " + guildName + "\n" +
    "길드장: " + account.name + "\n" +
    "레벨: 1\n\n" +
    "멤버 모집: 'ㄱㅅ길드가입 " + guildName + "'"
  );
}

function handleMineGuildJoin(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  if (account.guild) {
    replier.reply("[ 싱봇 광산 ] 이미 길드에 가입되어 있습니다. (" + account.guild + ")");
    return;
  }

  var guildName = msg.replace(/^(광산|ㄱㅅ|ㄳ)길드가입\s*/, "").trim();
  if (!guildName) {
    replier.reply("[ 싱봇 광산 ] 사용법: ㄱㅅ길드가입 [길드명]");
    return;
  }

  var guild = mineGuilds[guildName];
  if (!guild) {
    replier.reply("[ 싱봇 광산 ] 존재하지 않는 길드입니다.");
    return;
  }
  if (guild.members.length >= MineData.guild.maxMembers) {
    replier.reply("[ 싱봇 광산 ] 길드 인원이 가득 찼습니다. (" + guild.members.length + "/" + MineData.guild.maxMembers + ")");
    return;
  }

  // 가입 요청 추가
  for (var i = 0; i < guild.joinRequests.length; i++) {
    if (guild.joinRequests[i] === sender) {
      replier.reply("[ 싱봇 광산 ] 이미 가입 요청을 보냈습니다. 길드장의 수락을 기다리세요.");
      return;
    }
  }

  guild.joinRequests.push(sender);
  saveMineGuilds();

  replier.reply("[ 싱봇 광산 ] " + guildName + " 길드에 가입 요청을 보냈습니다!\n길드장 " + guild.leader + "님이 'ㄱㅅ길드수락 " + sender + "'로 수락합니다.");
}

function handleMineGuildAccept(room, msg, sender, replier) {
  var guild = getPlayerGuild(sender);
  if (!guild) { replier.reply("[ 싱봇 광산 ] 길드에 가입되어 있지 않습니다."); return; }
  if (guild.leader !== sender) {
    replier.reply("[ 싱봇 광산 ] 길드장만 수락할 수 있습니다.");
    return;
  }

  var target = msg.replace(/^(광산|ㄱㅅ|ㄳ)길드수락\s*/, "").trim();
  if (!target) {
    // 요청 목록 표시
    if (guild.joinRequests.length === 0) {
      replier.reply("[ 싱봇 광산 ] 가입 요청이 없습니다.");
      return;
    }
    var text = "[ 싱봇 광산 ] 가입 요청 목록:\n";
    for (var i = 0; i < guild.joinRequests.length; i++) {
      text += "  " + (i + 1) + ". " + guild.joinRequests[i] + "\n";
    }
    text += "\n'ㄱㅅ길드수락 [이름]'으로 수락";
    replier.reply(text);
    return;
  }

  // 요청 찾기
  var idx = -1;
  for (var i = 0; i < guild.joinRequests.length; i++) {
    if (guild.joinRequests[i] === target) { idx = i; break; }
  }
  if (idx === -1) {
    replier.reply("[ 싱봇 광산 ] " + target + "님의 가입 요청이 없습니다.");
    return;
  }
  if (guild.members.length >= MineData.guild.maxMembers) {
    replier.reply("[ 싱봇 광산 ] 길드 인원이 가득 찼습니다.");
    return;
  }

  guild.joinRequests.splice(idx, 1);
  guild.members.push(target);

  var targetAccount = loadMineAccount(target);
  if (targetAccount) {
    targetAccount.guild = guild.name;
    saveMineAccount(target, targetAccount);
  }

  saveMineGuilds();
  replier.reply("[ 싱봇 광산 ] 🏰 " + target + "님이 " + guild.name + " 길드에 가입했습니다! (" + guild.members.length + "/" + MineData.guild.maxMembers + ")");
}

function handleMineGuildInfo(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  var guild = getPlayerGuild(sender);
  if (!guild) {
    replier.reply("[ 싱봇 광산 ] 길드에 가입되어 있지 않습니다.\n'ㄱㅅ길드생성 [이름]' 또는 'ㄱㅅ길드가입 [길드명]'");
    return;
  }

  var levelExp = MineData.guild.levelUpExp;
  var maxExp = guild.level <= levelExp.length ? levelExp[guild.level - 1] : 999999;
  var bonus = getGuildBonus(account);

  var text = "[ 싱봇 광산 ] 🏰 " + guild.name + "\n";
  text += "━━━━━━━━━━━━━━━━━━\n";
  text += "길드장: " + guild.leader + "\n";
  text += "레벨: " + guild.level + " (경험치: " + guild.exp + "/" + maxExp + ")\n";
  text += "멤버: " + guild.members.length + "/" + MineData.guild.maxMembers + "\n";
  text += "길드 깊이: " + guild.guildDepth + "\n";
  text += "총 채굴: " + guild.totalMined + "\n\n";

  text += "📊 길드 보너스:\n";
  if (bonus.miningPower > 0) text += "  ⛏️ 채굴력 +" + Math.round(bonus.miningPower * 100) + "%\n";
  if (bonus.idleRate > 0) text += "  💤 방치 +" + Math.round(bonus.idleRate * 100) + "%\n";
  if (bonus.luck > 0) text += "  🍀 행운 +" + Math.round(bonus.luck * 100) + "%\n";

  text += "\n👥 멤버:\n";
  for (var i = 0; i < guild.members.length; i++) {
    var m = guild.members[i];
    var mAcc = loadMineAccount(m);
    var prefix = m === guild.leader ? "👑 " : "  ";
    text += prefix + (mAcc ? mAcc.name : m) + "\n";
  }

  if (guild.joinRequests.length > 0) {
    text += "\n📩 가입 요청: " + guild.joinRequests.length + "건";
  }

  replier.reply(text);
}

function handleMineGuildMine(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account) { replier.reply("[ 싱봇 광산 ] 먼저 'ㄱㅅ등록 [이름]'으로 등록하세요!"); return; }

  var guild = getPlayerGuild(sender);
  if (!guild) {
    replier.reply("[ 싱봇 광산 ] 길드에 가입되어 있지 않습니다.");
    return;
  }

  regenStamina(account);
  if (account.stamina <= 0) {
    replier.reply("[ 싱봇 광산 ] 체력이 부족합니다!");
    return;
  }

  account.stamina--;
  account.lastActiveTime = Date.now();

  // 길드 채굴: 개인 파워 기반으로 길드 경험치 + 깊이 기여
  var power = getPickaxePower(account);
  var contribution = Math.floor(power * (1 + Math.random() * 0.5));

  guild.exp += contribution;
  guild.totalMined++;
  guild.guildDepth += 1;

  // 레벨업 체크
  var levelExp = MineData.guild.levelUpExp;
  var leveledUp = false;
  while (guild.level <= levelExp.length && guild.exp >= levelExp[guild.level - 1]) {
    guild.exp -= levelExp[guild.level - 1];
    guild.level++;
    leveledUp = true;
  }

  // 개인 보상 (소량 골드 + 자원)
  var goldReward = Math.floor(10 + power * 0.5);
  account.gold += goldReward;
  account.totalGoldEarned += goldReward;

  saveMineAccount(sender, account);
  saveMineGuilds();

  var text = "[ 싱봇 광산 ] 🏰 길드 채굴!\n\n";
  text += "기여도: +" + contribution + " exp\n";
  text += "개인 보상: +" + goldReward + "G\n";
  text += "길드 깊이: " + guild.guildDepth + "\n";
  text += "체력: " + account.stamina + "/" + account.maxStamina + "\n";

  if (leveledUp) {
    text += "\n🎉 길드 레벨 업! Lv." + guild.level + "\n";
    var bonus = getGuildBonus(account);
    if (bonus.miningPower > 0) text += "  ⛏️ 채굴력 +" + Math.round(bonus.miningPower * 100) + "%\n";
    if (bonus.idleRate > 0) text += "  💤 방치 +" + Math.round(bonus.idleRate * 100) + "%\n";
    if (bonus.luck > 0) text += "  🍀 행운 +" + Math.round(bonus.luck * 100) + "%\n";
  }

  replier.reply(text);
}

function handleMineGuildLeave(room, msg, sender, replier) {
  var account = loadMineAccount(sender);
  if (!account || !account.guild) {
    replier.reply("[ 싱봇 광산 ] 길드에 가입되어 있지 않습니다.");
    return;
  }

  var guild = mineGuilds[account.guild];
  if (!guild) {
    account.guild = null;
    saveMineAccount(sender, account);
    replier.reply("[ 싱봇 광산 ] 길드를 탈퇴했습니다.");
    return;
  }

  if (guild.leader === sender) {
    // 길드장이 탈퇴 → 길드 해산
    for (var i = 0; i < guild.members.length; i++) {
      var mAcc = loadMineAccount(guild.members[i]);
      if (mAcc) {
        mAcc.guild = null;
        saveMineAccount(guild.members[i], mAcc);
      }
    }
    delete mineGuilds[guild.name];
    saveMineGuilds();
    replier.reply("[ 싱봇 광산 ] 길드장 탈퇴로 " + guild.name + " 길드가 해산되었습니다.");
    return;
  }

  // 일반 멤버 탈퇴
  var idx = -1;
  for (var i = 0; i < guild.members.length; i++) {
    if (guild.members[i] === sender) { idx = i; break; }
  }
  if (idx !== -1) guild.members.splice(idx, 1);

  account.guild = null;
  saveMineAccount(sender, account);
  saveMineGuilds();

  replier.reply("[ 싱봇 광산 ] " + guild.name + " 길드를 탈퇴했습니다.");
}

// 길드 보너스를 기존 계산에 반영하는 래퍼
// (getPickaxePower, calculateIdleIncome, doMine에서 이미 prestige/codex/equip 보너스 적용 중)
// 길드 보너스는 채굴 시 추가 적용

// 길드 데이터 초기 로드
try { loadMineGuilds(); } catch (e) {}

// ===== 커맨드 등록 =====

// 광산 → ㄱㅅ/ㄳ 축약어 자동 생성
function _mt(triggers) {
  var result = [];
  for (var i = 0; i < triggers.length; i++) {
    result.push(triggers[i]);
    if (triggers[i].indexOf("광산") === 0) {
      result.push("ㄱㅅ" + triggers[i].substring(2));
      result.push("ㄳ" + triggers[i].substring(2));
    }
  }
  return result;
}

var MINE_COMMANDS = [
  { triggers: _mt(["광산등록"]), handler: handleMineRegister, hasArgs: true },
  { triggers: ["채굴", "ㅊㄱ"], handler: handleMine },
  { triggers: _mt(["광산정보"]), handler: handleMineInfo },
  { triggers: ["곡괭이강화"], handler: handlePickaxeUpgrade },
  { triggers: ["곡괭이정보"], handler: handlePickaxeInfo },
  { triggers: _mt(["광산인벤토리", "광산인벤"]), handler: handleMineInventory },
  { triggers: _mt(["광산도감"]), handler: handleMineCodex },
  { triggers: _mt(["광산상점"]), handler: handleMineShop },
  { triggers: _mt(["광산구매"]), handler: handleMineBuy, hasArgs: true },
  { triggers: _mt(["광산판매"]), handler: handleMineSell, hasArgs: true },
  // Phase 2: 장비
  { triggers: _mt(["광산장비"]), handler: handleMineEquipInfo },
  { triggers: _mt(["광산제작목록"]), handler: handleMineCraftList },
  { triggers: _mt(["광산제작"]), handler: handleMineCraft, hasArgs: true },
  { triggers: _mt(["광산해제"]), handler: handleMineUnequip, hasArgs: true },
  // Phase 2: 거래
  { triggers: _mt(["광산거래신청"]), handler: handleMineTradeRequest, hasArgs: true },
  { triggers: _mt(["광산거래수락"]), handler: handleMineTradeAccept },
  { triggers: _mt(["광산거래거절"]), handler: handleMineTradeDecline },
  { triggers: _mt(["광산거래올리기"]), handler: handleMineTradeOffer, hasArgs: true },
  { triggers: _mt(["광산거래골드"]), handler: handleMineTradeGold, hasArgs: true },
  { triggers: _mt(["광산거래확인"]), handler: handleMineTradeConfirm },
  { triggers: _mt(["광산거래취소"]), handler: handleMineTradeCancel },
  // Phase 2: 시장
  { triggers: _mt(["광산시장"]), handler: handleMineMarket },
  { triggers: _mt(["광산시장판매"]), handler: handleMineMarketSell, hasArgs: true },
  // Phase 3: 프레스티지
  { triggers: _mt(["광산환생"]), handler: handleMinePrestige, hasArgs: true },
  { triggers: _mt(["광산환생상점"]), handler: handleMinePrestigeShop },
  { triggers: _mt(["광산환생구매"]), handler: handleMinePrestigeBuy, hasArgs: true },
  // Phase 3: PvP
  { triggers: _mt(["광산대결"]), handler: handleMineDuel, hasArgs: true },
  { triggers: _mt(["광산대결수락"]), handler: handleMineDuelAccept },
  { triggers: _mt(["광산대결거절"]), handler: handleMineDuelDecline },
  { triggers: _mt(["광산캐기"]), handler: handleMineDig },
  { triggers: _mt(["광산항복"]), handler: handleMineSurrender },
  // Phase 3: 랭킹
  { triggers: _mt(["광산랭킹"]), handler: handleMineRanking },
  // Phase 4: 길드
  { triggers: _mt(["광산길드생성"]), handler: handleMineGuildCreate, hasArgs: true },
  { triggers: _mt(["광산길드가입"]), handler: handleMineGuildJoin, hasArgs: true },
  { triggers: _mt(["광산길드수락"]), handler: handleMineGuildAccept, hasArgs: true },
  { triggers: _mt(["광산길드정보"]), handler: handleMineGuildInfo },
  { triggers: _mt(["광산길드채굴"]), handler: handleMineGuildMine },
  { triggers: _mt(["광산길드탈퇴"]), handler: handleMineGuildLeave },
  { triggers: _mt(["광산도움말"]), handler: handleMineHelp },
  // Phase 5: 도박/퀘스트/칭호
  { triggers: _mt(["광산도박"]), handler: handleGamble, hasArgs: true },
  { triggers: _mt(["광산도박기록"]), handler: handleGambleStats },
  { triggers: _mt(["광산퀘스트보상"]), handler: handleDailyQuestClaim },
  { triggers: _mt(["광산퀘스트"]), handler: handleDailyQuest },
  { triggers: _mt(["광산상인구매"]), handler: handleMerchantBuy },
  { triggers: _mt(["광산칭호장착"]), handler: handleTitleEquip, hasArgs: true },
  { triggers: _mt(["광산칭호"]), handler: handleTitleList }
];
// ===== 정답/힌트/포기 핸들러 =====

function handleAnswer(room, msg, sender, replier) {
  var quiz = roomState[room].activeQuiz;
  var userAnswer = msg.replace(/^정답[\s:]+/, "").trim();

  if (!userAnswer) {
    replier.reply("'정답 [답]' 형식으로 입력해주세요!");
    return;
  }

  var correctAnswer = stripParens(quiz.answer);
  if (normalize(userAnswer) === normalize(correctAnswer)) {
    var elapsed = Math.round((Date.now() - quiz.startTime) / 1000);
    addScore(room, quiz.type, sender, true);

    replier.reply(
      "[ 싱봇 ] 정답입니다! 🎉\n\n" +
      sender + "님 대단해요!\n" +
      "정답: " + quiz.answer + "\n" +
      "소요시간: " + elapsed + "초"
    );

    roomState[room].activeQuiz = null;
  } else {
    addScore(room, quiz.type, sender, false);

    // 맞은 글자 공개
    var normUser = normalize(userAnswer);
    var normCorrect = normalize(correctAnswer);
    if (!quiz.revealedChars) quiz.revealedChars = {};
    for (var i = 0; i < normUser.length && i < normCorrect.length; i++) {
      if (normUser[i] === normCorrect[i]) {
        quiz.revealedChars[i] = normCorrect[i];
      }
    }
    var matchDisplay = "";
    for (var i = 0; i < normCorrect.length; i++) {
      matchDisplay += quiz.revealedChars[i] ? quiz.revealedChars[i] : "○";
    }

    replier.reply("• " + matchDisplay + " 틀렸습니다!");
  }
}

function handleHint(room, msg, sender, replier) {
  var quiz = roomState[room].activeQuiz;
  var correctAnswer = normalize(stripParens(quiz.answer));

  // 이미 공개된 글자가 1/2 이상이면 현재 상태만 보여줌
  var revealedCount = 0;
  if (quiz.revealedChars) {
    for (var k in quiz.revealedChars) {
      if (quiz.revealedChars.hasOwnProperty(k)) revealedCount++;
    }
  }

  if (revealedCount >= Math.ceil(correctAnswer.length / 2)) {
    var current = "";
    for (var i = 0; i < correctAnswer.length; i++) {
      current += (quiz.revealedChars && quiz.revealedChars[i]) ? quiz.revealedChars[i] : "○";
    }
    replier.reply("[ 싱봇 ] 힌트: " + current + "\n이미 절반 이상 공개됐어요!");
    return;
  }

  if (!quiz.revealedChars) quiz.revealedChars = {};

  // 아직 안 공개된 글자 중 랜덤 1개만 추가 공개
  var answerChars = stripParens(quiz.answer).split("");
  var hidden = [];
  for (var i = 0; i < answerChars.length; i++) {
    if (!quiz.revealedChars[i]) hidden.push(i);
  }

  if (hidden.length > 0) {
    var pick = hidden[Math.floor(Math.random() * hidden.length)];
    quiz.revealedChars[pick] = answerChars[pick];
  }

  var display = "";
  for (var i = 0; i < answerChars.length; i++) {
    display += quiz.revealedChars[i] ? quiz.revealedChars[i] : "○";
  }

  replier.reply("[ 싱봇 ] 힌트: " + display);
}

function handleSkip(room, msg, sender, replier) {
  var quiz = roomState[room].activeQuiz;
  var text = "[ 싱봇 ] 정답은 '" + stripParens(quiz.answer) + "' 이었습니다!\n";
  if (quiz.reason) {
    text += "이유: " + quiz.reason + "\n";
  }
  text += "다시 도전하려면 '" + (quiz.type === "nonsense" ? "넌센스" : "캐치마인드") + "'를 입력하세요.";
  replier.reply(text);
  roomState[room].activeQuiz = null;
}

// ===== 도움말 =====

function handleHelp(room, msg, sender, replier) {
  replier.reply(
    "[ 싱봇 도움말 ]\n\n" +
    "🧩 넌센스 — 넌센스 퀴즈 시작\n" +
    "🎨 캐치마인드 — 단어 맞추기 시작\n" +
    "📖 오늘의한마디 — 오늘의 명언\n" +
    "🔮 운세 성별 생년월일\n" +
    "   예) 운세 여자 19901122\n" +
    "   예) 오늘의운세,남,19901122\n" +
    "⚡ vs 선택지1 선택지2 ...\n" +
    "   예) vs 짜장면 짬뽕 탕수육\n\n" +
    "퀴즈 중 명령어:\n" +
    "  정답 [답] — 정답 제출\n" +
    "  힌트 — 힌트 보기\n" +
    "  포기 — 정답 공개\n\n" +
    "넌센스점수판 / 캐치점수판\n\n" +
    "🤖 ~ <질문> — AI 대화\n" +
    "🔄 대화초기화 — AI 대화 기록 리셋\n\n" +
    "⛏️ ㄱㅅ등록 [이름] — 광산 게임 시작\n" +
    "⛏️ ㄱㅅ도움말 — 광산 명령어 보기"
  );
}

// ===== 싱봇 명령어 라우터 =====

var COMMANDS = [
  { triggers: ["~"], handler: handleChat, hasArgs: true },
  { triggers: ["대화초기화"], handler: handleChatReset },
  { triggers: ["넌센스", "넌센스퀴즈"], handler: handleNonsenseStart },
  { triggers: ["캐치마인드", "캐치"], handler: handleCatchmindStart },
  { triggers: ["오늘의한마디"], handler: handleDailyQuote },
  { triggers: ["운세", "오늘의운세"], handler: handleFortune, hasArgs: true },
  { triggers: ["vs", "VS"], handler: handleVsPick, hasArgs: true },
  { triggers: ["넌센스점수판"], handler: handleNonsenseLeaderboard },
  { triggers: ["캐치점수판"], handler: handleCatchmindLeaderboard },
  { triggers: ["도움말", "명령어"], handler: handleHelp }
];

var CONTEXT_COMMANDS = [
  { prefix: "정답 ", handler: handleAnswer },
  { prefix: "정답:", handler: handleAnswer },
  { triggers: ["힌트"], handler: handleHint },
  { triggers: ["포기", "스킵"], handler: handleSkip }
];

function _response(room, msg, sender, isGroupChat, replier, imageDB, packageName) {
  var trimmed = msg.trim();

  // 1. 퀴즈 진행 중이면 컨텍스트 명령 먼저
  if (roomState[room] && roomState[room].activeQuiz) {
    for (var i = 0; i < CONTEXT_COMMANDS.length; i++) {
      var cmd = CONTEXT_COMMANDS[i];
      if (cmd.prefix && trimmed.indexOf(cmd.prefix) === 0) {
        cmd.handler(room, trimmed, sender, replier);
        return;
      }
      if (cmd.triggers) {
        for (var j = 0; j < cmd.triggers.length; j++) {
          if (trimmed === cmd.triggers[j]) {
            cmd.handler(room, trimmed, sender, replier);
            return;
          }
        }
      }
    }
  }

  // 2. 전역 명령 (COMMANDS + MINE_COMMANDS)
  var allCommands = COMMANDS;
  if (typeof MINE_COMMANDS !== "undefined") {
    allCommands = COMMANDS.concat(MINE_COMMANDS);
  }
  for (var i = 0; i < allCommands.length; i++) {
    var cmd = allCommands[i];
    for (var j = 0; j < cmd.triggers.length; j++) {
      var trigger = cmd.triggers[j];
      if (cmd.hasArgs) {
        if (trimmed.indexOf(trigger + " ") === 0 || trimmed === trigger) {
          cmd.handler(room, trimmed, sender, replier);
          return;
        }
      } else {
        if (trimmed === trigger) {
          cmd.handler(room, trimmed, sender, replier);
          return;
        }
      }
    }
  }
}

function response(room, msg, sender, isGroupChat, replier, imageDB, packageName) {
  if (packageName !== "com.kakao.talk") return;
  try {
    _response(room, msg, sender, isGroupChat, replier, imageDB, packageName);
  } catch (e) {
    Log.e("싱봇 오류: " + e.toString());
  }
}
